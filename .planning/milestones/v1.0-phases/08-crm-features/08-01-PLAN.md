---
phase: 08-crm-features
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/crates/crm-demo/src/entities/note.rs
  - backend/crates/crm-demo/src/entities/tag.rs
  - backend/crates/crm-demo/src/entities/contact_tag.rs
  - backend/crates/crm-demo/src/entities/interaction.rs
  - backend/crates/crm-demo/src/entities/mod.rs
  - backend/crates/crm-demo/src/migration/m20260323_000005_create_note.rs
  - backend/crates/crm-demo/src/migration/m20260323_000006_create_tag.rs
  - backend/crates/crm-demo/src/migration/m20260323_000007_create_contact_tag.rs
  - backend/crates/crm-demo/src/migration/m20260323_000008_create_interaction.rs
  - backend/crates/crm-demo/src/migration/mod.rs
  - backend/crates/crm-demo/src/seed.rs
autonomous: true
requirements: [CRM-06, CRM-08, CRM-10]
must_haves:
  truths:
    - "Four new tables exist in SQLite: note, tag, contact_tag, interaction"
    - "Entities compile and have correct SeaORM relations defined"
    - "Seed data populates tags, notes, and interactions for demo"
  artifacts:
    - path: "backend/crates/crm-demo/src/entities/note.rs"
      provides: "Note entity with nullable FK to contact and company"
      contains: "table_name = \"note\""
    - path: "backend/crates/crm-demo/src/entities/tag.rs"
      provides: "Tag entity with unique name"
      contains: "table_name = \"tag\""
    - path: "backend/crates/crm-demo/src/entities/contact_tag.rs"
      provides: "Junction table entity for contact-tag M:N"
      contains: "table_name = \"contact_tag\""
    - path: "backend/crates/crm-demo/src/entities/interaction.rs"
      provides: "Interaction entity with type enum, contact FK"
      contains: "table_name = \"interaction\""
    - path: "backend/crates/crm-demo/src/migration/mod.rs"
      provides: "Migrator with all 8 migrations registered"
      contains: "m20260323_000008_create_interaction"
  key_links:
    - from: "backend/crates/crm-demo/src/entities/note.rs"
      to: "contact and company entities"
      via: "nullable FK relations"
      pattern: "NoteContact|NoteCompany"
    - from: "backend/crates/crm-demo/src/entities/contact_tag.rs"
      to: "contact and tag entities"
      via: "composite primary key"
      pattern: "primary_key.*auto_increment = false"
---

<objective>
Create database migrations, SeaORM entities, and seed data for the four new tables needed by Phase 8 features: note, tag, contact_tag (junction), and interaction.

Purpose: All subsequent plans (notes, tags/search/filter, interactions) depend on these entities existing and compiling.
Output: 4 migration files, 4 entity files, updated entity mod.rs, updated migration mod.rs, updated seed.rs with demo data.
</objective>

<execution_context>
@/home/oetiker/.claude/get-shit-done/workflows/execute-plan.md
@/home/oetiker/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-crm-features/08-CONTEXT.md
@.planning/phases/08-crm-features/08-RESEARCH.md

<interfaces>
<!-- Existing entity pattern to follow -->

From backend/crates/crm-demo/src/entities/contact.rs:
```rust
use sea_orm::entity::prelude::*;
use serde::{Deserialize, Serialize};

#[derive(Clone, Debug, PartialEq, DeriveEntityModel, Serialize, Deserialize)]
#[sea_orm(table_name = "contact")]
pub struct Model {
    #[sea_orm(primary_key)]
    pub contact_id: i32,
    pub contact_name: String,
    // ...
}

#[derive(Copy, Clone, Debug, EnumIter, DeriveRelation)]
pub enum Relation {
    #[sea_orm(belongs_to = "super::company::Entity", from = "Column::ContactCompany", to = "super::company::Column::CompanyId")]
    Company,
}
impl Related<super::company::Entity> for Entity {
    fn to() -> RelationDef { Relation::Company.def() }
}
impl ActiveModelBehavior for ActiveModel {}
```

From backend/crates/crm-demo/src/migration/m20260323_000004_create_contact.rs:
```rust
use sea_orm_migration::prelude::*;
pub struct Migration;
impl MigrationName for Migration {
    fn name(&self) -> &'static str { "m20260323_000004_create_contact" }
}
#[async_trait::async_trait]
impl MigrationTrait for Migration {
    async fn up(&self, manager: &SchemaManager) -> Result<(), DbErr> {
        manager.get_connection().execute_unprepared("CREATE TABLE ...").await?;
        Ok(())
    }
    async fn down(&self, manager: &SchemaManager) -> Result<(), DbErr> {
        manager.get_connection().execute_unprepared("DROP TABLE IF EXISTS contact").await?;
        Ok(())
    }
}
```

From backend/crates/crm-demo/src/entities/mod.rs:
```rust
pub mod audit_log;
pub mod company;
pub mod contact;
pub mod user;
```

From backend/crates/crm-demo/src/migration/mod.rs:
```rust
mod m20260323_000001_create_user;
mod m20260323_000002_create_audit_log;
mod m20260323_000003_create_company;
mod m20260323_000004_create_contact;
pub struct Migrator;
// migrations() returns vec of all 4 Boxes
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create migrations and entity files for note, tag, contact_tag, interaction</name>
  <files>
    backend/crates/crm-demo/src/migration/m20260323_000005_create_note.rs,
    backend/crates/crm-demo/src/migration/m20260323_000006_create_tag.rs,
    backend/crates/crm-demo/src/migration/m20260323_000007_create_contact_tag.rs,
    backend/crates/crm-demo/src/migration/m20260323_000008_create_interaction.rs,
    backend/crates/crm-demo/src/migration/mod.rs,
    backend/crates/crm-demo/src/entities/note.rs,
    backend/crates/crm-demo/src/entities/tag.rs,
    backend/crates/crm-demo/src/entities/contact_tag.rs,
    backend/crates/crm-demo/src/entities/interaction.rs,
    backend/crates/crm-demo/src/entities/mod.rs
  </files>
  <read_first>
    backend/crates/crm-demo/src/migration/m20260323_000004_create_contact.rs,
    backend/crates/crm-demo/src/migration/mod.rs,
    backend/crates/crm-demo/src/entities/contact.rs,
    backend/crates/crm-demo/src/entities/mod.rs
  </read_first>
  <action>
Create 4 migration files following the exact pattern from m20260323_000004_create_contact.rs (raw SQL via execute_unprepared, MigrationName impl, up/down methods):

**m20260323_000005_create_note.rs** - SQL:
```sql
CREATE TABLE note (
    note_id INTEGER PRIMARY KEY AUTOINCREMENT,
    note_contact INTEGER REFERENCES contact(contact_id) ON DELETE CASCADE,
    note_company INTEGER REFERENCES company(company_id) ON DELETE CASCADE,
    note_text TEXT NOT NULL,
    note_user INTEGER NOT NULL REFERENCES user(user_id),
    note_created_at TEXT NOT NULL DEFAULT (datetime('now'))
)
```

**m20260323_000006_create_tag.rs** - SQL:
```sql
CREATE TABLE tag (
    tag_id INTEGER PRIMARY KEY AUTOINCREMENT,
    tag_name TEXT NOT NULL UNIQUE
)
```

**m20260323_000007_create_contact_tag.rs** - SQL:
```sql
CREATE TABLE contact_tag (
    contact_tag_contact INTEGER NOT NULL REFERENCES contact(contact_id) ON DELETE CASCADE,
    contact_tag_tag INTEGER NOT NULL REFERENCES tag(tag_id) ON DELETE CASCADE,
    PRIMARY KEY (contact_tag_contact, contact_tag_tag)
)
```

**m20260323_000008_create_interaction.rs** - SQL:
```sql
CREATE TABLE interaction (
    interaction_id INTEGER PRIMARY KEY AUTOINCREMENT,
    interaction_contact INTEGER NOT NULL REFERENCES contact(contact_id) ON DELETE CASCADE,
    interaction_type TEXT NOT NULL CHECK(interaction_type IN ('call', 'email', 'meeting')),
    interaction_subject TEXT NOT NULL,
    interaction_notes TEXT,
    interaction_user INTEGER NOT NULL REFERENCES user(user_id),
    interaction_date TEXT NOT NULL,
    interaction_created_at TEXT NOT NULL DEFAULT (datetime('now'))
)
```

Update `migration/mod.rs` to add all 4 new migration modules and register them in `migrations()` vec.

Create 4 entity files following the contact.rs pattern (DeriveEntityModel, Serialize, Deserialize, Relations, ActiveModelBehavior):

**entities/note.rs**: Fields: note_id (PK), note_contact (Option<i32>), note_company (Option<i32>), note_text (String), note_user (i32), note_created_at (String). Relations: belongs_to Contact (from NoteContact to ContactId), belongs_to Company (from NoteCompany to CompanyId), belongs_to User (from NoteUser to UserId).

**entities/tag.rs**: Fields: tag_id (PK), tag_name (String). Relations: empty enum (related through junction table).

**entities/contact_tag.rs**: Fields: contact_tag_contact (i32, `primary_key, auto_increment = false`), contact_tag_tag (i32, `primary_key, auto_increment = false`). Relations: belongs_to Contact, belongs_to Tag. Both PK columns need `#[sea_orm(primary_key, auto_increment = false)]`.

**entities/interaction.rs**: Fields: interaction_id (PK), interaction_contact (i32), interaction_type (String), interaction_subject (String), interaction_notes (Option<String>), interaction_user (i32), interaction_date (String), interaction_created_at (String). Relations: belongs_to Contact (from InteractionContact to ContactId).

Update `entities/mod.rs` to add: `pub mod note; pub mod tag; pub mod contact_tag; pub mod interaction;`
  </action>
  <verify>
    <automated>cd /home/oetiker/checkouts/marionette/backend && cargo check -p crm-demo 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep -q 'table_name = "note"' backend/crates/crm-demo/src/entities/note.rs
    - grep -q 'table_name = "tag"' backend/crates/crm-demo/src/entities/tag.rs
    - grep -q 'table_name = "contact_tag"' backend/crates/crm-demo/src/entities/contact_tag.rs
    - grep -q 'table_name = "interaction"' backend/crates/crm-demo/src/entities/interaction.rs
    - grep -q 'm20260323_000008_create_interaction' backend/crates/crm-demo/src/migration/mod.rs
    - grep -q 'pub mod interaction' backend/crates/crm-demo/src/entities/mod.rs
    - grep -q 'auto_increment = false' backend/crates/crm-demo/src/entities/contact_tag.rs
    - cargo check -p crm-demo succeeds with no errors
  </acceptance_criteria>
  <done>All 4 migration files and 4 entity files exist, compile, and have correct relations. Migration mod registers all 8 migrations. Entity mod exports all 8 entity modules.</done>
</task>

<task type="auto">
  <name>Task 2: Add seed data for tags, notes, and interactions</name>
  <files>backend/crates/crm-demo/src/seed.rs, backend/crates/crm-demo/src/main.rs</files>
  <read_first>
    backend/crates/crm-demo/src/seed.rs,
    backend/crates/crm-demo/src/main.rs,
    backend/crates/crm-demo/src/entities/note.rs,
    backend/crates/crm-demo/src/entities/tag.rs,
    backend/crates/crm-demo/src/entities/contact_tag.rs,
    backend/crates/crm-demo/src/entities/interaction.rs
  </read_first>
  <action>
Add three new seed functions to seed.rs following the existing seed_companies/seed_contacts pattern (check count > 0 to skip if already seeded, use NotSet for auto-increment PKs and timestamps):

**seed_tags(db)**: Create 5 demo tags if tag table is empty: "VIP", "Lead", "Partner", "Inactive", "Newsletter". Then link tags to existing contacts via contact_tag junction: Alice gets "VIP" + "Partner", Bob gets "Lead" + "Newsletter", Carol gets "Inactive".

**seed_notes(db)**: Create 3 demo notes if note table is empty: one note on Alice (contact), one note on Bob (contact), one note on Acme Corp (company). Set note_user to 1 (admin). Use NotSet for note_created_at to get DB default.

**seed_interactions(db)**: Create 3 demo interactions if interaction table is empty: a "call" on Alice with subject "Initial outreach", an "email" on Bob with subject "Follow-up proposal", a "meeting" on Alice with subject "Contract review". Set interaction_user to 1, interaction_date to "2026-03-20 10:00:00", "2026-03-21 14:00:00", "2026-03-22 09:00:00" respectively. Use NotSet for interaction_created_at.

Update main.rs to call `seed::seed_tags(&db)`, `seed::seed_notes(&db)`, `seed::seed_interactions(&db)` after `seed_contacts`, each with `.await.expect("failed to seed ...")`.

To look up contact/company IDs for seeding tags, notes, and interactions: query by name (same pattern as seed_contacts does for companies).
  </action>
  <verify>
    <automated>cd /home/oetiker/checkouts/marionette/backend && cargo test -p crm-demo 2>&1 | tail -10 && cargo clippy -p crm-demo -- -D warnings 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - grep -q 'seed_tags' backend/crates/crm-demo/src/seed.rs
    - grep -q 'seed_notes' backend/crates/crm-demo/src/seed.rs
    - grep -q 'seed_interactions' backend/crates/crm-demo/src/seed.rs
    - grep -q 'seed_tags' backend/crates/crm-demo/src/main.rs
    - grep -q 'seed_notes' backend/crates/crm-demo/src/main.rs
    - grep -q 'seed_interactions' backend/crates/crm-demo/src/main.rs
    - cargo test -p crm-demo passes
    - cargo clippy -p crm-demo -- -D warnings passes
  </acceptance_criteria>
  <done>Seed functions create demo tags (5), contact-tag links (5), notes (3), and interactions (3). Main.rs calls all seed functions. Project compiles and passes clippy.</done>
</task>

</tasks>

<verification>
- `cargo check -p crm-demo` compiles without errors
- `cargo test -p crm-demo` passes
- `cargo clippy -p crm-demo -- -D warnings` passes
- All 4 entity files exist with correct table_name annotations
- All 4 migration files exist with correct SQL DDL
- migration/mod.rs registers all 8 migrations
- entities/mod.rs exports all 8 entity modules
- seed.rs has seed_tags, seed_notes, seed_interactions functions
- main.rs calls all seed functions
</verification>

<success_criteria>
The crm-demo crate compiles cleanly with 4 new entities (note, tag, contact_tag, interaction), 4 new migrations, and seed data for all new tables. This foundation enables Plans 02-04 to build handlers and UI on top.
</success_criteria>

<output>
After completion, create `.planning/phases/08-crm-features/08-01-SUMMARY.md`
</output>
