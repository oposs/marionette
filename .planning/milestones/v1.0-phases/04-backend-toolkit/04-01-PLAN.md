---
phase: 04-backend-toolkit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/Cargo.toml
  - backend/crates/marionette-protocol/Cargo.toml
  - backend/crates/marionette-protocol/src/lib.rs
  - backend/crates/marionette-protocol/src/common.rs
  - backend/crates/marionette-protocol/src/component.rs
  - backend/crates/marionette-protocol/src/data.rs
  - backend/crates/marionette-protocol/src/messages.rs
autonomous: true
requirements:
  - BACK-03
  - BACK-11

must_haves:
  truths:
    - "All 6 protocol message types (hello, render, patch, action, event, error) serialize/deserialize to JSON matching the OpenAPI spec"
    - "Component, ComponentAction, PatchOperation, ValidationError structs match spec/schemas/*.yaml exactly"
    - "Round-trip encoding (Rust -> JSON -> Rust) preserves all fields including optional ones"
    - "JSON output uses lowercase type discriminator tag matching spec convention"
  artifacts:
    - path: "backend/crates/marionette-protocol/src/messages.rs"
      provides: "6 message types as serde tagged enum"
      contains: "pub enum ProtocolMessage"
    - path: "backend/crates/marionette-protocol/src/component.rs"
      provides: "Component and ComponentAction structs"
      contains: "pub struct Component"
    - path: "backend/crates/marionette-protocol/src/data.rs"
      provides: "PatchOperation, ValidationError, KeyedCollection"
      contains: "pub struct PatchOperation"
    - path: "backend/crates/marionette-protocol/src/common.rs"
      provides: "Surface, JsonPointer, MessageId type aliases"
      contains: "pub type Surface"
  key_links:
    - from: "backend/crates/marionette-protocol/src/messages.rs"
      to: "spec/schemas/message.yaml"
      via: "serde tag + field names match schema"
      pattern: 'serde\(tag = "type"'
    - from: "backend/crates/marionette-protocol/src/component.rs"
      to: "spec/schemas/component.yaml"
      via: "struct fields match schema properties"
      pattern: "pub struct Component"
---

<objective>
Set up workspace dependencies and implement all protocol types in marionette-protocol crate.

Purpose: The protocol types are the foundation for every other crate -- macros generate code referencing these types, the framework crate uses them for message routing, and the crm-demo sends/receives them.
Output: Fully typed, serde-compatible protocol message types with comprehensive round-trip tests.
</objective>

<execution_context>
@/home/oetiker/.claude/get-shit-done/workflows/execute-plan.md
@/home/oetiker/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-backend-toolkit/04-CONTEXT.md
@.planning/phases/04-backend-toolkit/04-RESEARCH.md

<interfaces>
<!-- Source of truth: spec/schemas/*.yaml -->

From spec/schemas/message.yaml:
- HelloMessage: { type: "hello", version: string }
- RenderMessage: { type: "render", id?: string, surface: string, root: string, nodes: map<string, Component>, data: object }
- PatchMessage: { type: "patch", id?: string, patch: PatchOperation[] }
- ActionMessage: { type: "action", id?: string, name: string, source?: string, payload?: object, optimistic?: { patch: PatchOperation[] } }
- EventMessage: { type: "event", id?: string, name: string, surface?: string, hint?: object }
- ErrorMessage: { type: "error", id?: string, errors: ValidationError[] }

From spec/schemas/component.yaml:
- Component: { type: string, props?: object, children?: string[], bind?: string, action?: ComponentAction, visible?: string }
- ComponentAction: { type: string, name?: string, target?: string, idPath?: string, ...additionalProperties }

From spec/schemas/data.yaml:
- PatchOperation: { path: string, value: any }
- ValidationError: { path?: string, message: string }
- KeyedCollection: map<string, object>

From spec/schemas/common.yaml:
- Surface: string (e.g., "main", "sidebar", "modal", "toast")
- JsonPointer: string matching ^(/[^/]*)*$
- MessageId: string (e.g., "msg-a1b2c3")
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add workspace dependencies and create protocol type modules</name>
  <files>
    backend/Cargo.toml,
    backend/crates/marionette-protocol/Cargo.toml,
    backend/crates/marionette-protocol/src/lib.rs,
    backend/crates/marionette-protocol/src/common.rs,
    backend/crates/marionette-protocol/src/component.rs,
    backend/crates/marionette-protocol/src/data.rs,
    backend/crates/marionette-protocol/src/messages.rs
  </files>
  <read_first>
    backend/Cargo.toml,
    backend/crates/marionette-protocol/Cargo.toml,
    backend/crates/marionette-protocol/src/lib.rs,
    spec/schemas/message.yaml,
    spec/schemas/component.yaml,
    spec/schemas/data.yaml,
    spec/schemas/common.yaml
  </read_first>
  <action>
1. Add new workspace dependencies to backend/Cargo.toml [workspace.dependencies]:
   - uuid = { version = "1", features = ["v4", "serde"] }
   - sea-orm = { version = "1.1", features = ["sqlx-sqlite", "runtime-tokio-rustls", "macros"] }
   - sea-orm-migration = { version = "1.1", features = ["sqlx-sqlite", "runtime-tokio-rustls"] }
   - darling = "0.23"
   - futures = "0.3"
   - syn = { version = "2", features = ["full"] }
   - quote = "1"
   - proc-macro2 = "1"

2. Add uuid dependency to marionette-protocol/Cargo.toml:
   uuid.workspace = true

3. Create backend/crates/marionette-protocol/src/common.rs:
   - pub type Surface = String;
   - pub type JsonPointer = String;
   - pub type MessageId = String;

4. Create backend/crates/marionette-protocol/src/component.rs:
   - pub struct Component with fields: r#type (String, serde rename to "type"), props (Option&lt;serde_json::Value&gt;), children (Option&lt;Vec&lt;String&gt;&gt;), bind (Option&lt;String&gt;), action (Option&lt;ComponentAction&gt;), visible (Option&lt;String&gt;)
   - All optional fields use #[serde(skip_serializing_if = "Option::is_none")]
   - pub struct ComponentAction with fields: r#type (String, serde rename to "type"), name (Option&lt;String&gt;), target (Option&lt;String&gt;), id_path (Option&lt;String&gt;, serde rename to "idPath")
   - ComponentAction also has #[serde(flatten)] pub extra: serde_json::Map&lt;String, serde_json::Value&gt; for additionalProperties
   - Both derive Debug, Clone, Serialize, Deserialize, PartialEq

5. Create backend/crates/marionette-protocol/src/data.rs:
   - pub struct PatchOperation { pub path: String, pub value: serde_json::Value }
   - pub struct ValidationError { pub path: Option&lt;String&gt;, pub message: String } with skip_serializing_if on path
   - Both derive Debug, Clone, Serialize, Deserialize, PartialEq

6. Create backend/crates/marionette-protocol/src/messages.rs:
   - pub enum ProtocolMessage with #[serde(tag = "type", rename_all = "lowercase")]:
     - Hello(HelloMessage)
     - Render(RenderMessage)
     - Patch(PatchMessage)
     - Action(ActionMessage)
     - Event(EventMessage)
     - Error(ErrorMessage)
   - HelloMessage: version (String)
   - RenderMessage: id (Option&lt;MessageId&gt;), surface (Surface), root (String), nodes (HashMap&lt;String, Component&gt;), data (serde_json::Value)
   - PatchMessage: id (Option&lt;MessageId&gt;), patch (Vec&lt;PatchOperation&gt;)
   - ActionMessage: id (Option&lt;MessageId&gt;), name (String), source (Option&lt;String&gt;), payload (Option&lt;serde_json::Value&gt;), optimistic (Option&lt;OptimisticUpdate&gt;)
   - OptimisticUpdate: patch (Vec&lt;PatchOperation&gt;)
   - EventMessage: id (Option&lt;MessageId&gt;), name (String), surface (Option&lt;Surface&gt;), hint (Option&lt;serde_json::Value&gt;)
   - ErrorMessage: id (Option&lt;MessageId&gt;), errors (Vec&lt;ValidationError&gt;)
   - All Option fields use #[serde(skip_serializing_if = "Option::is_none")]
   - All structs derive Debug, Clone, Serialize, Deserialize, PartialEq

7. Update backend/crates/marionette-protocol/src/lib.rs:
   - pub mod common; pub mod component; pub mod data; pub mod messages;
   - pub use common::*; pub use component::*; pub use data::*; pub use messages::*;
   - Keep existing #![warn(clippy::pedantic)] and #![allow(clippy::module_name_repetitions)]
  </action>
  <verify>
    <automated>cd /home/oetiker/checkouts/marionette/backend && cargo check -p marionette-protocol 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - backend/Cargo.toml contains "uuid = " in workspace.dependencies
    - backend/Cargo.toml contains "sea-orm = " in workspace.dependencies
    - backend/Cargo.toml contains "darling = " in workspace.dependencies
    - backend/Cargo.toml contains "syn = " in workspace.dependencies
    - marionette-protocol/src/messages.rs contains 'pub enum ProtocolMessage'
    - marionette-protocol/src/messages.rs contains '#[serde(tag = "type", rename_all = "lowercase")]'
    - marionette-protocol/src/component.rs contains 'pub struct Component'
    - marionette-protocol/src/component.rs contains 'pub struct ComponentAction'
    - marionette-protocol/src/data.rs contains 'pub struct PatchOperation'
    - marionette-protocol/src/data.rs contains 'pub struct ValidationError'
    - marionette-protocol/src/common.rs contains 'pub type Surface = String'
    - marionette-protocol/src/lib.rs contains 'pub mod messages'
    - cargo check -p marionette-protocol exits 0
  </acceptance_criteria>
  <done>All protocol types compile. 6 message variants, Component, ComponentAction, PatchOperation, ValidationError defined with serde derives matching spec schemas.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Protocol type round-trip and conformance tests</name>
  <files>
    backend/crates/marionette-protocol/src/messages.rs,
    backend/crates/marionette-protocol/src/component.rs,
    backend/crates/marionette-protocol/src/data.rs
  </files>
  <read_first>
    backend/crates/marionette-protocol/src/messages.rs,
    backend/crates/marionette-protocol/src/component.rs,
    backend/crates/marionette-protocol/src/data.rs,
    spec/schemas/message.yaml,
    spec/schemas/component.yaml,
    spec/schemas/data.yaml
  </read_first>
  <behavior>
    - hello_round_trip: HelloMessage { version: "1.0.0" } serializes to {"type":"hello","version":"1.0.0"} and deserializes back
    - render_round_trip: RenderMessage with nodes HashMap, surface "main", root "page-1" round-trips correctly; JSON has "type":"render"
    - patch_round_trip: PatchMessage with patch operations round-trips; JSON has "type":"patch"
    - action_round_trip: ActionMessage with name, payload, optimistic round-trips; JSON has "type":"action"
    - event_round_trip: EventMessage with name, surface, hint round-trips; JSON has "type":"event"
    - error_round_trip: ErrorMessage with errors array round-trips; JSON has "type":"error"
    - optional_fields_omitted: Optional fields set to None are not present in serialized JSON (no null values)
    - component_round_trip: Component with all fields set round-trips; type field serializes as "type" not "r#type"
    - component_action_extra_fields: ComponentAction with extra fields in flatten map serializes them at top level
    - patch_operation_round_trip: PatchOperation with path and value round-trips
    - validation_error_round_trip: ValidationError with and without path round-trips
    - deserialize_from_spec_json: Parse example JSON matching spec/schemas format into ProtocolMessage variants
  </behavior>
  <action>
Add #[cfg(test)] mod tests to messages.rs, component.rs, and data.rs. Each test constructs a value, serializes to JSON with serde_json::to_value, checks the JSON shape (type discriminator, field names, optional field absence), then deserializes back and asserts equality. Use serde_json::json! macro for expected JSON comparisons. Compare serde_json::Value objects, NOT string comparisons (avoids HashMap ordering issues per research pitfall #6).
  </action>
  <verify>
    <automated>cd /home/oetiker/checkouts/marionette/backend && cargo test -p marionette-protocol -- --nocapture 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - messages.rs contains '#[cfg(test)]' and 'mod tests'
    - messages.rs contains 'fn hello_round_trip'
    - messages.rs contains 'fn render_round_trip'
    - messages.rs contains 'fn action_round_trip'
    - messages.rs contains 'fn optional_fields_omitted'
    - component.rs contains '#[cfg(test)]' and 'mod tests'
    - component.rs contains 'fn component_round_trip'
    - data.rs contains '#[cfg(test)]' and 'mod tests'
    - data.rs contains 'fn patch_operation_round_trip'
    - cargo test -p marionette-protocol outputs "test result: ok" with 0 failures
  </acceptance_criteria>
  <done>All 12+ round-trip and conformance tests pass. Protocol types verified to serialize/deserialize matching the OpenAPI spec JSON shapes.</done>
</task>

</tasks>

<verification>
cd /home/oetiker/checkouts/marionette/backend && cargo test -p marionette-protocol && cargo clippy -p marionette-protocol -- -D warnings
</verification>

<success_criteria>
- marionette-protocol crate compiles with all types defined
- All 6 message types serialize with correct "type" discriminator tag
- Round-trip tests pass for every message type and data structure
- Optional fields are omitted (not null) when None
- clippy passes with no warnings
</success_criteria>

<output>
After completion, create `.planning/phases/04-backend-toolkit/04-01-SUMMARY.md`
</output>
