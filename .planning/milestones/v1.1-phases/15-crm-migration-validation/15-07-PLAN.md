---
phase: 15-crm-migration-validation
plan: 07
type: execute
wave: 4
depends_on: [15-03, 15-04, 15-05, 15-06]
files_modified:
  - frontend/tests/e2e/company-edit.spec.ts
  - frontend/tests/e2e/user-edit.spec.ts
  - frontend/tests/e2e/interaction-edit.spec.ts
  - frontend/tests/e2e/contact-edit.spec.ts
  - frontend/tests/visual/form.spec.ts
  - frontend/tests/uat/company-edit-uat.spec.ts
  - frontend/tests/uat/user-edit-uat.spec.ts
  - frontend/tests/uat/interaction-edit-uat.spec.ts
  - frontend/tests/uat/contact-tag-add-uat.spec.ts
  - frontend/tests/uat/contact-note-add-uat.spec.ts
  - .planning/phases/15-crm-migration-validation/15-uat-evidence/company-edit/
  - .planning/phases/15-crm-migration-validation/15-uat-evidence/user-edit/
  - .planning/phases/15-crm-migration-validation/15-uat-evidence/interaction-edit/
  - .planning/phases/15-crm-migration-validation/15-uat-evidence/contact-tag-add/
  - .planning/phases/15-crm-migration-validation/15-uat-evidence/contact-note-add/
autonomous: false
requirements: [COMP-03]
tags: [e2e, visual, uat, chrome-mcp, playwright, phase-closure, validation]

must_haves:
  truths:
    - "E2E spec company-edit.spec.ts exists and covers render, FieldSet legend, save, per-field validation (D-A1 + D-D1 + D-E3)"
    - "E2E spec user-edit.spec.ts exists and covers render, two FieldSets, RadioGroup state migration, save, validation"
    - "E2E spec interaction-edit.spec.ts exists and covers render, RadioGroup (not Select), Textarea full_width, save, validation"
    - "contact-edit.spec.ts extended with inline tag-add + inline note-add smoke coverage"
    - "Visual form.spec.ts has 6 new baselines: company-edit/user-edit/interaction-edit × desktop (1280×720) + mobile (375×800)"
    - "UAT evidence folder exists for each of 5 screens with desktop.png + mobile.png + assertions.json + console.log"
    - "Chrome-MCP UAT (preferred) or Playwright UAT fallback (Phase 14 precedent) captures the evidence"
    - "Phase 14 contact-edit visual baseline still matches (D-B2 refactor must not regress)"
    - "All existing E2E tests still pass — no regression from Phase 15 changes"
    - "Goal-backward check: every locked 15-UI-SPEC visual assertion is covered by at least one UAT or E2E spec"
  artifacts:
    - path: "frontend/tests/e2e/company-edit.spec.ts"
      provides: "E2E coverage for company edit form (FieldSet, validation, save)"
    - path: "frontend/tests/e2e/user-edit.spec.ts"
      provides: "E2E coverage for user edit form (2 FieldSets, RadioGroup, validation, save)"
    - path: "frontend/tests/e2e/interaction-edit.spec.ts"
      provides: "E2E coverage for interaction edit form (RadioGroup, Textarea full_width, validation, save)"
    - path: "frontend/tests/visual/form.spec.ts"
      provides: "6 new visual snapshot baselines"
    - path: ".planning/phases/15-crm-migration-validation/15-uat-evidence/"
      provides: "5 evidence folders with screenshots + assertion JSON + console logs per screen"
  key_links:
    - from: "E2E specs"
      to: "window.__mrnSendAction hook"
      via: "page.evaluate"
      pattern: "__mrnSendAction"
    - from: "Visual baselines"
      to: "playwright.config visual tolerance"
      via: "toHaveScreenshot maxDiffPixels: 200"
      pattern: "maxDiffPixels"
    - from: "UAT specs"
      to: ".planning/phases/15-crm-migration-validation/15-uat-evidence/{screen}/"
      via: "fs.writeFileSync path join"
      pattern: "15-uat-evidence"

user_setup:
  - service: playwright-dev-server
    why: "UAT + E2E + visual tests drive a live `make dev` instance; task 3 checkpoint pauses for human verification of UAT evidence."
    dashboard_config:
      - task: "Run `make dev` in one terminal; keep it running while UAT task executes. Verify the dev server is reachable at http://localhost:5173 (or whichever port the project uses) before triggering Chrome-MCP / Playwright UAT."

threat_model:
  trust_boundaries:
    - name: "UAT evidence → repo"
      description: "Screenshots + console logs captured during UAT commit to the repo. Must not include secrets or PII from seed data."
  stride:
    - id: T-15-03-PLAN07
      category: Information Disclosure
      component: "UAT evidence PNG screenshots"
      disposition: accept
      mitigation: "Seed data contains synthetic contacts (Alice/Bob/Carol) with example.com emails — no real PII. Screenshots committed to repo are intentional governance artifacts. Pre-deployment; no production data captured."
    - id: T-15-01
      category: Test-hook leak
      component: "UAT uses __mrnSendAction"
      disposition: mitigate
      mitigation: "Per Plan 05 D-G1: hooks are dev-only. UAT runs under `make dev` which is DEV mode — hooks are present. Production bundles (verified Plan 05 Task 2) strip them. UAT tests cannot run against a production build; this is expected."
    - id: T-15-02
      category: Flowbite regression
      component: "CI guard runs as part of full suite"
      disposition: mitigate
      mitigation: "Plan 06 CI guard runs as part of the `playwright test` full-suite invocation in this plan's phase-gate verification."
---

<objective>
Close Phase 15 with the three test layers 15-VALIDATION and 15-UI-SPEC require: E2E specs per migrated screen (D-H2), visual rebaselines (D-H3), and Chrome-MCP / Playwright UAT evidence per screen (D-H1). Prove the full CRM demo runs on the new component stack end-to-end — every migrated handler renders, validates, and saves correctly.

Purpose: This plan is the phase-gate. It proves the COMP-03 success criteria are met:
1. All CRM screens render and function (E2E + UAT evidence).
2. Zero Flowbite references (enforced by Plan 06 CI guard, re-run in full suite here).
3. CRM navigation / CRUD / search / Listmonk work (E2E suite regression coverage).

Output:
- Three new E2E specs (company, user, interaction edit forms) covering render + FieldSet + validation + save + RadioGroup.
- Extended contact-edit.spec.ts with inline tag-add + note-add smoke.
- Six new visual baselines in form.spec.ts (3 screens × 2 viewports).
- Five UAT evidence folders with screenshots, JSON assertions, console logs.
- Phase-gate verification: full suite (`cargo test --workspace` + `npm run check && npx vitest --run && npx playwright test`) green.

This plan is NOT autonomous — Task 3 (UAT capture) is a human-verify checkpoint because Chrome-MCP UAT requires a live `make dev` server the human starts; plus the visual baseline commit step asks for human confirmation that the first-run snapshots look right before locking them in.

Runs in Wave 4 after all migration + helper + scope-closure + doc/CI work completes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-crm-migration-validation/15-CONTEXT.md
@.planning/phases/15-crm-migration-validation/15-RESEARCH.md
@.planning/phases/15-crm-migration-validation/15-PATTERNS.md
@.planning/phases/15-crm-migration-validation/15-UI-SPEC.md
@.planning/phases/15-crm-migration-validation/15-VALIDATION.md
@.planning/phases/14-formscreen-enhancements/14-08-SUMMARY.md

<interfaces>
Patterns for each test layer (verbatim from 15-PATTERNS.md items 17-21, SP-3, SP-4, SP-5):

**E2E spec shape (analog: frontend/tests/e2e/contact-edit.spec.ts):**

- login helper reads Email + Password fields via shadcn Field layout locators; fills admin@localhost / admin; clicks login; awaits "Contact Management" heading.
- openEdit helper calls page.evaluate() to invoke `window.__mrnSendAction('{screen}_edit', { {screen}_id: 1 }, '{screen}-edit-1')`; awaits the "Edit {Screen}" heading.
- Test blocks cover: FieldSet legend visible, action-row buttons present, per-field validation via empty-required-field scenario, save flow lands back on list view, RadioGroup state migration (for user + interaction only).

**Per-field validation assertion pattern (SP-5):**
- Clear a required field via `page.locator('#{screen}-form-{field}').fill('')`.
- Click Save.
- Assert `page.locator('[data-slot="field-error"]').filter({ hasText: /required/i })` is visible.
- Assert form-level banner (`.bg-destructive\\/10`) has count 0 (per-field errors don't trigger banner — D-D4).

**Visual spec shape (analog: tests/visual/form.spec.ts contact-edit baselines at lines 59-79):**
- loginDemo + openEditForm helpers.
- `expect(page).toHaveScreenshot('{screen}-edit-form.png', { fullPage: true, maxDiffPixels: 200 })` for desktop.
- Separate test block with `page.setViewportSize({ width: 375, height: 800 })` for mobile `{screen}-edit-form-mobile.png`.

**UAT spec shape (analog: frontend/tests/uat/uat-driver.spec.ts):**
- EVIDENCE_DIR computed relative to frontend root pointing to `.planning/phases/15-crm-migration-validation/15-uat-evidence/{screen}/`.
- `fs.mkdirSync(EVIDENCE_DIR, { recursive: true })`.
- Scenario per 15-UI-SPEC §UAT Evidence Contract §Scope:
  1. Render — screenshot desktop.png + mobile.png; assertions.json records fieldset_legends, fields_in_order, action_row, descriptions_present.
  2. Validation — submit with empty required field; capture Field.Error DOM + screenshot + assertion.
  3. Save — fill valid data, submit, confirm landing on list view.
  4. (Optional user + interaction) RadioGroup click-through — data-state migrates.

**assertions.json shape (15-UI-SPEC §UAT Evidence Contract):**
A JSON object per screen with required keys: screen, viewport, fieldset_legends, fields_in_order, action_row (with buttons + align), descriptions_present (map of field → locked description string), validation_trigger (scenario + expected_error_path + expected_error_text + expected_aria_invalid), console_errors (count, must be 0).

**Chrome-MCP availability (per 15-RESEARCH.md A4 + environment check):**
- Check `.mcp.json` for `chrome` or `chrome-mcp` entry; if present, prefer Chrome-MCP.
- Fallback: Playwright (Phase 14 Plan 08 substituted successfully; same evidence contract).
- Either way, EVIDENCE_DIR folder layout is the same.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add E2E specs for company/user/interaction edit forms + extend contact-edit.spec.ts for inline forms</name>
  <files>frontend/tests/e2e/company-edit.spec.ts, frontend/tests/e2e/user-edit.spec.ts, frontend/tests/e2e/interaction-edit.spec.ts, frontend/tests/e2e/contact-edit.spec.ts</files>
  <read_first>
    - frontend/tests/e2e/contact-edit.spec.ts (Phase 14 canonical template for E2E — 15-PATTERNS.md items 17-19 analog)
    - frontend/src/lib/components/ui/field/field-error.svelte (confirms `data-slot="field-error"` attribute used by SP-5 assertions)
    - .planning/phases/15-crm-migration-validation/15-UI-SPEC.md §Per-Screen Composition Blueprint (expected heading texts, legend strings, button labels, description strings per screen)
    - .planning/phases/15-crm-migration-validation/15-VALIDATION.md §Per-Task Verification Map rows 15-03-01, 15-03-02, 15-04-01, 15-04-02
    - .planning/phases/15-crm-migration-validation/15-PATTERNS.md items 17-19 (deltas per spec) + SP-3 (login) + SP-4 (hook) + SP-5 (validation assertion)
  </read_first>
  <behavior>
    - Three new E2E specs created at `frontend/tests/e2e/{company,user,interaction}-edit.spec.ts`, each following the contact-edit pattern: login helper, openEdit helper via __mrnSendAction, test blocks for render/FieldSet/validation/save.
    - Contact-edit.spec.ts extended with inline tag-add + note-add smoke tests: "add a tag → visible in tag list", "add a note → visible in notes list", "empty tag submit → Field.Error visible".
    - Each spec uses the actual IDs set by the Plan 03/04 handler rewrites (company-form-name, user-form-email, interaction-form-type, etc.).
    - RadioGroup state-migration test for user-edit (preferred_contact_method) + interaction-edit (interaction_type): click each option, assert data-state migrates.
    - Textarea full_width assertion for interaction-edit: inspect the notes field's wrapper for `col-span-full` class (per 15-UI-SPEC).
    - Description copy assertions: expect the locked description strings to be visible ("Will appear on invoices and contact details." on company name; "Used for password resets and notifications." on user email; "Format: YYYY-MM-DD HH:MM (24-hour)." on interaction date).
  </behavior>
  <action>
Use the contact-edit.spec.ts structure verbatim as the template. For each new spec, replace the `openEditContactForm` helper action name + payload shape + expected heading.

**company-edit.spec.ts content (skeleton):**

    import { test, expect, type Page } from '@playwright/test';

    async function login(page: Page): Promise<void> {
        await page.goto('/');
        // Use the same field locators as contact-edit.spec.ts — copy verbatim.
        await page.locator('div[data-slot="field"]:has(label:has-text("Email"))').locator('input').first().fill('admin@localhost');
        await page.locator('div[data-slot="field"]:has(label:has-text("Password"))').locator('input').first().fill('admin');
        await page.getByRole('button', { name: /log in/i }).click();
        await expect(page.getByText('Contact Management')).toBeVisible({ timeout: 10000 });
    }

    async function openCompanyEditForm(page: Page): Promise<void> {
        await page.evaluate(() => {
            // eslint-disable-next-line @typescript-eslint/no-explicit-any
            const hook = (window as any).__mrnSendAction as
                | ((name: string, payload?: Record<string, unknown>, source?: string) => void)
                | undefined;
            if (!hook) throw new Error('__mrnSendAction hook missing');
            hook('company_edit', { company_id: 1 }, 'company-edit-1');
        });
        await expect(page.getByRole('heading', { name: 'Edit Company' })).toBeVisible({ timeout: 5000 });
    }

    test.describe('Company edit form (Phase 15 D-A1 + D-D1 + D-E3)', () => {
        test('renders FieldSet legend and action row', async ({ page }) => {
            await login(page);
            await openCompanyEditForm(page);
            await expect(page.getByText('Company details')).toBeVisible();
            await expect(page.getByText('Will appear on invoices and contact details.')).toBeVisible();
            await expect(page.getByRole('button', { name: 'Cancel' })).toBeVisible();
            await expect(page.getByRole('button', { name: 'Save company' })).toBeVisible();
        });

        test('per-field validation fires on empty name (D-D1)', async ({ page }) => {
            await login(page);
            await openCompanyEditForm(page);
            await page.locator('#company-form-name').fill('');
            await page.getByRole('button', { name: 'Save company' }).click();
            const error = page.locator('[data-slot="field-error"]').filter({ hasText: /required/i });
            await expect(error).toBeVisible();
            // Form-level banner must NOT appear for per-field validation (D-D4)
            await expect(page.locator('.bg-destructive\\/10')).toHaveCount(0);
        });

        test('save flow returns to company list', async ({ page }) => {
            await login(page);
            // Navigate to companies list first via nav, then open edit via hook.
            await openCompanyEditForm(page);
            // name is pre-filled from edit; just click Save.
            await page.getByRole('button', { name: 'Save company' }).click();
            await expect(page.getByText('Company Management')).toBeVisible({ timeout: 5000 });
        });
    });

**user-edit.spec.ts content — same skeleton** with:
- action: `hook('user_edit', { user_id: 1 }, 'user-edit-1')`
- heading: `'Edit User'`
- legends: `'Account'` and `'Permissions'`
- description: `'Used for password resets and notifications.'`
- save button: `'Save user'`
- Additional test: click each preferred_contact_method radio option (email/sms/phone), verify `data-state="checked"` migrates to the clicked option via `.toHaveAttribute('data-state', 'checked')`.

**interaction-edit.spec.ts content — same skeleton** with:
- action: `hook('interaction_edit', { interaction_id: 1 }, 'interaction-edit-1')` or whichever action name Plan 04 uses (grep handle_interaction_form / action registration)
- heading: probably `'Edit Interaction'` or `'Log Interaction'` — match what Plan 04 set
- legend: `'Interaction'`
- description: `'Format: YYYY-MM-DD HH:MM (24-hour).'`
- save button: `'Save interaction'`
- RadioGroup test: click each interaction_type radio option (call/email/meeting), verify data-state migration
- Textarea full_width: inspect `#interaction-form-notes` wrapper for `col-span-full` class.

**contact-edit.spec.ts extension:** Read the current file and append 2-3 new test blocks at the end:

    test('inline tag-add smoke: "+ Add tag" submits', async ({ page }) => {
        await login(page);
        await openEditContactForm(page); // existing helper
        await page.locator('#tag-input').fill('vip');
        await page.getByRole('button', { name: '+ Add tag' }).click();
        // Tag appears in tag list (assertion depends on how tags render — check current file for tag-list pattern)
        await expect(page.getByText('vip')).toBeVisible({ timeout: 3000 });
    });

    test('inline tag-add validation: empty tag emits field error', async ({ page }) => {
        await login(page);
        await openEditContactForm(page);
        await page.locator('#tag-input').fill('');
        await page.getByRole('button', { name: '+ Add tag' }).click();
        const error = page.locator('[data-slot="field-error"]').filter({ hasText: /required/i });
        await expect(error).toBeVisible();
    });

    test('inline note-add smoke: "+ Add note" submits', async ({ page }) => {
        await login(page);
        await openEditContactForm(page);
        // note-input is now a Textarea
        await page.locator('#note-input').fill('Follow up next week.');
        await page.getByRole('button', { name: '+ Add note' }).click();
        await expect(page.getByText('Follow up next week.')).toBeVisible({ timeout: 3000 });
    });

(Adapt locator patterns to match the actual rendered DOM — read the current contact-edit.spec.ts for any existing tag/note locator conventions.)

**Verification:** After writing, run each spec against `make dev`:

    cd /home/oetiker/checkouts/marionette/frontend
    npx playwright test tests/e2e/company-edit.spec.ts tests/e2e/user-edit.spec.ts tests/e2e/interaction-edit.spec.ts tests/e2e/contact-edit.spec.ts

All tests pass green. If any heading/legend/description text mismatches, the handler rewrites from Plans 03/04 diverged from 15-UI-SPEC — fix the handler, not the test (the test encodes the locked spec).
  </action>
  <verify>
    <automated>cd /home/oetiker/checkouts/marionette/frontend && npx playwright test tests/e2e/company-edit.spec.ts tests/e2e/user-edit.spec.ts tests/e2e/interaction-edit.spec.ts tests/e2e/contact-edit.spec.ts 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `test -f /home/oetiker/checkouts/marionette/frontend/tests/e2e/company-edit.spec.ts` passes
    - `test -f /home/oetiker/checkouts/marionette/frontend/tests/e2e/user-edit.spec.ts` passes
    - `test -f /home/oetiker/checkouts/marionette/frontend/tests/e2e/interaction-edit.spec.ts` passes
    - `grep -c "Will appear on invoices and contact details" /home/oetiker/checkouts/marionette/frontend/tests/e2e/company-edit.spec.ts` returns ≥1
    - `grep -c "Used for password resets and notifications" /home/oetiker/checkouts/marionette/frontend/tests/e2e/user-edit.spec.ts` returns ≥1
    - `grep -c "Format: YYYY-MM-DD HH:MM" /home/oetiker/checkouts/marionette/frontend/tests/e2e/interaction-edit.spec.ts` returns ≥1
    - `grep -c "data-state.*checked\\|toHaveAttribute.*data-state" /home/oetiker/checkouts/marionette/frontend/tests/e2e/user-edit.spec.ts` returns ≥1 (RadioGroup state migration test)
    - `grep -c "data-state.*checked\\|toHaveAttribute.*data-state" /home/oetiker/checkouts/marionette/frontend/tests/e2e/interaction-edit.spec.ts` returns ≥1
    - `grep -c "+ Add tag\\|+ Add note" /home/oetiker/checkouts/marionette/frontend/tests/e2e/contact-edit.spec.ts` returns ≥2 (inline tag + note smoke tests added)
    - `cd /home/oetiker/checkouts/marionette/frontend && npx playwright test tests/e2e/company-edit.spec.ts tests/e2e/user-edit.spec.ts tests/e2e/interaction-edit.spec.ts tests/e2e/contact-edit.spec.ts` — all tests pass green
  </acceptance_criteria>
  <done>All three new E2E specs exist and pass; contact-edit extended with inline coverage; RadioGroup + Textarea full_width assertions present; full E2E suite for Phase 15 migrated screens green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend frontend/tests/visual/form.spec.ts with 6 new baselines (3 screens × desktop + mobile)</name>
  <files>frontend/tests/visual/form.spec.ts</files>
  <read_first>
    - frontend/tests/visual/form.spec.ts (existing contact-edit-form baselines at lines 59-79 — analog for 15-PATTERNS.md item 21)
    - .planning/phases/15-crm-migration-validation/15-UI-SPEC.md §Visual Rebaseline Plan (expected snapshot filenames + viewports + tolerance)
  </read_first>
  <behavior>
    - Three new helper functions: `openCompanyEditForm`, `openUserEditForm`, `openInteractionEditForm` — mirror the existing `openEditForm` for contact.
    - Six new test blocks: `{screen}-edit-form` (desktop 1280×720) and `{screen}-edit-form-mobile` (375×800) for each of company/user/interaction.
    - `maxDiffPixels: 200` for each (matches contact-edit tolerance).
    - First run uses `--update-snapshots` to create the baselines.
    - Second run (without flag) must be green — no flaky diffs.
    - Contact-edit-form existing baselines must STILL match post-refactor (Plan 04 D-B2 is structural-only, not visual).
  </behavior>
  <action>
Open `frontend/tests/visual/form.spec.ts`. After the existing `contact edit form — mobile 375px baseline` test (around line 79), append three helper functions and six test blocks:

    async function openCompanyEditForm(page: Page): Promise<void> {
        await page.evaluate(() => {
            // eslint-disable-next-line @typescript-eslint/no-explicit-any
            const hook = (window as any).__mrnSendAction;
            if (!hook) throw new Error('__mrnSendAction hook missing');
            hook('company_edit', { company_id: 1 }, 'company-edit-1');
        });
        await page.waitForSelector('[data-slot="field"]', { state: 'visible' });
    }

    async function openUserEditForm(page: Page): Promise<void> {
        // same pattern with 'user_edit' + user_id 1
    }

    async function openInteractionEditForm(page: Page): Promise<void> {
        // same pattern with 'interaction_edit' + interaction_id 1 (or whichever payload shape Plan 04 set)
    }

    test('company edit form — desktop baseline', async ({ page }) => {
        await loginDemo(page); // existing helper in form.spec.ts
        await openCompanyEditForm(page);
        await expect(page).toHaveScreenshot('company-edit-form.png', {
            fullPage: true,
            maxDiffPixels: 200,
        });
    });

    test('company edit form — mobile 375px baseline', async ({ page }) => {
        await page.setViewportSize({ width: 375, height: 800 });
        await loginDemo(page);
        await openCompanyEditForm(page);
        await expect(page).toHaveScreenshot('company-edit-form-mobile.png', {
            fullPage: true,
            maxDiffPixels: 200,
        });
    });

    // Repeat the same pair for user-edit-form and interaction-edit-form.

Six new test blocks total. Do NOT modify the existing contact-edit-form baselines — they must still match after Plan 04 D-B2 refactor.

**Baseline capture:**

First run with `--update-snapshots`:

    cd /home/oetiker/checkouts/marionette/frontend
    npx playwright test tests/visual/form.spec.ts --update-snapshots

This creates the 6 new PNG baselines under `tests/visual/form.spec.ts-snapshots/`. Inspect the generated PNGs manually to confirm they look correct (legend text visible, FieldSet grid rendered, action row right-aligned, RadioGroup vertical stack — everything the 15-UI-SPEC locks in).

Second run WITHOUT the flag:

    npx playwright test tests/visual/form.spec.ts

All 8 tests (2 existing contact + 6 new) must pass green. If contact-edit-form diffs, Plan 04's refactor leaked visual changes — investigate + fix the handler.

Commit the snapshot PNGs alongside the spec file. File sizes: 6 PNGs × ~500KB-1MB each; acceptable as governance artifacts.
  </action>
  <verify>
    <automated>cd /home/oetiker/checkouts/marionette/frontend && npx playwright test tests/visual/form.spec.ts 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "company edit form" /home/oetiker/checkouts/marionette/frontend/tests/visual/form.spec.ts` returns ≥2 (desktop + mobile)
    - `grep -c "user edit form" /home/oetiker/checkouts/marionette/frontend/tests/visual/form.spec.ts` returns ≥2
    - `grep -c "interaction edit form" /home/oetiker/checkouts/marionette/frontend/tests/visual/form.spec.ts` returns ≥2
    - `grep -c "contact edit form" /home/oetiker/checkouts/marionette/frontend/tests/visual/form.spec.ts` returns ≥2 (existing preserved)
    - Snapshot files exist: `ls /home/oetiker/checkouts/marionette/frontend/tests/visual/form.spec.ts-snapshots/ | grep -cE "(company|user|interaction)-edit-form(-mobile)?\\.png"` returns 6
    - `cd /home/oetiker/checkouts/marionette/frontend && npx playwright test tests/visual/form.spec.ts` — all 8 tests pass green (no --update-snapshots flag)
  </acceptance_criteria>
  <done>6 new visual baselines created, committed, and match on clean second run. Contact-edit baselines unchanged.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Chrome-MCP (or Playwright fallback) UAT per screen + phase-gate verification</name>
  <what-built>
Five UAT evidence folders capturing Phase 15's migrated screens end-to-end per 15-UI-SPEC §UAT Evidence Contract:
- `.planning/phases/15-crm-migration-validation/15-uat-evidence/company-edit/` (3 scenarios)
- `15-uat-evidence/user-edit/` (3-4 scenarios including RadioGroup click-through)
- `15-uat-evidence/interaction-edit/` (3-4 scenarios including RadioGroup + Textarea full_width)
- `15-uat-evidence/contact-tag-add/` (2 scenarios, smoke)
- `15-uat-evidence/contact-note-add/` (2 scenarios, smoke)

Each folder contains desktop.png + mobile.png + assertions.json + console.log.

Claude has automated everything it can:
1. Created UAT spec files at `frontend/tests/uat/{company,user,interaction}-edit-uat.spec.ts`, `contact-tag-add-uat.spec.ts`, `contact-note-add-uat.spec.ts` — mirroring the Phase 14 `uat-driver.spec.ts` pattern (15-PATTERNS.md item 20) with EVIDENCE_DIR pointing at each per-screen evidence folder.
2. Populated each spec with 2-4 scenarios per 15-UI-SPEC §UAT Evidence Contract §Scope.
3. Set up evidence-folder creation via `fs.mkdirSync(EVIDENCE_DIR, { recursive: true })`.
4. Chrome-MCP availability check: reads `.mcp.json` for Chrome-MCP entry. Prefers it if present (per user memory `feedback_use_chrome_for_uat.md`); falls back to Playwright UAT driver otherwise (Phase 14 Plan 08 precedent).
5. Ran the UAT suite: `npx playwright test --config tests/uat/playwright.uat.config.ts tests/uat/{company,user,interaction}-edit-uat.spec.ts tests/uat/contact-tag-add-uat.spec.ts tests/uat/contact-note-add-uat.spec.ts`.

This task is a checkpoint because:
- The UAT run requires a live `make dev` server which the human must have running.
- After Claude captures the evidence, the human should spot-check at least one desktop.png + one assertions.json per screen to confirm the locked 15-UI-SPEC strings (legends, descriptions, RadioGroup options, action row labels) are all correctly rendered — this is the final sanity check before declaring Phase 15 complete.
  </what-built>
  <how-to-verify>
1. **Start `make dev` in a separate terminal** (keep it running during this checkpoint):

       cd /home/oetiker/checkouts/marionette
       make dev

2. **Let Claude run the full UAT suite:** Claude will execute each UAT spec, generating evidence artifacts in `.planning/phases/15-crm-migration-validation/15-uat-evidence/`.

3. **Inspect the captured evidence per screen:**

   a. Open `.planning/phases/15-crm-migration-validation/15-uat-evidence/company-edit/desktop.png` — confirm:
      - Heading reads "Edit Company"
      - Fieldset legend "Company details" is visible
      - Description "Will appear on invoices and contact details." renders below Name field
      - Action row shows Cancel + "Save company" right-aligned

   b. Open `.planning/phases/15-crm-migration-validation/15-uat-evidence/user-edit/desktop.png` — confirm:
      - Heading reads "Edit User"
      - Two FieldSets visible: "Account" and "Permissions" separated by a visible separator line
      - Email description "Used for password resets and notifications." visible
      - RadioGroup "Preferred contact method" shows 3 options (Email, SMS, Phone) stacked vertically with per-option descriptions

   c. Open `.planning/phases/15-crm-migration-validation/15-uat-evidence/interaction-edit/desktop.png` — confirm:
      - Heading reads "Edit Interaction" (or "Log Interaction" — match Plan 04 decision)
      - FieldSet legend "Interaction" visible
      - RadioGroup "Type" shows 3 options (Call, Email, Meeting) stacked vertically — NO per-option descriptions
      - Textarea "Notes" spans full width on desktop (occupies both grid columns)
      - Date description "Format: YYYY-MM-DD HH:MM (24-hour)." visible

   d. Open each `assertions.json` — confirm `fieldset_legends`, `fields_in_order`, `action_row`, `descriptions_present`, `console_errors: 0` keys are all present and match the locked strings.

   e. Open each `console.log` — confirm no JavaScript errors were logged during UAT (console_errors count == 0).

4. **Run the phase-gate full suite:**

       cd /home/oetiker/checkouts/marionette
       cd backend && cargo test --workspace && cargo clippy -p marionette-protocol -p marionette -- -D warnings
       cd ../frontend && npm run check && npx vitest --run && npx playwright test

   All must be green. In particular:
   - `ci-guards.spec.ts` (from Plan 06) passes the Flowbite residue test with 0 matches.
   - Visual baselines from Task 2 all match.
   - E2E specs from Task 1 all pass.
   - Rust tests from Plans 01-04 all green.

5. **Cross-reference against 15-UI-SPEC §Checker Sign-Off** (if the checker pattern is in play) — confirm all 6 dimensions (Copywriting, Visuals, Color, Typography, Spacing, Registry Safety) are covered by evidence.

6. **Goal-backward check:** for each must-have M1..M9 in the phase plan, confirm evidence supports it:
   - M1 (4 form handlers rendered via FieldSet composition) — E2E specs + UAT desktop.png
   - M2 (contact DB extended, persists) — Plan 01 round-trip test + contact-edit UAT
   - M3 (per-field validation via /_errors wired in every migrated save handler) — E2E validation tests + UAT validation scenarios
   - M4 (RadioGroup in CRM — interaction.type + user.preferred_contact_method) — interaction-edit and user-edit UAT desktop.png
   - M5 (Zero Flowbite residue) — ci-guards.spec.ts Flowbite grep test
   - M6 (Docs describe shadcn-svelte) — grep of CONCEPT.md/TOOLING.md/STACK.md
   - M7 (PROTOCOL.md only canonical /_errors/{bind}) — grep for deleted legacy section + present worked example
   - M8 (Phase 14 review items resolved) — Plan 05 outputs (dev-gate, Form.svelte, Button builder, node: prefix)
   - M9 (E2E + visual + UAT evidence per screen) — this task's artifacts
  </how-to-verify>
  <files>frontend/tests/uat/company-edit-uat.spec.ts, frontend/tests/uat/user-edit-uat.spec.ts, frontend/tests/uat/interaction-edit-uat.spec.ts, frontend/tests/uat/contact-tag-add-uat.spec.ts, frontend/tests/uat/contact-note-add-uat.spec.ts, .planning/phases/15-crm-migration-validation/15-uat-evidence/company-edit/, .planning/phases/15-crm-migration-validation/15-uat-evidence/user-edit/, .planning/phases/15-crm-migration-validation/15-uat-evidence/interaction-edit/, .planning/phases/15-crm-migration-validation/15-uat-evidence/contact-tag-add/, .planning/phases/15-crm-migration-validation/15-uat-evidence/contact-note-add/</files>
  <action>
Drive the UAT suite end-to-end via Chrome-MCP (preferred per user memory feedback_use_chrome_for_uat) or Playwright fallback (Phase 14 Plan 08 precedent):

1. Detect Chrome-MCP availability: `cat .mcp.json | grep -i chrome` — if a chrome-mcp server entry is present, use Chrome-MCP tools (mcp__chrome__* calls) to drive the browser. Else fall back to Playwright UAT specs via `npx playwright test --config tests/uat/playwright.uat.config.ts tests/uat/*-uat.spec.ts`.

2. Create the five UAT spec files, each following the 15-PATTERNS.md item 20 template:
   - frontend/tests/uat/company-edit-uat.spec.ts (EVIDENCE_DIR → 15-uat-evidence/company-edit)
   - frontend/tests/uat/user-edit-uat.spec.ts (EVIDENCE_DIR → 15-uat-evidence/user-edit)
   - frontend/tests/uat/interaction-edit-uat.spec.ts (EVIDENCE_DIR → 15-uat-evidence/interaction-edit)
   - frontend/tests/uat/contact-tag-add-uat.spec.ts (EVIDENCE_DIR → 15-uat-evidence/contact-tag-add)
   - frontend/tests/uat/contact-note-add-uat.spec.ts (EVIDENCE_DIR → 15-uat-evidence/contact-note-add)

3. Each spec creates its EVIDENCE_DIR via `fs.mkdirSync(EVIDENCE_DIR, { recursive: true })` and runs scenarios per 15-UI-SPEC §UAT Evidence Contract §Scope (3-4 scenarios for main edit forms, 2 for inline). Each scenario captures:
   - `{N}-{scenario}.png` — screenshot at the relevant viewport (desktop 1280×720 OR mobile 375×800)
   - `{N}-{scenario}.json` — DOM assertion snapshot with the 8 required keys from 15-UI-SPEC §assertions.json Required Keys
   - `console.log` — browser console capture for the entire session

4. Required per-screen scenarios:
   - company-edit: (1) render desktop+mobile, (2) validation on empty name, (3) save flow returns to list
   - user-edit: (1) render desktop+mobile showing 2 FieldSets, (2) validation on empty email, (3) save flow, (4) RadioGroup click-through (data-state migration)
   - interaction-edit: (1) render desktop+mobile showing RadioGroup + Textarea full_width, (2) validation on empty subject, (3) save flow, (4) RadioGroup click-through
   - contact-tag-add: (1) render, (2) empty-submit validation
   - contact-note-add: (1) render, (2) empty-submit validation

5. After all specs run, pause at the checkpoint for human spot-check per the how-to-verify steps.

6. Run the phase-gate full suite after human approval:
   ```
   cd backend && cargo test --workspace && cargo clippy -p marionette-protocol -p marionette -- -D warnings
   cd ../frontend && npm run check && npx vitest --run && npx playwright test
   ```
   All must be green before Phase 15 is declared complete.
  </action>
  <verify>
    <automated>ls /home/oetiker/checkouts/marionette/.planning/phases/15-crm-migration-validation/15-uat-evidence/ 2>&1 && find /home/oetiker/checkouts/marionette/.planning/phases/15-crm-migration-validation/15-uat-evidence/ -name "*.png" 2>&1 | wc -l</automated>
  </verify>
  <done>Five UAT evidence folders populated with screenshots + assertion JSONs + console logs; human has typed "approved" after spot-checking at least one evidence folder per screen; phase-gate full suite green across backend + frontend.</done>
  <resume-signal>Type "approved" to mark Phase 15 complete, or describe any issue found (missing evidence, legend mismatch, visual regression, etc.) for Claude to fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UAT evidence → repo | Screenshots + JSON + console logs committed to the repo as governance artifacts. No secrets / real PII in seed data (Alice/Bob/Carol @ example.com). |
| __mrnSendAction hook usage | UAT drives action dispatch via the dev-mode window hook — Plan 05 gated to DEV; UAT runs under `make dev`, so the hook is present. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-03-PLAN07 | Information Disclosure | UAT evidence PNGs | accept | Seed data is synthetic (no real PII). Screenshots are intentional governance artifacts. Pre-deployment project. |
| T-15-01 | Test-hook leak | UAT uses __mrnSendAction | mitigate (Plan 05) | Hooks are DEV-gated; UAT runs under DEV mode — expected behavior. Production builds strip them (verified Plan 05). |
| T-15-02 | Flowbite regression | n/a (Plan 06 CI guard enforces) | mitigate (Plan 06) | Full suite runs Plan 06 ci-guards as part of the phase-gate verification. |
| T-15-03-PLAN07-b | Replay / evidence tampering | UAT evidence committed to repo | accept | Git history is the tamper-evident record. No additional crypto-signing needed for pre-deployment governance. |
</threat_model>

<verification>
Final phase-gate verification (run during Task 3 checkpoint):

- Backend: `cargo test --workspace` + `cargo clippy -p marionette-protocol -p marionette -- -D warnings` both green.
- Frontend: `npm run check` green (no svelte-check errors).
- Vitest: `npx vitest --run` green (browser tests for all components pass).
- Playwright E2E: `npx playwright test` green (all specs including ci-guards, Phase 15 edit specs, existing shell/datatable specs).
- Playwright Visual: baselines match on clean second run.
- UAT evidence: 5 folders populated with expected artifacts; console_errors: 0 per assertion.json.
- Chrome-MCP availability check ran — either MCP used OR Playwright fallback used successfully.
- Goal-backward must-haves M1..M9: every one has evidence.
</verification>

<success_criteria>
- 3 new E2E specs + extended contact-edit.spec.ts all pass.
- 6 new visual baselines exist and match on clean run.
- 5 UAT evidence folders exist with complete artifacts.
- Phase-gate full suite green across backend + frontend.
- Zero Flowbite residue in runtime paths (CI guard green).
- Human has spot-checked at least one evidence folder per screen and confirmed locked UI-SPEC strings render correctly.
- Phase 14 contact-edit baseline still matches (D-B2 refactor was structural-only).
- All 9 phase must-haves (M1..M9) have evidence.
</success_criteria>

<output>
After completion, create `.planning/phases/15-crm-migration-validation/15-07-SUMMARY.md` following `~/.claude/get-shit-done/templates/summary.md`.

This is the final plan for Phase 15 — after SUMMARY commits, Phase 15 is ready for `/gsd-verify-work` and then the v1.1 milestone close via `/gsd-complete-milestone`.
</output>
