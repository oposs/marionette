---
phase: 11-leaf-component-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/lib/utils.ts
  - frontend/src/lib/components/ui/button/index.ts
  - frontend/src/lib/components/ui/input/index.ts
  - frontend/src/lib/components/ui/select/index.ts
  - frontend/src/lib/components/ui/checkbox/index.ts
  - frontend/src/lib/components/ui/label/index.ts
  - frontend/src/lib/components/ui/dialog/index.ts
  - frontend/src/lib/components/ui/table/index.ts
  - frontend/src/lib/components/ui/card/index.ts
  - frontend/src/lib/components/ui/skeleton/index.ts
  - frontend/src/lib/components/ui/separator/index.ts
  - frontend/src/lib/components/ui/badge/index.ts
  - frontend/src/lib/registry/icons.ts
autonomous: true
requirements: [COMP-01, COMP-02]
must_haves:
  truths:
    - "shadcn-svelte primitives are installed and importable from $lib/components/ui/*"
    - "Icon names from server resolve to lucide-svelte components at runtime"
    - "Unknown icon names render a CircleHelp fallback icon"
  artifacts:
    - path: "frontend/src/lib/components/ui/button/index.ts"
      provides: "shadcn Button primitive"
    - path: "frontend/src/lib/components/ui/dialog/index.ts"
      provides: "shadcn Dialog primitive"
    - path: "frontend/src/lib/components/ui/select/index.ts"
      provides: "shadcn Select primitive"
    - path: "frontend/src/lib/components/ui/table/index.ts"
      provides: "shadcn Table primitive"
    - path: "frontend/src/lib/registry/icons.ts"
      provides: "Dynamic icon registry with registerIcon, getIcon exports"
      exports: ["registerIcon", "getIcon"]
    - path: "frontend/src/lib/utils.ts"
      provides: "cn() + WithElementRef + WithoutChildren types"
  key_links:
    - from: "frontend/src/lib/registry/icons.ts"
      to: "@lucide/svelte"
      via: "direct imports of icon components"
      pattern: "from '@lucide/svelte/icons/"
---

<objective>
Install all shadcn-svelte primitives needed for Phase 11 and create the dynamic icon registry.

Purpose: Establish the foundation that all subsequent component migration plans depend on. Without shadcn primitives installed and the icon registry available, no component can be migrated.

Output: 11 shadcn-svelte primitive directories under `$lib/components/ui/`, updated `utils.ts` with required types, and `$lib/registry/icons.ts` with `getIcon()`/`registerIcon()` API.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-leaf-component-migration/11-CONTEXT.md
@.planning/phases/11-leaf-component-migration/11-RESEARCH.md
@.planning/phases/11-leaf-component-migration/11-UI-SPEC.md

<interfaces>
<!-- From frontend/src/lib/utils.ts (current state — only has cn()) -->
```typescript
import { type ClassValue, clsx } from 'clsx';
import { twMerge } from 'tailwind-merge';
export function cn(...inputs: ClassValue[]) {
  return twMerge(clsx(...inputs));
}
```

<!-- From frontend/components.json (shadcn CLI config) -->
```json
{
  "$schema": "https://shadcn-svelte.com/schema.json",
  "tailwind": { "css": "src/app.css", "baseColor": "zinc" },
  "aliases": {
    "lib": "$lib",
    "components": "$lib/components",
    "utils": "$lib/utils",
    "ui": "$lib/components/ui",
    "hooks": "$lib/hooks"
  },
  "typescript": true
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install shadcn-svelte primitives and fix utils.ts</name>
  <files>
    frontend/src/lib/utils.ts
    frontend/src/lib/components/ui/* (created by CLI)
  </files>
  <read_first>
    frontend/src/lib/utils.ts
    frontend/components.json
    .planning/phases/11-leaf-component-migration/11-RESEARCH.md
  </read_first>
  <action>
Step 1: Add the missing types to `frontend/src/lib/utils.ts` BEFORE running the CLI. The file currently only exports `cn()`. Add these two types after the `cn` function (per D-02, Pitfall 3):

```typescript
export type WithElementRef<T, U extends HTMLElement = HTMLElement> = T & { ref?: U | null };
export type WithoutChildren<T> = T extends { children?: any } ? Omit<T, 'children'> : T;
```

Step 2: Run the shadcn-svelte CLI to bulk-install all 11 primitives (per D-02):

```bash
cd frontend && npx shadcn-svelte@latest add -y button input select checkbox label dialog table card skeleton separator badge
```

This installs each primitive into `frontend/src/lib/components/ui/<name>/`. The CLI reads `components.json` for the `ui` alias path.

Step 3: Verify all 11 directories exist under `frontend/src/lib/components/ui/` and that each has an `index.ts` file.

Step 4: Verify `npm run build` succeeds (no TypeScript errors from newly generated components).
  </action>
  <verify>
    <automated>cd frontend && ls -d src/lib/components/ui/button src/lib/components/ui/input src/lib/components/ui/select src/lib/components/ui/checkbox src/lib/components/ui/label src/lib/components/ui/dialog src/lib/components/ui/table src/lib/components/ui/card src/lib/components/ui/skeleton src/lib/components/ui/separator src/lib/components/ui/badge && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/lib/utils.ts contains `export type WithElementRef`
    - frontend/src/lib/utils.ts contains `export type WithoutChildren`
    - frontend/src/lib/components/ui/button/index.ts exists
    - frontend/src/lib/components/ui/input/index.ts exists
    - frontend/src/lib/components/ui/select/index.ts exists
    - frontend/src/lib/components/ui/checkbox/index.ts exists
    - frontend/src/lib/components/ui/label/index.ts exists
    - frontend/src/lib/components/ui/dialog/index.ts exists
    - frontend/src/lib/components/ui/table/index.ts exists
    - frontend/src/lib/components/ui/card/index.ts exists
    - frontend/src/lib/components/ui/skeleton/index.ts exists
    - frontend/src/lib/components/ui/separator/index.ts exists
    - frontend/src/lib/components/ui/badge/index.ts exists
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>All 11 shadcn-svelte primitives installed, utils.ts has WithElementRef and WithoutChildren types, frontend builds without errors.</done>
</task>

<task type="auto">
  <name>Task 2: Create dynamic icon registry</name>
  <files>
    frontend/src/lib/registry/icons.ts
    frontend/src/lib/index.ts
  </files>
  <read_first>
    frontend/src/lib/registry/registry.ts
    frontend/src/lib/registry/defaults.ts
    frontend/src/lib/index.ts
    .planning/phases/11-leaf-component-migration/11-RESEARCH.md
  </read_first>
  <action>
Create `frontend/src/lib/registry/icons.ts` implementing the dynamic icon registry (per D-05, D-06).

The file must:

1. Import `Component` type from `svelte`.
2. Import `CircleHelp` from `@lucide/svelte/icons/circle-help` as the fallback icon (per D-06).
3. Create a module-level `Record<string, Component>` called `ICON_REGISTRY`.
4. Export `registerIcon(name: string, component: Component): void` that adds to the registry.
5. Export `getIcon(name: string): Component` that returns the registered component or `CircleHelp` fallback.
6. Import and register these default icons (used by CRM demo):
   - `plus` from `@lucide/svelte/icons/plus`
   - `chevron-up` from `@lucide/svelte/icons/chevron-up`
   - `chevron-down` from `@lucide/svelte/icons/chevron-down`
   - `alert-circle` from `@lucide/svelte/icons/alert-circle`
   - `x` from `@lucide/svelte/icons/x`
   - `menu` from `@lucide/svelte/icons/menu`
   - `arrow-left` from `@lucide/svelte/icons/arrow-left`
   - `search` from `@lucide/svelte/icons/search`
   - `filter` from `@lucide/svelte/icons/filter`
   - `pencil` from `@lucide/svelte/icons/pencil`
   - `trash` mapped to `@lucide/svelte/icons/trash-2`
   - `check` from `@lucide/svelte/icons/check`
   - `loader` mapped to `@lucide/svelte/icons/loader-2`
   - `circle-help` mapped to `@lucide/svelte/icons/circle-help` (the fallback itself, also registerable)
7. Register all defaults in a loop using `registerIcon()`.

Then update `frontend/src/lib/index.ts` to re-export `getIcon` and `registerIcon` from the icons module:

```typescript
export { getIcon, registerIcon } from './registry/icons';
```

Add this alongside the existing exports. Do NOT remove any existing exports.
  </action>
  <verify>
    <automated>cd frontend && grep -c "export function getIcon" src/lib/registry/icons.ts && grep -c "export function registerIcon" src/lib/registry/icons.ts && grep "getIcon" src/lib/index.ts && npm run build 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/lib/registry/icons.ts contains `export function getIcon(name: string): Component`
    - frontend/src/lib/registry/icons.ts contains `export function registerIcon(name: string, component: Component): void`
    - frontend/src/lib/registry/icons.ts contains `from '@lucide/svelte/icons/circle-help'`
    - frontend/src/lib/registry/icons.ts contains at least 13 icon registrations (plus, chevron-up, chevron-down, alert-circle, x, menu, arrow-left, search, filter, pencil, trash, check, loader)
    - frontend/src/lib/index.ts contains `export { getIcon, registerIcon }`
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Icon registry created with getIcon/registerIcon API, 13+ default icons registered, CircleHelp fallback for unknown names, exported from barrel file, frontend builds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server -> icon registry | Server sends icon name strings that are used to look up components |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Spoofing | icon registry | mitigate | Icon names are looked up in a fixed Map, not used as import paths or evaluated. Unknown names return CircleHelp fallback -- no code injection vector. |
| T-11-02 | Information Disclosure | icon registry | accept | Icon names are not sensitive data. The registry is client-side only. |
</threat_model>

<verification>
- All 11 shadcn-svelte primitives installed and importable
- utils.ts has WithElementRef and WithoutChildren types
- Icon registry exports getIcon() and registerIcon()
- Unknown icon names return CircleHelp component
- `npm run build` exits 0
</verification>

<success_criteria>
1. `ls frontend/src/lib/components/ui/` shows 11 directories (button, input, select, checkbox, label, dialog, table, card, skeleton, separator, badge)
2. `grep "WithElementRef" frontend/src/lib/utils.ts` returns a match
3. `grep "getIcon" frontend/src/lib/registry/icons.ts` returns export function
4. `npm run build` exits 0 with no TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/11-leaf-component-migration/11-01-SUMMARY.md`
</output>
