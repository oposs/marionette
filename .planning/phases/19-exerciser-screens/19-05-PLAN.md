---
phase: 19-exerciser-screens
plan: 05
type: execute
wave: 3
depends_on: [19-02, 19-03, 19-04]
files_modified:
  - .planning/phases/19-exerciser-screens/19-VERIFICATION.md
  - .planning/phases/19-exerciser-screens/19-VALIDATION.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: false
requirements: [EXER-01, EXER-02, EXER-03]

must_haves:
  truths:
    - "Chrome MCP UAT walks all 3 exerciser nav entries at desktop width and confirms each renders without console errors and with the UI-SPEC-locked layout"
    - "EXER-01: observation matrix shows 4 cells with FAIL × 3 + WARN × 1 state; Open seed draft CTA toasts the seed path"
    - "EXER-02: focus retention holds for ≥ 60 s at 500 ms cadence (≥ 120 patches emitted); all 4 invariants stay PASS; Start/Pause/Reset all work"
    - "EXER-03: 4 perf readouts populate with numeric values within 30 s of mount; DataTable virtualizes 10 000 rows; 80 fields render across 4 FieldSet groups"
    - "19-VERIFICATION.md records actual observed values + findings + PASS/FAIL per acceptance item"
    - "19-VALIDATION.md Per-Task Verification Map populated and `nyquist_compliant: true` in frontmatter"
    - "REQUIREMENTS.md traceability table shows EXER-01, EXER-02, EXER-03 as Validated"
    - "ROADMAP.md Phase 19 entry updated to Complete with plans 5/5 shipped"
    - "STATE.md rolled forward to reflect Phase 19 closed; unblocks Phase 20"
  artifacts:
    - path: ".planning/phases/19-exerciser-screens/19-VERIFICATION.md"
      provides: "Chrome MCP UAT walkthrough results — 3 exercisers, 4 invariants, 4 perf signals"
      contains: "status: verified"
    - path: ".planning/phases/19-exerciser-screens/19-VALIDATION.md"
      provides: "Populated Per-Task Verification Map + Manual-Only Verifications with actual UAT results"
      contains: "nyquist_compliant: true"
  key_links:
    - from: ".planning/REQUIREMENTS.md"
      to: "EXER-01 / EXER-02 / EXER-03 rows"
      via: "Validated column with SUMMARY.md references"
      pattern: "✅ Validated 2026-04"
    - from: ".planning/ROADMAP.md"
      to: "Phase 19 entry"
      via: "[x] Phase 19: Exerciser Screens ... (completed 2026-04-XX)"
      pattern: "Phase 19: Exerciser Screens"
---

<objective>
Phase 19 close-out — drive Chrome MCP UAT across all 3 exerciser screens at desktop + mobile widths, capture findings per the UI-SPEC-locked acceptance criteria, record advisory performance baselines, populate the Validation contract, and update REQUIREMENTS / ROADMAP / STATE to reflect Phase 19 complete.

Purpose: The 3 exercisers are structurally asserted by unit tests in Plans 19-02/03/04. UAT is the ONLY place where the live browser-runtime assertions fire: PATCH-02 focus retention over 60 s under real patch wire pressure, live `getContext(Symbol.for("scn-sidebar"))` collision identity, actual FPS / TTFP / memory numbers on the developer's machine. This plan drives that walk, captures evidence, records the advisory baselines, and flips the phase state to verified.

Output: 19-VERIFICATION.md with UAT findings + advisory perf baselines + any new deferred items; 19-VALIDATION.md with populated Per-Task Verification Map; REQUIREMENTS.md / ROADMAP.md / STATE.md rolled forward.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/19-exerciser-screens/19-CONTEXT.md
@.planning/phases/19-exerciser-screens/19-UI-SPEC.md
@.planning/phases/19-exerciser-screens/19-VALIDATION.md
@.planning/phases/19-exerciser-screens/19-01-SUMMARY.md
@.planning/phases/19-exerciser-screens/19-02-SUMMARY.md
@.planning/phases/19-exerciser-screens/19-03-SUMMARY.md
@.planning/phases/19-exerciser-screens/19-04-SUMMARY.md
@.planning/phases/17-gallery-crate-skeleton-colocated-built-in-demos/17-VERIFICATION.md
@.planning/phases/18-catalog-screens/18-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full regression suite + start gallery-demo server</name>
  <files>(none created; verification only)</files>
  <read_first>
    - .planning/phases/19-exerciser-screens/19-01-SUMMARY.md (what Plan 19-01 changed)
    - .planning/phases/19-exerciser-screens/19-02-SUMMARY.md
    - .planning/phases/19-exerciser-screens/19-03-SUMMARY.md
    - .planning/phases/19-exerciser-screens/19-04-SUMMARY.md
  </read_first>
  <action>
    Run the full automated suite to confirm all green before UAT:

    1. Backend cargo tests:
       ```bash
       cd backend && cargo test --workspace
       cd backend && cargo clippy --workspace --all-targets -- -D warnings
       ```

    2. Frontend tests + type-check:
       ```bash
       cd frontend && pnpm test
       cd frontend && pnpm check
       ```

    3. Build the frontend for gallery-demo:
       ```bash
       cd frontend && pnpm build
       ```

    4. Start gallery-demo server in the background:
       ```bash
       cd backend && cargo run -p gallery-demo --features gallery &
       ```
       Wait ~5 s for server to come up. Verify with:
       ```bash
       curl -s http://localhost:3002/api/health
       ```
       Expected output: `ok`

    If ANY step fails, STOP and fix before proceeding. The UAT checkpoint depends on a green build.

    Record the initial log output from the gallery-demo server for the UAT record (port binding, any warn-level log lines, no panics).
  </action>
  <verify>
    <automated>cd backend && cargo test --workspace && cd ../frontend && pnpm test && pnpm check && curl -s http://localhost:3002/api/health | grep -q "^ok$"</automated>
  </verify>
  <acceptance_criteria>
    - `cd backend && cargo test --workspace` exits 0
    - `cd backend && cargo clippy --workspace --all-targets -- -D warnings` exits 0
    - `cd frontend && pnpm test` exits 0
    - `cd frontend && pnpm check` exits 0
    - `cd frontend && pnpm build` exits 0
    - `curl -s http://localhost:3002/api/health` returns `ok`
    - gallery-demo log shows `listening on 0.0.0.0:3002` without any `ERROR` or `WARN` lines beyond pre-existing ones
  </acceptance_criteria>
  <done>Full suite green; gallery-demo running on :3002; health check responds.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Chrome MCP UAT walkthrough — 3 exerciser screens, desktop + mobile</name>
  <files>.planning/phases/19-exerciser-screens/19-VERIFICATION.md (scratch notes during UAT)</files>
  <action>See `<how-to-verify>` below — drive Chrome MCP through the 3 exerciser screens at desktop + mobile widths, capture acceptance checklist results and the 4 EXER-03 perf baselines for Task 3 to transcribe into 19-VERIFICATION.md.</action>
  <verify>Human approval signal (blocking checkpoint — wait for "approved" or gap description).</verify>
  <done>User has typed "approved" OR described specific gap(s); gap handling follows D-4 (trivial inline fix OR v1.3 seed).</done>
  <what-built>
    Three new exerciser screens are live in the gallery-demo app:
    - **Exerciser: Nested AppShell** (`/#exer-01`) — real nested AppShell invocation with 4-cell observation matrix and v1.3 proposal Card
    - **Exerciser: Rapid Patching** (`/#exer-02`) — focused input + 4-option cadence RadioGroup + Start/Pause/Reset + 4-light invariant dashboard + patch log
    - **Exerciser: Pathological Scale** (`/#exer-03`) — 4-cell perf banner + 10 000-row virtualized DataTable + 80-field FormScreen across 4 FieldSet groups

    Gallery-demo is running on http://localhost:3002.
  </what-built>
  <how-to-verify>
    ## UAT checklist — drive via Chrome MCP (`feedback_use_chrome_for_uat.md`)

    **Setup:** Open Chrome. Navigate to http://localhost:3002. Confirm the outer gallery AppShell renders with all existing nav entries plus the three new exerciser entries at the top/bottom of the alphabetical nav list.

    ### EXER-01 Nested AppShell

    1. Click the "Exerciser: Nested AppShell" nav entry (or navigate to `/#exer-01`).
    2. **Visual confirmation of collision (evidence, not a bug):**
       - [ ] The outer gallery's sidebar (20+ nav entries) is REPLACED by the inner shell's 3-entry sidebar (Dashboard / Reports / Settings).
       - [ ] This IS expected per D-1 — confirms the G-02 collision is live.
    3. **Observation matrix (4 cells):**
       - [ ] Card 2 "Observation matrix" renders a 4-cell grid (1 col mobile, 2 cols sm, 4 cols lg).
       - [ ] Provider context cell: FAIL badge + details text about Symbol.for collision.
       - [ ] Mobile sheet cell: FAIL badge + details (resize browser below 768 px to confirm the FAIL becomes live — mobile sheets overlap).
       - [ ] Keyboard shortcuts cell: FAIL badge — press Ctrl+B; confirm BOTH shells toggle (check outer sidebar and inner sidebar open-state).
       - [ ] --sidebar-* tokens cell: WARN badge + details about CSS inheritance.
    4. **v1.3 proposal Card:**
       - [ ] Body text renders the UI-SPEC-locked copy about scoped-surface-name extension.
       - [ ] Click "Open seed draft" CTA → a toast appears with the seed file path `.planning/seeds/v1.3-appshell-nestability.md`.
    5. **Console check:** DevTools → Console. No red error messages. Expected: the probe's `sendAction` call is visible in Network → WS frames.
    6. **Resize test:** Narrow viewport to < 768 px. Confirm inner AppShell's mobile Sheet layers on top of outer's sheet (mobile-sheet FAIL cell becomes live).

    ### EXER-02 Rapid Patching

    1. Navigate back to gallery root, click "Exerciser: Rapid Patching" (`/#exer-02`).
    2. **Screen renders 4 Cards:**
       - [ ] Card 1: Focused input — TextInput with label "Type here — focus must not leak", placeholder, description.
       - [ ] Card 2: Cadence control — RadioGroup with 4 options (250 / 500 / 1000 / 2000 ms), Start/Pause/Reset button row.
       - [ ] Card 3: Invariant dashboard — 4 cells (Focus retention / Cursor position / Typed input / IME composition), each with icon + H4 label + Badge "PENDING" + details Text.
       - [ ] Card 4: Patch log — empty container with "No patches yet" placeholder.
    3. **Focus retention test (60 s × 500 ms cadence — the PATCH-02 proof):**
       - [ ] Click the Focused input to focus it; type a few characters (e.g., "hello world").
       - [ ] Click "Start patching" (default 500 ms cadence selected).
       - [ ] WAIT 60 seconds WITHOUT TOUCHING THE INPUT. Keep the window focused.
       - [ ] Confirm the cursor stays in the input; the "elapsed" counter in Card 3 increments ~every 500 ms; the Focus retention invariant badge stays PASS (or doesn't flip to FAIL).
       - [ ] After 60 s (elapsed ≥ 60), confirm patch log has ≥ 120 rows.
    4. **Cursor position test:**
       - [ ] Click Reset counters to clear state. Focus input. Type "abcdef". Press Home key + Right arrow × 2 to place cursor at position 2.
       - [ ] Click Start patching. Wait 10 s.
       - [ ] Confirm cursor still at position 2 (not jumped to end or 0). Cursor position invariant stays PASS.
    5. **Typed input integrity test:**
       - [ ] Click Reset counters. Click Start. While patches fire, type "the quick brown fox" into the input.
       - [ ] Confirm all 19 characters land; no dropped keystrokes. Typed input invariant stays PASS.
    6. **IME composition test (best-effort, Chrome DevTools synthetic):**
       - [ ] Click Reset counters. Open DevTools Console. Focus input.
       - [ ] Dispatch a synthetic compositionstart: `document.querySelector('#exer-02-focused-input input').dispatchEvent(new CompositionEvent('compositionstart'))`.
       - [ ] Click Start patching. Wait ~1 s.
       - [ ] Dispatch compositionend: `document.querySelector('#exer-02-focused-input input').dispatchEvent(new CompositionEvent('compositionend'))`.
       - [ ] Confirm the IME composition invariant badge flips to PASS (composition ended cleanly).
       - [ ] If Chrome OS IME is available, try composing actual CJK text (e.g., "你好") during patches. Expected: composition completes without interruption.
    7. **Pause + Reset:**
       - [ ] Click Pause — patch log stops growing; elapsed freezes.
       - [ ] Click Reset counters — log clears, elapsed goes to 0, 4 invariant badges return to PENDING.
    8. **Console check:** No errors. Network → WS shows `gallery-demo/exer-02/tick` actions firing at 500 ms cadence while running.

    ### EXER-03 Pathological Scale

    1. Navigate to "Exerciser: Pathological Scale" (`/#exer-03`).
    2. **Page mounts without freezing:**
       - [ ] Wall-clock stopwatch: page becomes interactive in < 10 s (hard gate). Record the approximate time.
    3. **Card 1 — Perf readouts:**
       - [ ] 4 cells: TTFP, Scroll FPS, Memory growth, Patch latency p95.
       - [ ] Within 30 s of mount, all 4 cells show numeric values (not `—` em-dash) — or Memory shows `—` if browser is non-Chromium (on Chrome: should be numeric).
       - [ ] Each cell's badge reads "WITHIN TARGET" or "OVER TARGET".
       - [ ] RECORD the observed values: TTFP ____ ms, Memory growth ____ MB, Patch latency p95 ____ ms.
    4. **Scroll FPS test:**
       - [ ] Scroll the DataTable Card for 5+ seconds. Confirm the Scroll FPS readout populates.
       - [ ] RECORD: observed median FPS ____ fps.
    5. **Card 2 — Pathological DataTable:**
       - [ ] Shows filter bar (Name + Status), column headers (ID, Name, Email, Score, Status, Joined, Actions), and ~10-20 visible rows.
       - [ ] Scroll downward — more rows load via fetch-rows (Network → WS shows fetch-rows actions). Scroll stays responsive.
       - [ ] Total rows indicator (if UI shows it) reads 10 000.
       - [ ] Column visibility toggle (if present) works.
       - [ ] Filter by name (e.g., type "smi" in the Name filter) — rows filter; table remains responsive.
    6. **Card 3 — Pathological FormScreen:**
       - [ ] 4 FieldSet groups with legends: Personal info, Contact, Preferences, Advanced.
       - [ ] Each group has 20 fields. Tab through the first group; confirm focus rings land on each field in order (~20 tab presses to exit group 1).
       - [ ] Spot-check: the Switch cluster in Preferences renders as 8 switches (notif-email through notif-updates). Toggle one — confirm its state updates in the data store (binding works).
       - [ ] FieldSeparator lines visible between groups.
    7. **Remeasure CTA:**
       - [ ] Click "Remeasure" in Card 1. Perf readouts refresh (or at least the remeasure-tick value updates). Confirm no errors.
    8. **Console check:** No errors. No uncaught promise rejections.
    9. **Non-Chromium check (optional):** If Firefox is available, navigate to `/#exer-03` — Memory readout should stay `—` with "Perf measurement API unavailable" implication (per UI-SPEC §Error messages line 315). Other 3 signals should still populate.

    ### Mobile UAT (desktop + mobile per project memory `feedback_use_chrome_for_uat.md`)

    - Chrome DevTools Device Mode → iPhone 14 Pro or similar. Repeat key visual checks:
      - [ ] EXER-01 observation matrix collapses to 2 cols, still readable.
      - [ ] EXER-01 inner AppShell mobile Sheet observable; dismiss cascade visible.
      - [ ] EXER-02 invariant dashboard STAYS 2×2 (per UI-SPEC line 446 — never collapses to 1 col).
      - [ ] EXER-02 control row (Start/Pause/Reset) wraps gracefully under ~360 px.
      - [ ] EXER-03 perf readouts stay 2×2.
      - [ ] EXER-03 FormScreen FieldSet collapses to `cols=1` (single column per group) at mobile width.

    ### Final evidence recording

    For each exerciser, record in `19-VERIFICATION.md`:
    - Screenshot (or description of visible state) at desktop + mobile
    - Pass/fail per acceptance item (all items should pass per D-3 exer-03 advisory caveat)
    - Any findings triggered (per D-4: trivial fixes apply inline; substantive findings → defer to v1.3 seeds)
    - Observed performance baselines (EXER-03) as numeric values
  </how-to-verify>
  <resume-signal>Type "approved" once all three exerciser walkthroughs complete (with evidence recorded in VERIFICATION.md scratch notes). Type "gap" followed by a description if any checklist item fails — executor will then either apply a trivial inline fix (per D-4) or create a v1.3 seed.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Populate 19-VERIFICATION.md + 19-VALIDATION.md; update REQUIREMENTS / ROADMAP / STATE</name>
  <files>.planning/phases/19-exerciser-screens/19-VERIFICATION.md, .planning/phases/19-exerciser-screens/19-VALIDATION.md, .planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/STATE.md</files>
  <read_first>
    - .planning/phases/18-catalog-screens/18-VERIFICATION.md (analog — prior phase VERIFICATION format)
    - .planning/phases/17-gallery-crate-skeleton-colocated-built-in-demos/17-VERIFICATION.md (analog)
    - .planning/phases/19-exerciser-screens/19-VALIDATION.md (current draft frontmatter + template)
    - .planning/REQUIREMENTS.md (current state — EXER-01/02/03 rows Pending)
    - .planning/ROADMAP.md §Phase 19 (current)
    - .planning/STATE.md (Session Continuity / Blockers / Last activity)
    - Checkpoint scratch notes from Task 2 (UAT evidence the executor recorded)
  </read_first>
  <action>
    **Part A — write `.planning/phases/19-exerciser-screens/19-VERIFICATION.md`** in the same shape as Phase 18's VERIFICATION. Include:

    ```markdown
    ---
    phase: 19
    slug: exerciser-screens
    status: verified
    verified_date: "2026-04-XX"       # TODAY
    verified_via: "Chrome MCP UAT (desktop + mobile)"
    upstream:
      - 19-01-SUMMARY.md
      - 19-02-SUMMARY.md
      - 19-03-SUMMARY.md
      - 19-04-SUMMARY.md
    ---

    # Phase 19 — Verification Record

    ## Scope verified

    All 3 exerciser screens (EXER-01 / EXER-02 / EXER-03) ship with working
    auto-discovered nav entries, full-fidelity composition matching UI-SPEC, and
    passing automated + manual acceptance.

    ## Automated suite

    | Suite | Command | Result |
    |-------|---------|--------|
    | Backend unit + integration | `cargo test --workspace` | PASS |
    | Backend clippy | `cargo clippy --workspace --all-targets -- -D warnings` | PASS |
    | Frontend unit + browser | `pnpm test` | PASS |
    | Frontend typecheck | `pnpm check` | PASS |
    | Frontend build | `pnpm build` | PASS |

    ## Manual acceptance (Chrome MCP UAT)

    ### EXER-01 Nested AppShell

    | Acceptance item | Result | Notes |
    |-----------------|--------|-------|
    | Outer sidebar visually replaced by inner 3-entry sidebar | PASS | Confirms G-02 collision live — per D-1 this is evidence, not a bug |
    | Observation matrix renders 4 cells with FAIL × 3 + WARN × 1 | PASS | Matches UI-SPEC lines 180-183 |
    | Open seed draft CTA toasts the seed file path | PASS | Toast contains `.planning/seeds/v1.3-appshell-nestability.md` |
    | Ctrl+B toggles BOTH shells (keyboard shortcut collision) | PASS | Confirms the live FAIL in Keyboard shortcuts cell |
    | Mobile sheet cascade visible at viewport < 768 px | PASS | Mobile sheet FAIL becomes live |
    | No console errors | PASS | — |

    ### EXER-02 Rapid Patching

    | Acceptance item | Result | Notes |
    |-----------------|--------|-------|
    | 4 Cards render (Focused input / Cadence / Invariant dashboard / Patch log) | PASS | — |
    | Focus retained for 60 s × 500 ms cadence (≥ 120 patches) | PASS | PATCH-02 invariant upheld |
    | Cursor position stable under sustained patches | PASS | Cursor stayed at position 2 after 10 s |
    | Typed input integrity — all keystrokes preserved | PASS | 19-char type test clean |
    | IME composition completes cleanly during patches | PASS | Verified via synthetic compositionstart/end + optional real IME |
    | Start/Pause/Reset all work | PASS | Pause freezes; Reset clears log + resets badges |
    | Patch log populates with per-tick entries | PASS | ≥ 120 rows after 60 s |
    | Invariant dashboard stays 2×2 at mobile width | PASS | Never collapsed to 1 col |
    | No console errors | PASS | — |

    ### EXER-03 Pathological Scale

    | Acceptance item | Result | Notes |
    |-----------------|--------|-------|
    | Page mounts in < 10 s | PASS | Actual: ~X s — fill in |
    | 4 perf readouts populate within 30 s | PASS | All non-em-dash on Chrome |
    | DataTable virtualizes 10 000 rows | PASS | Scroll stayed responsive; fetch-rows paginated |
    | 80 fields render across 4 FieldSet groups | PASS | All focusable in tab order |
    | Remeasure CTA triggers re-capture | PASS | remeasure-tick value updated |
    | No console errors | PASS | — |

    ## Advisory performance baselines (EXER-03, per D-3)

    Recorded on dev machine — Chrome on Ubuntu, approx system profile ____.

    | Signal | Observed | Advisory target | Within target? |
    |--------|----------|-----------------|----------------|
    | TTFP | ____ ms | ≤ 3000 ms | [yes/no] |
    | Scroll FPS | ____ fps | ≥ 30 fps | [yes/no] |
    | Memory growth (after 30 s scroll) | ____ MB | ≤ +50 MB | [yes/no] |
    | Patch latency p95 | ____ ms | ≤ 50 ms | [yes/no] |

    Per D-3, missing a threshold is a FINDING not a FAILURE. If any signal is
    "no" above, the corresponding deferred-finding is recorded in `## Findings`
    below.

    ## Findings

    ### Deferred to v1.3 (per D-4 default)

    - `.planning/seeds/v1.3-appshell-nestability.md` — scoped-surface-name framework
      extension (drafted in Plan 19-02; this is the primary Phase 19 artefact in the
      "deferred" column because EXER-01's whole job was to draft this seed).
    - [any other findings captured during UAT — list with a v1.3 seed file reference]

    ### Trivial fixes applied inline (per D-4 carve-out)

    - [none OR list — each must be < 30 min, no scope expansion, no new deps]

    ## Requirement closure

    | Requirement | Evidence |
    |-------------|----------|
    | EXER-01 | Plans 19-02 artefacts (exerciser/nested_appshell.rs + handlers/exer01.rs + observe.svelte.ts + seed); UAT confirmed observation matrix renders; Open seed draft toast works |
    | EXER-02 | Plans 19-03 artefacts (exerciser/rapid_patching.rs + handlers/exer02.rs + invariants.svelte.ts); UAT confirmed PATCH-02 focus retention at 60 s × 500 ms; 4 invariants live |
    | EXER-03 | Plans 19-04 artefacts (exerciser/pathological_scale.rs + handlers/exer03.rs + perf.svelte.ts + exer-03-synthetic fetch-rows arm at 10k); UAT confirmed 4 perf readouts + 10k DataTable + 80 fields |

    ## Sign-off

    - [x] All 3 exerciser nav entries render and execute without console errors
    - [x] UI-SPEC dimensions (copy / visuals / color / typography / spacing / registry) all PASS
    - [x] Automated test suite all PASS
    - [x] Manual acceptance all PASS (per tables above)
    - [x] Advisory perf baselines recorded (regardless of within-target status — D-3 advisory not gating)
    - [x] Findings routed: either trivial-fix inline or v1.3 seed drafted
    - [x] v1.3 seed `.planning/seeds/v1.3-appshell-nestability.md` exists with Problem / Proposed scope / Acceptance sections

    **Status:** verified — Phase 19 closed. Phase 20 (Live Token Editor) unblocked.
    ```

    Fill in the `____` placeholders with actual values from the UAT checkpoint. For the "observed" perf numbers, transcribe exactly what Chrome showed — don't round.

    **Part B — update `.planning/phases/19-exerciser-screens/19-VALIDATION.md`.**

    Read the current draft. Flip frontmatter:
    ```yaml
    status: verified           # was: draft
    nyquist_compliant: true    # was: false
    wave_0_complete: true      # was: false
    verified_date: "2026-04-XX"
    ```

    Populate the `## Per-Task Verification Map` table with rows for each task across Plans 19-01 / 19-02 / 19-03 / 19-04, citing the actual `cargo test` / `pnpm vitest` commands each plan already has. Mark Status ✅ green for all. Example rows:

    ```markdown
    | Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
    |---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
    | 19-01-T1 | 19-01 | 1 | (framework) | — | N/A | vitest | `pnpm vitest run src/lib/init.patchprobe.test.ts` | ✅ | ✅ green |
    | 19-01-T2 | 19-01 | 1 | (framework) | — | N/A | cargo test | `cargo test -p gallery-demo --features gallery state::tests` | ✅ | ✅ green |
    | 19-01-T3 | 19-01 | 1 | EXER-03 | T-19-01 | cadence clamping scaffold | cargo test | `cargo test -p gallery-demo --features gallery handlers::show::tests handlers::fetch_rows::tests::exer03` | ✅ | ✅ green |
    | 19-02-T1 | 19-02 | 2 | EXER-01 | — | N/A | cargo test | `cargo test -p gallery-demo --features gallery exerciser::nested_appshell::tests` | ✅ | ✅ green |
    | 19-02-T2 | 19-02 | 2 | EXER-01 | T-19-02-02 | safe path echo | cargo test + vitest | `cargo test -p gallery-demo --features gallery handlers::exer01::tests; pnpm vitest run src/lib/exer01/observe.test.ts` | ✅ | ✅ green |
    | 19-03-T1 | 19-03 | 2 | EXER-02 | — | N/A | cargo test | `cargo test -p gallery-demo --features gallery exerciser::rapid_patching::tests` | ✅ | ✅ green |
    | 19-03-T2 | 19-03 | 2 | EXER-02 | T-19-01, T-19-03-03 | cadence clamp + Pitfall 2 guard | cargo test | `cargo test -p gallery-demo --features gallery handlers::exer02::tests` | ✅ | ✅ green |
    | 19-03-T3 | 19-03 | 2 | EXER-02 | — | N/A | vitest | `pnpm vitest run src/lib/exer02/invariants.test.ts` | ✅ | ✅ green |
    | 19-04-T1 | 19-04 | 2 | EXER-03 | — | N/A | cargo test | `cargo test -p gallery-demo --features gallery exerciser::pathological_scale::tests` | ✅ | ✅ green |
    | 19-04-T2 | 19-04 | 2 | EXER-03 | T-19-04-01 | threshold evaluation | cargo test | `cargo test -p gallery-demo --features gallery handlers::exer03::tests` | ✅ | ✅ green |
    | 19-04-T3 | 19-04 | 2 | EXER-03 | — | N/A | vitest | `pnpm vitest run src/lib/exer03/perf.test.ts` | ✅ | ✅ green |
    ```

    Mark the Manual-Only Verifications section as checked (all entries covered by Task 2 checkpoint):

    ```markdown
    | Behavior | Requirement | Why Manual | UAT Result |
    |----------|-------------|------------|------------|
    | EXER-01 4-dimension observation matrix renders | EXER-01 | Visual-DOM collision only observable in browser | ✅ PASS — see 19-VERIFICATION.md EXER-01 table |
    | EXER-02 focus retention for 60 s at 500 ms cadence | EXER-02 | IME hardware + sustained human presence | ✅ PASS — PATCH-02 invariant upheld |
    | EXER-03 perf readouts on 10k-row page | EXER-03 | Hardware-dependent perf; advisory-only | ✅ PASS — all 4 populated; baselines in 19-VERIFICATION.md |
    ```

    Flip `**Approval:** pending` → `**Approval:** verified 2026-04-XX`.

    **Part C — update `.planning/REQUIREMENTS.md`:**

    Replace the three Pending rows in §Exerciser screens and in the Traceability table.

    In §Exerciser screens (lines 44-48):
    ```markdown
    - [x] **EXER-01**: Gallery includes a Nested AppShell screen ... Validated 2026-04-XX — see [19-02-SUMMARY.md](phases/19-exerciser-screens/19-02-SUMMARY.md) and [19-VERIFICATION.md](phases/19-exerciser-screens/19-VERIFICATION.md).
    - [x] **EXER-02**: Gallery includes a Rapid Patching screen ... Validated 2026-04-XX — see [19-03-SUMMARY.md](phases/19-exerciser-screens/19-03-SUMMARY.md) and [19-VERIFICATION.md](phases/19-exerciser-screens/19-VERIFICATION.md).
    - [x] **EXER-03**: Gallery includes a Pathological Scale screen ... Validated 2026-04-XX — see [19-04-SUMMARY.md](phases/19-exerciser-screens/19-04-SUMMARY.md) and [19-VERIFICATION.md](phases/19-exerciser-screens/19-VERIFICATION.md).
    ```

    In the Traceability table:
    ```markdown
    | EXER-01 | Phase 19 | 19-02 | ✅ Validated 2026-04-XX ([19-02-SUMMARY.md](phases/19-exerciser-screens/19-02-SUMMARY.md)) |
    | EXER-02 | Phase 19 | 19-03 | ✅ Validated 2026-04-XX ([19-03-SUMMARY.md](phases/19-exerciser-screens/19-03-SUMMARY.md)) |
    | EXER-03 | Phase 19 | 19-04 | ✅ Validated 2026-04-XX ([19-04-SUMMARY.md](phases/19-exerciser-screens/19-04-SUMMARY.md)) |
    ```

    Update the Coverage section:
    ```markdown
    **Coverage:**
    - v1.2 requirements: 20 total
    - Mapped to phases: 20
    - Validated: 11 (Phase 17's 8 + Phase 18's 5 now + Phase 19's 3 = 11 total — verify current count and increment)
    - Unmapped: 0
    ```

    **NB:** current REQUIREMENTS.md shows Phase 17 items 8 + Phase 18 5 = 13 validated. After Phase 19: 13 + 3 = 16. Adjust wording to match actual state.

    **Part D — update `.planning/ROADMAP.md` Phase 19 entry:**

    Replace:
    ```markdown
    - [ ] **Phase 19: Exerciser Screens** — Nested AppShell, Rapid Patching, Pathological Scale
    ```
    with:
    ```markdown
    - [x] **Phase 19: Exerciser Screens** — Nested AppShell, Rapid Patching, Pathological Scale (completed 2026-04-XX)
    ```

    Update the Phase Details §Phase 19 block:
    - `**Plans:** 5 plans`
    - Add the plan list:
      ```
      Plans:
      - [x] 19-01-PLAN.md — Wave 1 framework polish (16 icons + patchProbe + state extension + handler stubs + 3 seed arms + exer-03-synthetic source)
      - [x] 19-02-PLAN.md — EXER-01 Nested AppShell (real nested shell + observation matrix + v1.3 seed)
      - [x] 19-03-PLAN.md — EXER-02 Rapid Patching (client-initiated tick + 4 invariants)
      - [x] 19-04-PLAN.md — EXER-03 Pathological Scale (10k DataTable + 80-field FormScreen + 4 perf signals)
      - [x] 19-05-PLAN.md — Chrome MCP UAT + phase close-out
      ```

    Update the Progress table row for Phase 19:
    ```
    | 19. Exerciser Screens | v1.2 | 5/5 | Complete | 2026-04-XX |
    ```

    Append a dated "Updated" footnote at the bottom:
    ```
    *Updated: 2026-04-XX — **Phase 19 COMPLETE** (all 3 exercisers ship; EXER-01 observation matrix + v1.3 seed delivered per D-1; PATCH-02 invariant upheld at 60 s × 500 ms via client-initiated tick resolution; EXER-03 4 advisory perf baselines recorded; REQUIREMENTS EXER-01/02/03 validated; v1.2 progress now 4/5 phases — Phase 20 Live Token Editor is the last remaining phase of v1.2)*
    ```

    **Part E — update `.planning/STATE.md`:**

    Update frontmatter:
    ```yaml
    stopped_at: Phase 19 complete
    last_updated: "2026-04-XXTXX:XX:XX.000Z"
    last_activity: "2026-04-XX -- Phase 19 complete"
    progress:
      total_phases: 11          # verify current count
      completed_phases: 4       # was 3 (16 + 17 + 18); now +19 = 4
      total_plans: 25           # was 20; +5 Phase 19 plans = 25
      completed_plans: 25
      percent: 100              # v1.2 is not yet 100%; recompute if STATE tracks v1.2 vs all — see current state
    ```

    Note: the current STATE.md shows progress as 100% for the milestone-so-far tracker (not the whole v1.2). Verify and adjust accordingly — the numbers above are illustrative.

    In the `## Current Position` section, update:
    ```markdown
    Phase: 19 (exerciser-screens) — COMPLETE (2026-04-XX)
    Plan: 5 of 5 shipped
    Status: Phase 19 verified via Chrome MCP UAT (desktop + mobile, all 3 exercisers, 4 perf baselines recorded)
    Last activity: 2026-04-XX -- Phase 19 complete
    ```

    Move any Phase 19-specific items out of `### Blockers/Concerns` (the AppShell nestability blocker is now resolved-by-handoff — mark it as "✅ resolved via v1.3 seed 2026-04-XX" with a reference to `.planning/seeds/v1.3-appshell-nestability.md`).

    Add a new `### Phase 19 close-out (2026-04-XX)` section summarizing what shipped (3 exercisers), what deferred (v1.3 seed + any UAT-found items), and what comes next (Phase 20 Live Token Editor).

    **Part F — stop the gallery-demo server** (clean up Task 1's background process):
    ```bash
    # Identify pid of cargo-run-started gallery-demo
    pkill -f "gallery-demo" || true
    ```
  </action>
  <verify>
    <automated>grep "status: verified" .planning/phases/19-exerciser-screens/19-VERIFICATION.md && grep "nyquist_compliant: true" .planning/phases/19-exerciser-screens/19-VALIDATION.md && grep -E "EXER-0[123] \| Phase 19.*✅ Validated" .planning/REQUIREMENTS.md | wc -l | grep -q "^3$" && grep -E "^- \[x\] \*\*Phase 19:" .planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep "status: verified" .planning/phases/19-exerciser-screens/19-VERIFICATION.md` matches exactly one line
    - `grep "nyquist_compliant: true" .planning/phases/19-exerciser-screens/19-VALIDATION.md` matches exactly one line
    - `grep -E "EXER-0[123] \| Phase 19.*Validated" .planning/REQUIREMENTS.md | wc -l` returns 3
    - `grep "^- \[x\] \*\*Phase 19:" .planning/ROADMAP.md` matches one line
    - `grep "Phase 19 complete" .planning/STATE.md` matches ≥ 1 line
    - `grep -E "\| 19\. Exerciser Screens \| v1\.2 \| 5/5 \| Complete" .planning/ROADMAP.md` matches one line
    - `.planning/phases/19-exerciser-screens/19-VERIFICATION.md` contains non-placeholder values (no remaining `____` in perf baseline numbers OR explicit note "not measured — memory API unavailable")
  </acceptance_criteria>
  <done>VERIFICATION.md + VALIDATION.md reflect Phase 19 verified status with real UAT evidence; REQUIREMENTS.md EXER-01/02/03 validated; ROADMAP.md Phase 19 entry marked complete; STATE.md rolled forward; gallery-demo process stopped cleanly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Human ↔ gallery-demo UAT | User drives Chrome MCP against `localhost:3002`; no external network boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-05-01 | (N/A) | UAT-only plan | accept | No new code surface. Plan is documentation + manual verification. All upstream plans' threat models apply. |
</threat_model>

<verification>
- Task 1 automated suite green
- Task 2 UAT walkthrough completes successfully (gate: human approval signal)
- Task 3 doc updates pass their grep-verifiable acceptance criteria
- Gallery-demo process stopped cleanly at end of plan (ps shows no lingering cargo-run-gallery)
</verification>

<success_criteria>
Plan 19-05 complete when:

1. Full automated suite green (cargo test --workspace, pnpm test, clippy, pnpm check, pnpm build)
2. Chrome MCP UAT walkthrough all 3 exerciser screens, at desktop + mobile, with evidence recorded
3. All 4 EXER-03 advisory perf baselines measured + recorded (even if some are null due to non-Chromium memory)
4. 19-VERIFICATION.md written with `status: verified` + UAT tables + findings + sign-off
5. 19-VALIDATION.md flipped to `status: verified` + `nyquist_compliant: true` + Per-Task Verification Map populated
6. REQUIREMENTS.md EXER-01/02/03 marked ✅ Validated
7. ROADMAP.md Phase 19 entry marked complete with 5/5 plans
8. STATE.md rolled forward — Phase 19 close-out section added; nestability blocker now points at v1.3 seed
9. `.planning/seeds/v1.3-appshell-nestability.md` exists (verified in Plan 19-02) — this is the primary deferred artefact from the phase
10. gallery-demo background process stopped
</success_criteria>

<output>
After completion, create `.planning/phases/19-exerciser-screens/19-05-SUMMARY.md` with:

- Chrome MCP UAT summary (pass/fail per acceptance item)
- 4 recorded advisory perf baselines (TTFP / FPS / Memory / Latency) with the within-target booleans
- Any UAT-triggered findings and their disposition (trivial-inline-fix OR v1.3 seed)
- v1.2 milestone progress snapshot: 4 of 5 phases complete (Phase 20 remaining)
- Sign-off that Phase 19 closed cleanly and Phase 20 is unblocked
</output>
