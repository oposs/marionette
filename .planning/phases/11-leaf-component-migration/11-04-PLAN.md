---
phase: 11-leaf-component-migration
plan: 04
type: execute
wave: 2
depends_on: [11-01]
files_modified:
  - frontend/src/lib/components/layout/Container.svelte
  - frontend/src/lib/components/layout/Grid.svelte
  - frontend/src/lib/components/layout/Heading.svelte
  - frontend/src/lib/components/layout/Text.svelte
  - frontend/src/lib/components/nav/SideNav.svelte
  - frontend/src/lib/components/nav/NavItem.svelte
  - frontend/src/lib/components/nav/NavGroup.svelte
  - frontend/src/lib/components/core/LoadingSkeleton.svelte
  - frontend/src/lib/components/core/FallbackComponent.svelte
  - frontend/src/lib/components/core/ConnectionBanner.svelte
  - frontend/src/lib/components/core/ErrorBoundary.svelte
  - frontend/src/lib/components/feedback/Spinner.svelte
  - frontend/src/lib/components/feedback/ErrorDisplay.svelte
  - frontend/src/lib/components/core/NodeRenderer.svelte
  - frontend/src/lib/components/core/Surface.svelte
  - frontend/src/lib/components/layout/Container.browser-test.ts
  - frontend/src/lib/components/layout/Grid.browser-test.ts
  - frontend/src/lib/components/layout/Heading.browser-test.ts
  - frontend/src/lib/components/layout/Text.browser-test.ts
  - frontend/src/lib/components/nav/SideNav.browser-test.ts
  - frontend/src/lib/components/nav/NavItem.browser-test.ts
  - frontend/src/lib/components/nav/NavGroup.browser-test.ts
  - frontend/src/lib/components/core/LoadingSkeleton.browser-test.ts
  - frontend/src/lib/components/feedback/Spinner.browser-test.ts
  - frontend/src/lib/components/feedback/ErrorDisplay.browser-test.ts
  - frontend/src/lib/components/core/ConnectionBanner.browser-test.ts
  - frontend/src/lib/components/core/FallbackComponent.browser-test.ts
  - frontend/src/lib/components/core/NodeRenderer.browser-test.ts
  - frontend/src/lib/components/core/Surface.browser-test.ts
autonomous: true
requirements: [COMP-01, COMP-02]
must_haves:
  truths:
    - "Container card variant uses shadcn Card component"
    - "Grid uses inline style for columns, not dynamic Tailwind classes"
    - "NavItem uses shadcn Button ghost variant with icon support"
    - "LoadingSkeleton uses shadcn Skeleton component"
    - "FallbackComponent uses semantic destructive tokens, not hardcoded red"
    - "ConnectionBanner uses destructive semantic tokens per UI-SPEC"
    - "All layout/nav/core/feedback components have passing browser tests"
  artifacts:
    - path: "frontend/src/lib/components/layout/Container.svelte"
      provides: "Card variant using shadcn Card"
      contains: "Card"
    - path: "frontend/src/lib/components/layout/Grid.svelte"
      provides: "Fixed grid with inline style"
      contains: "grid-template-columns"
    - path: "frontend/src/lib/components/nav/NavItem.svelte"
      provides: "Ghost button nav item with icon"
      contains: "getIcon"
    - path: "frontend/src/lib/components/core/LoadingSkeleton.svelte"
      provides: "shadcn Skeleton pulse animation"
      contains: "Skeleton"
  key_links:
    - from: "frontend/src/lib/components/layout/Container.svelte"
      to: "$lib/components/ui/card"
      via: "import"
      pattern: "from '\\$lib/components/ui/card'"
    - from: "frontend/src/lib/components/nav/NavItem.svelte"
      to: "$lib/registry/icons"
      via: "getIcon for nav icons"
      pattern: "getIcon"
---

<objective>
Migrate all layout, navigation, core, and feedback components to shadcn-svelte primitives with browser tests.

Purpose: These components are mostly minimal-complexity (simple HTML with semantic tokens) but several have specific issues to fix: Grid's dynamic Tailwind class bug (Pitfall 4), LoadingSkeleton's hardcoded colors (Pitfall 6), FallbackComponent's raw red colors, and NavItem's missing icon support. This plan addresses all of them.

Output: 13 migrated components (most with small changes), 14 browser test files (3 rewritten, 11 new).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-leaf-component-migration/11-CONTEXT.md
@.planning/phases/11-leaf-component-migration/11-RESEARCH.md
@.planning/phases/11-leaf-component-migration/11-UI-SPEC.md
@.planning/phases/11-leaf-component-migration/11-01-SUMMARY.md

<interfaces>
<!-- From $lib/registry/icons.ts (Plan 01 output) -->
```typescript
export function getIcon(name: string): Component;
export function registerIcon(name: string, component: Component): void;
```

<!-- Expected shadcn Card API -->
```typescript
import * as Card from '$lib/components/ui/card';
// Card.Root, Card.Header, Card.Title, Card.Description, Card.Content, Card.Footer
```

<!-- Expected shadcn Skeleton API -->
```typescript
import { Skeleton } from '$lib/components/ui/skeleton';
// <Skeleton class="h-4 w-full" />
```

<!-- Expected shadcn Button API (ghost variant for NavItem) -->
```typescript
import { Button as ShadcnButton } from '$lib/components/ui/button';
// <ShadcnButton variant="ghost" class="w-full justify-start">
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate layout, nav, core, and feedback components</name>
  <files>
    frontend/src/lib/components/layout/Container.svelte
    frontend/src/lib/components/layout/Grid.svelte
    frontend/src/lib/components/layout/Heading.svelte
    frontend/src/lib/components/layout/Text.svelte
    frontend/src/lib/components/nav/SideNav.svelte
    frontend/src/lib/components/nav/NavItem.svelte
    frontend/src/lib/components/nav/NavGroup.svelte
    frontend/src/lib/components/core/LoadingSkeleton.svelte
    frontend/src/lib/components/core/FallbackComponent.svelte
    frontend/src/lib/components/core/ConnectionBanner.svelte
    frontend/src/lib/components/core/ErrorBoundary.svelte
    frontend/src/lib/components/feedback/Spinner.svelte
    frontend/src/lib/components/feedback/ErrorDisplay.svelte
    frontend/src/lib/components/core/NodeRenderer.svelte
    frontend/src/lib/components/core/Surface.svelte
  </files>
  <read_first>
    frontend/src/lib/components/layout/Container.svelte
    frontend/src/lib/components/layout/Grid.svelte
    frontend/src/lib/components/layout/Heading.svelte
    frontend/src/lib/components/layout/Text.svelte
    frontend/src/lib/components/nav/SideNav.svelte
    frontend/src/lib/components/nav/NavItem.svelte
    frontend/src/lib/components/nav/NavGroup.svelte
    frontend/src/lib/components/core/LoadingSkeleton.svelte
    frontend/src/lib/components/core/FallbackComponent.svelte
    frontend/src/lib/components/core/ConnectionBanner.svelte
    frontend/src/lib/components/core/ErrorBoundary.svelte
    frontend/src/lib/components/feedback/Spinner.svelte
    frontend/src/lib/components/feedback/ErrorDisplay.svelte
    frontend/src/lib/components/core/NodeRenderer.svelte
    frontend/src/lib/components/core/Surface.svelte
    .planning/phases/11-leaf-component-migration/11-UI-SPEC.md
  </read_first>
  <action>
Migrate each component. Many need only minor semantic token fixes. A few need shadcn primitive integration.

**Container.svelte:**
- Import `* as Card` from `$lib/components/ui/card`
- Read current file to see the `isCard` derived logic
- When `isCard` is true: render `<Card.Root class="p-4">{@render children?.()}</Card.Root>` (per UI-SPEC: `bg-card border-border rounded-md` with 16px padding -- shadcn Card provides all except padding)
- When `isCard` is false: render plain `<div>{@render children?.()}</div>` (no border, no bg, layout grouping only per UI-SPEC)
- Keep SDUI contract props

**Grid.svelte (FIX Pitfall 4):**
- Replace the dynamic Tailwind class `grid-cols-${cols}` with inline style
- Change from: `` `grid grid-cols-${cols} ${gapClass}` ``
- Change to: keep `grid` class and `gapClass`, use `style="grid-template-columns: repeat({cols}, 1fr)"` on the div
- For flex mode: keep existing `flex flex-wrap {gapClass}` as-is
- The `layoutClass` derived should NOT include `grid-cols-${cols}` -- only `grid {gapClass} {flow === 'col' ? 'grid-flow-col' : ''}`
- Add `style` attribute: `style={useFlex ? undefined : \`grid-template-columns: repeat(${cols}, 1fr)\`}`

**Heading.svelte:**
- Read current file. It likely renders semantic h1-h6 tag.
- Per UI-SPEC: h1/h2 use `text-xl font-semibold text-foreground`, h3-h6 use `text-base font-semibold text-foreground`
- Use `font-semibold` (weight 600) per UI-SPEC typography contract (NOT font-medium)
- No bottom margin (parent controls spacing per UI-SPEC)
- If file uses `font-medium`, change to `font-semibold`

**Text.svelte:**
- Read current file.
- Per UI-SPEC: renders as `<p>`, 14px regular (text-sm), `text-foreground`
- Muted variant: `text-muted-foreground` when `props.muted=true`
- Ensure it uses `text-sm` (14px) and `text-foreground` (or `text-muted-foreground` for muted)

**SideNav.svelte:**
- Per UI-SPEC: vertical flex column, `bg-sidebar-background` (check if this CSS variable exists in app.css; if not, use `bg-card`), full height
- Current file is minimal -- just a nav with padding. Update class to include sidebar background per UI-SPEC.
- Keep existing `aria-label` and children rendering

**NavItem.svelte:**
- Import `{ Button as ShadcnButton }` from `$lib/components/ui/button`
- Import `getIcon` from `$lib/registry/icons`
- Replace the manual `<button>` with `<ShadcnButton variant="ghost" class="w-full justify-start gap-2 {isActive ? 'bg-sidebar-accent text-sidebar-accent-foreground' : ''}">`
- Add icon support: if `props.icon` is set, render the icon component from `getIcon(props.icon as string)` at 16px (class="size-4") before label text per UI-SPEC
- Keep existing `handleClick` and `isActive` logic
- Active state per UI-SPEC: `bg-sidebar-accent text-sidebar-accent-foreground` (check if these CSS variables exist; if not, use `bg-accent text-accent-foreground`)

**NavGroup.svelte:**
- Read current file.
- Per UI-SPEC: 8px top margin, optional 12px semibold uppercase group label in `text-muted-foreground`
- If `props.label` exists, render: `<p class="px-3 py-1 text-xs font-semibold uppercase text-muted-foreground">{props.label}</p>`
- Add `class="mt-2"` to container div for 8px top margin
- Render children after the optional label

**LoadingSkeleton.svelte (FIX Pitfall 6):**
- Import `{ Skeleton }` from `$lib/components/ui/skeleton`
- Replace the custom `<div class="animate-pulse bg-gray-200 dark:bg-gray-700 ...">` elements with `<Skeleton class="h-4 mb-2" style="width: {widths[i]}%" />`
- shadcn Skeleton uses `bg-primary/10` with pulse animation per UI-SPEC
- Keep the existing `lines` prop and `widths` array
- Per UI-SPEC: 3 rows of varying widths (100%, 80%, 60%) with 8px gaps

**FallbackComponent.svelte:**
- Replace hardcoded `border-red-500 bg-red-50 text-red-700` with `border-destructive bg-destructive/10 text-destructive` per UI-SPEC
- Replace `text-red-600` with `text-destructive`
- Keep the `import.meta.env.DEV` guard and existing logic

**ConnectionBanner.svelte:**
- Per UI-SPEC: full-width bar at top, `bg-destructive text-primary-foreground`, 8px vertical padding, 14px semibold centered text
- Current file uses `bg-accent text-accent-foreground` -- change to `bg-destructive text-primary-foreground` per UI-SPEC
- Add `font-semibold` to text per UI-SPEC
- Replace inline SVG spinner with lucide `Loader2`: import from `@lucide/svelte/icons/loader-2`, render `<Loader2 class="size-4 animate-spin" />`

**ErrorBoundary.svelte:**
- Read current file. It's a Svelte boundary component, not visual.
- Likely needs no changes. Only update if it has raw color classes.

**Spinner.svelte:**
- Per UI-SPEC: 24px default, `text-primary`, `animate-spin`, centered
- Current file likely has an inline SVG. Keep the SVG spinner but ensure it uses `text-primary` for color
- Or import `Loader2` from `@lucide/svelte/icons/loader-2` and render `<Loader2 class="size-6 animate-spin text-primary" />`

**ErrorDisplay.svelte:**
- Already uses lucide `AlertCircle` and semantic tokens. Read and verify.
- Per UI-SPEC: icon 20px (`size-5`), `text-destructive`, message 14px `text-foreground`, background `bg-destructive/10`, 8px padding, `rounded-md`
- Current file looks correct. Only fix if any raw colors remain.

**NodeRenderer.svelte and Surface.svelte:**
- Read both files. These are structural/renderer components, not visual.
- They should need no changes (they don't render UI themselves, they delegate to registered components).
- Only update if they contain raw color classes or Flowbite references.
  </action>
  <verify>
    <automated>cd frontend && grep "Card" src/lib/components/layout/Container.svelte && grep "grid-template-columns" src/lib/components/layout/Grid.svelte && grep "Skeleton" src/lib/components/core/LoadingSkeleton.svelte && grep "border-destructive" src/lib/components/core/FallbackComponent.svelte && npm run build 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - Container.svelte contains `import * as Card from '$lib/components/ui/card'`
    - Container.svelte contains `Card.Root`
    - Grid.svelte contains `grid-template-columns: repeat(` (inline style)
    - Grid.svelte does NOT contain `grid-cols-${cols}` (dynamic Tailwind class removed)
    - Heading.svelte contains `font-semibold` (not font-medium)
    - NavItem.svelte contains `import { Button as ShadcnButton } from '$lib/components/ui/button'`
    - NavItem.svelte contains `getIcon` import
    - LoadingSkeleton.svelte contains `import { Skeleton }` from shadcn
    - LoadingSkeleton.svelte does NOT contain `bg-gray-200` or `bg-gray-700`
    - FallbackComponent.svelte contains `border-destructive bg-destructive/10 text-destructive`
    - FallbackComponent.svelte does NOT contain `border-red-500` or `bg-red-50`
    - ConnectionBanner.svelte contains `bg-destructive text-primary-foreground`
    - ConnectionBanner.svelte does NOT contain `bg-accent text-accent-foreground`
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>All 13 layout/nav/core/feedback components migrated. Grid Tailwind bug fixed. LoadingSkeleton uses shadcn Skeleton. All raw color classes replaced with semantic tokens. Frontend builds.</done>
</task>

<task type="auto">
  <name>Task 2: Write browser tests for all layout/nav/core/feedback components</name>
  <files>
    frontend/src/lib/components/layout/Container.browser-test.ts
    frontend/src/lib/components/layout/Grid.browser-test.ts
    frontend/src/lib/components/layout/Heading.browser-test.ts
    frontend/src/lib/components/layout/Text.browser-test.ts
    frontend/src/lib/components/nav/SideNav.browser-test.ts
    frontend/src/lib/components/nav/NavItem.browser-test.ts
    frontend/src/lib/components/nav/NavGroup.browser-test.ts
    frontend/src/lib/components/core/LoadingSkeleton.browser-test.ts
    frontend/src/lib/components/feedback/Spinner.browser-test.ts
    frontend/src/lib/components/feedback/ErrorDisplay.browser-test.ts
    frontend/src/lib/components/core/ConnectionBanner.browser-test.ts
    frontend/src/lib/components/core/FallbackComponent.browser-test.ts
    frontend/src/lib/components/core/NodeRenderer.browser-test.ts
    frontend/src/lib/components/core/Surface.browser-test.ts
  </files>
  <read_first>
    frontend/src/lib/components/nav/SideNav.browser-test.ts
    frontend/src/lib/components/core/NodeRenderer.browser-test.ts
    frontend/src/lib/components/core/Surface.browser-test.ts
    frontend/vitest-browser.config.ts
    .planning/codebase/TESTING.md
  </read_first>
  <action>
Delete existing SideNav.browser-test.ts, NodeRenderer.browser-test.ts, and Surface.browser-test.ts content and rewrite (per D-07). Create 11 new test files (per D-08).

Follow the browser test pattern from TESTING.md. Each test file uses `render` from `vitest-browser-svelte` and `expect.element()` assertions.

**Layout component tests (Container, Grid, Heading, Text) -- 1-2 tests each:**
- Container: renders card variant (check for Card structure), renders plain variant (no card border)
- Grid: renders grid with inline style containing `grid-template-columns` (verifies Pitfall 4 fix)
- Heading: renders correct heading level tag (h1, h2, etc.), applies font-semibold
- Text: renders paragraph text, renders muted variant

**Nav component tests (SideNav, NavItem, NavGroup) -- 2-3 tests each:**
- SideNav: renders nav element with aria-label, renders children
- NavItem: renders button with label text, dispatches action on click, renders icon when provided
- NavGroup: renders group label when provided, renders children

**Core component tests (LoadingSkeleton, FallbackComponent, ConnectionBanner, NodeRenderer, Surface) -- 1-3 tests each:**
- LoadingSkeleton: renders skeleton elements (verify no bg-gray classes), renders correct number of lines
- FallbackComponent: renders component type name in dev mode, uses destructive token classes
- ConnectionBanner: mock isConnected to return false, verify banner text visible
- NodeRenderer: rewrite existing tests targeting current component structure (mock registry)
- Surface: rewrite existing tests targeting current structure (mock surface tree store)

**Feedback component tests (Spinner, ErrorDisplay) -- 1-2 tests each:**
- Spinner: renders SVG or Loader2 icon with animate-spin
- ErrorDisplay: renders error messages when bound data has errors, renders AlertCircle icon

Mock patterns:
```typescript
// For ConnectionBanner
vi.mock('$lib/transport/websocket.svelte', () => ({
  isConnected: vi.fn(() => false),
}));

// For NavItem
vi.mock('$lib/transport/dispatcher', () => ({
  sendAction: vi.fn(),
}));

// For ErrorDisplay
vi.mock('$lib/store/data.svelte', () => ({
  getData: vi.fn(),
}));
```

Run all test files grouped by directory after writing:
```bash
cd frontend && npx vitest run --config vitest-browser.config.ts src/lib/components/layout/ src/lib/components/nav/ src/lib/components/core/ src/lib/components/feedback/
```
  </action>
  <verify>
    <automated>cd frontend && npx vitest run --config vitest-browser.config.ts src/lib/components/layout/ src/lib/components/nav/ src/lib/components/core/ src/lib/components/feedback/ --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - 14 browser test files exist (4 layout + 3 nav + 4 core + 2 feedback + NodeRenderer)
    - Each test file contains at least 1 test() call
    - Grid.browser-test.ts contains assertion for `grid-template-columns` (verifies Pitfall 4 fix)
    - LoadingSkeleton.browser-test.ts verifies no `bg-gray` classes
    - All browser tests pass: exits 0
  </acceptance_criteria>
  <done>All 14 layout/nav/core/feedback components have passing browser tests. Full test coverage for this component group per D-08.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server -> NavItem icon name | Server sends icon names via props.icon for nav items |
| server -> Grid cols prop | Server controls grid column count |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-09 | Injection | NavItem icon prop | mitigate | Icon name resolved via getIcon registry lookup (safe map access). Unknown names return CircleHelp fallback. No dynamic import or eval. |
| T-11-10 | Injection | Grid cols prop | mitigate | cols value used only in inline style `repeat(N, 1fr)` where N is cast to number. Svelte auto-escapes style attribute values. No CSS injection vector. |
| T-11-11 | Information Disclosure | FallbackComponent | accept | Shows component type name and props in dev mode only (guarded by `import.meta.env.DEV`). Not visible in production. |
</threat_model>

<verification>
- Grid uses inline style for columns (Pitfall 4 fixed)
- LoadingSkeleton uses shadcn Skeleton (Pitfall 6 fixed)
- FallbackComponent uses semantic destructive tokens
- ConnectionBanner uses destructive bg per UI-SPEC
- NavItem supports icons via getIcon registry
- No `bg-gray-*`, `text-gray-*`, `border-red-*` raw colors in any modified file
- All 14 browser test files pass
- `npm run build` exits 0
</verification>

<success_criteria>
1. `grep -r "bg-gray\|text-gray\|border-red\|bg-red" frontend/src/lib/components/layout/ frontend/src/lib/components/core/LoadingSkeleton.svelte frontend/src/lib/components/core/FallbackComponent.svelte` returns no matches
2. `grep "grid-template-columns" frontend/src/lib/components/layout/Grid.svelte` returns match
3. All browser tests pass
4. `npm run build` exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/11-leaf-component-migration/11-04-SUMMARY.md`
</output>
