---
phase: 16
plan: 04
type: execute
wave: 3
depends_on: [01, 02, 03]
files_modified:
  - .planning/PROJECT.md
  - .planning/STATE.md
autonomous: true
requirements: [FRAME-02]
tags: [docs, project-meta, decision-log, blocker-close]
threat_model_severity: low
must_haves:
  truths:
    - "PROJECT.md Key Decisions table has a new row naming `linkme` as the registration library with explicit rationale"
    - "STATE.md no longer lists 'Registration library selection' as a blocker; closed with a pointer to Phase 16 Plan 01"
    - "STATE.md Accumulated Context reflects that Phase 16 resolved the inventory-vs-linkme question"
    - "Phase 17 receives an explicit hand-off note about D-C1's 'every `#[gallery_demo]` annotation must use explicit `key = \"...\"`' implication"
  artifacts:
    - path: ".planning/PROJECT.md"
      provides: "Key Decisions row naming linkme + rationale"
      contains: "linkme"
    - path: ".planning/STATE.md"
      provides: "closed 'Registration library selection' blocker; Phase 17 hand-off note"
      contains: "registration library selection"
  key_links:
    - from: ".planning/PROJECT.md"
      to: ".planning/phases/16-framework-hooks/16-CONTEXT.md"
      via: "Key Decisions row cites CONTEXT.md D-A1 as the decision record"
      pattern: "D-A1|CONTEXT.md"
    - from: ".planning/STATE.md"
      to: ".planning/phases/16-framework-hooks/16-01-SUMMARY.md"
      via: "closure note points to the plan that implemented the decision"
      pattern: "16-01"
---

<objective>
Close the documentation loop on Phase 16. FRAME-02 requires the registration-library choice (linkme) to be "recorded in a Key Decisions entry with rationale." STATE.md lists "Registration library selection" as a deferred-to-Phase-16 blocker; now that Phases 16-01/02/03 have shipped, the blocker is resolved. Plan 04 lands both updates and adds a hand-off note warning Phase 17's planner that D-C1's default-from-fn-ident key rule collides at scale if every built-in reuses the `gallery_demo()` fn name (so DEMO-01 must use explicit `key = "..."` overrides).

Purpose: satisfy FRAME-02's "choice logged" clause and ensure the next phase inherits the relevant implementation constraint. No code changes; pure documentation closure.

Output: modified `.planning/PROJECT.md` (one new Key Decisions row); modified `.planning/STATE.md` (close blocker, update accumulated-context, add Phase 17 hand-off note). Covers the documentation clause of FRAME-02.

**Wave 3 rationale:** depends on Plans 01-03 so the docs reflect shipped reality. If any of 01-03 reveal an implementation change (e.g. `linkme` version bump, unexpected `inventory` fallback), this plan's docs remain accurate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/16-framework-hooks/16-CONTEXT.md
@.planning/phases/16-framework-hooks/16-RESEARCH.md
@.planning/phases/16-framework-hooks/16-01-PLAN.md
@.planning/phases/16-framework-hooks/16-02-PLAN.md
@.planning/phases/16-framework-hooks/16-03-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add PROJECT.md Key Decisions row for linkme registration-library choice</name>
  <files>.planning/PROJECT.md</files>
  <read_first>
    - .planning/PROJECT.md (current state — Key Decisions table, lines 110-132)
    - .planning/phases/16-framework-hooks/16-CONTEXT.md §Area A "D-A1: `linkme` backs the distributed-slice registry" (lines 33-35)
    - .planning/phases/16-framework-hooks/16-RESEARCH.md §"Code Examples — PROJECT.md Key Decisions entry (Plan 04 writes this)" (lines 1423-1432)
  </read_first>
  <behavior>
    - PROJECT.md Key Decisions table has exactly one new row naming linkme, with rationale + outcome columns populated.
    - Existing rows are not modified.
    - Row is inserted at the bottom of the table (after the existing "Pure `fn() -> Node` demo contract" row at line 131) to preserve chronological order.
  </behavior>
  <action>
    Read `.planning/PROJECT.md` and locate the Key Decisions table (starts at `## Key Decisions`, line 110). The current final row is line 131:
    ```
    | Pure `fn() -> Node` demo contract | Keeps demo code in the `marionette` crate harmless; composite demos are nested fn calls; stateful fixtures live in the gallery binary, not the framework crate | → v1.2 |
    ```

    Insert the following new row immediately after line 131 (before the closing `---` and `## Evolution` heading on line 134):

    ```
    | linkme over inventory for gallery-demo registry | Type-safe `#[distributed_slice]` attribute binds element type to the static slice declaration; zero runtime cost (no ctor-style global initializers); explicit mental model consistent with marionette's clean-framework posture. Stable-iteration-order promise is owned by marionette (sort at iteration time), not delegated to linkme — linkme's native link-time order is not portable across platforms. Decision record: `.planning/phases/16-framework-hooks/16-CONTEXT.md` D-A1. | ✓ Good — v1.2 Phase 16 |
    ```

    Use the Edit tool (or Read-then-Write) to perform the insertion. Do NOT touch any other line in the file. Preserve trailing-newline discipline.
  </action>
  <verify>
    <automated>grep -q 'linkme over inventory for gallery-demo registry' .planning/PROJECT.md; grep -c '^| ' .planning/PROJECT.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'linkme over inventory for gallery-demo registry' .planning/PROJECT.md` succeeds.
    - `grep -q 'Type-safe .#\[distributed_slice\]. attribute' .planning/PROJECT.md` succeeds.
    - `grep -q 'D-A1' .planning/PROJECT.md` succeeds.
    - `grep -q '→ v1.2 Phase 16' .planning/PROJECT.md` succeeds (or a close variant with Phase 16 reference).
    - `grep -c '^|' .planning/PROJECT.md` shows the table grew by exactly 1 row (original count + 1).
    - The final row before `---` at line ~134 is the new linkme row.
    - No other rows have been modified (verify by reading before/after diff).
  </acceptance_criteria>
  <done>PROJECT.md Key Decisions table has the linkme row with rationale; FRAME-02's "choice logged in Key Decisions" clause is satisfied.</done>
</task>

<task type="auto">
  <name>Task 2: Close STATE.md "Registration library selection" blocker + add Phase 17 hand-off note</name>
  <files>.planning/STATE.md</files>
  <read_first>
    - .planning/STATE.md (current state — full file, focus on Blockers/Concerns section lines 76-81)
    - .planning/phases/16-framework-hooks/16-CONTEXT.md §Area C "D-C1" (lines 48-49) — the default-ident vs explicit-`key` tension
    - .planning/phases/16-framework-hooks/16-CONTEXT.md §specifics "Every Phase 17 annotation will use `key = \"…\"` explicitly" (line 180)
    - .planning/phases/16-framework-hooks/16-RESEARCH.md §"Recommended Plan Decomposition — Plan 04" (lines 1359-1368)
  </read_first>
  <behavior>
    - STATE.md `Blockers/Concerns` section no longer lists "Registration library selection".
    - STATE.md `Accumulated Context` or `Session Continuity` section reflects the Phase 16 shipment.
    - A Phase 17 hand-off note is captured, either inline in STATE.md or in a companion file referenced from STATE.md.
  </behavior>
  <action>
    Read `.planning/STATE.md` and make these targeted edits:

    **Edit 1 — `Blockers/Concerns` section (around lines 76-81):**

    Current text contains:
    ```
    - **Registration library selection** — `inventory` vs `linkme` decision deferred to Phase 16 scoping. `inventory` is widely used; `linkme` gives more explicit control. Pick during `/gsd-plan-phase 16`.
    ```

    Replace with:
    ```
    - ✅ **Registration library selection (resolved 2026-04-21):** `linkme` chosen over `inventory` per Phase 16 CONTEXT.md D-A1 — type-safe `#[distributed_slice]`, zero runtime cost, explicit mental model. Logged in PROJECT.md Key Decisions. See `.planning/phases/16-framework-hooks/16-01-PLAN.md` for the implementation; stable iteration order is owned by `marionette::gallery::registered_demos()` via sort-at-iteration-time, not delegated to linkme.
    ```

    **Edit 2 — append a Phase 17 hand-off note to the `Accumulated Context` section.** After the final bullet in `Decisions` (the `Phase 17 bundles gallery-crate skeleton...` bullet on line 67), add a new paragraph or sub-section titled `### Phase 17 hand-off (from Phase 16)`:

    ```markdown
    ### Phase 17 hand-off (from Phase 16)

    Phase 16 ships the `#[gallery_demo]` macro with a D-C1 convention: when no `key = "..."` arg is provided, the registry key defaults to the annotated fn's ident. Phase 17's DEMO-01 convention names every built-in's demo fn `gallery_demo()` (pure-fn sibling in each builder file), which would mass-collide at runtime since every default-derived key becomes `"gallery_demo"`. **Phase 17 planners must therefore use explicit `key = "..."` overrides on every `#[gallery_demo]` annotation in `backend/crates/marionette/src/builders/`.** The natural choice is to match each builder's `#[component(type = "…")]` string (e.g. `#[gallery_demo(key = "button")]` on Button, `#[gallery_demo(key = "text-input")]` on TextInput). Phase 18/19 catalog + exerciser screens use distinct fn idents and may skip the override.

    Cross-reference: `.planning/phases/16-framework-hooks/16-CONTEXT.md` §D-C1 (lines 48-49), §specifics "Every Phase 17 annotation will use `key = \"…\"` explicitly" (line 180).
    ```

    **Edit 3 — update `Session Continuity` section (lines 84-87):**

    Update `Stopped at:` and `Resume:` to reflect Phase 16 shipment:

    ```markdown
    Last session: <use the actual current date from env, e.g. 2026-04-21T...>
    Stopped at: Phase 16 shipped — framework hooks complete; awaiting `/gsd-verify-work 16` or `/gsd-plan-phase 17`
    Resume: run `/gsd-verify-work 16` to validate Phase 16 success criteria, then `/gsd-plan-phase 17` to start the gallery-crate skeleton + colocated built-in demos
    ```

    Use the Edit tool for surgical changes — the rest of STATE.md must be preserved as-is.
  </action>
  <verify>
    <automated>grep -qi 'Registration library selection (resolved' .planning/STATE.md; grep -q 'Phase 17 hand-off' .planning/STATE.md; grep -qv 'Pick during /gsd-plan-phase 16' .planning/STATE.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -qi 'Registration library selection (resolved' .planning/STATE.md` succeeds.
    - `grep -qi 'linkme chosen over' .planning/STATE.md` succeeds.
    - `grep -q 'Phase 17 hand-off' .planning/STATE.md` succeeds.
    - `grep -q 'key = \"button\"' .planning/STATE.md` succeeds (proves the explicit-key convention was captured).
    - `grep -qi 'inventory vs linkme decision deferred' .planning/STATE.md` should FAIL (old text removed). Invert: `! grep -qi 'inventory vs linkme decision deferred' .planning/STATE.md`.
    - `grep -q '16-01-PLAN.md' .planning/STATE.md` succeeds (pointer to Plan 01 for implementation detail).
    - `grep -qi 'Phase 16 shipped' .planning/STATE.md` succeeds OR `grep -qi 'framework hooks complete' .planning/STATE.md` succeeds.
    - Line count of STATE.md increases by 8-15 lines (hand-off note adds ~5 lines + blocker rewrite ~2 lines + session-continuity rewrite ~1 line).
  </acceptance_criteria>
  <done>STATE.md's "Registration library selection" blocker is marked resolved with a pointer to the implementing plan. Phase 17 hand-off note is captured so the next phase's planner sees the `key = "..."` convention warning without re-reading CONTEXT.md. Session continuity reflects Phase 16 completion.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| documentation integrity | edits to `.planning/PROJECT.md` and `.planning/STATE.md` are load-bearing metadata that downstream agents read |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-04-01 | Tampering | doc edits clobber unrelated rows / sections | mitigate | Acceptance criteria include a `grep -c '^|' .planning/PROJECT.md` check that the Key Decisions table grew by exactly 1 row. Every edit uses the Edit tool with targeted find-and-replace, not wholesale file rewrites. The Read-before-edit protocol ensures current state is inspected first. |
| T-16-04-02 | Repudiation | decision log row lacks a decision-record pointer | mitigate | The new PROJECT.md row cites `CONTEXT.md D-A1` as the decision record. STATE.md blocker closure cites `16-01-PLAN.md` as the implementing plan. Both are machine-greppable for traceability. |
| T-16-04-03 | Information disclosure | docs leak internal paths or secrets | accept | Both files are already version-controlled under `.planning/` and are visible to all contributors. No secrets, no credentials, no PII — only design rationale and status. |

**No high-severity threats.** Pure documentation work.
</threat_model>

<verification>
After both tasks complete:

1. `grep -q 'linkme over inventory for gallery-demo registry' .planning/PROJECT.md` — Key Decisions row landed.
2. `grep -qi 'Registration library selection (resolved' .planning/STATE.md` — blocker closed.
3. `grep -q 'Phase 17 hand-off' .planning/STATE.md` — hand-off note captured.
4. Read both files end-to-end to confirm no unrelated content was modified.
</verification>

<success_criteria>
- PROJECT.md Key Decisions row for linkme exists with rationale (FRAME-02 "choice logged").
- STATE.md blocker "Registration library selection" is closed with a pointer to 16-01-PLAN.md.
- Phase 17 hand-off note in STATE.md captures the D-C1 explicit-`key` convention.
- Session Continuity reflects Phase 16 shipment.
- No unrelated file modifications.
</success_criteria>

<output>
After completion, create `.planning/phases/16-framework-hooks/16-04-SUMMARY.md`.
</output>
