---
phase: 10-foundation
plan: 03
type: execute
wave: 3
depends_on: [10-02]
files_modified: []
autonomous: false
requirements: [FOUND-01, FOUND-02, FOUND-03]
must_haves:
  truths:
    - "Visual rendering of stubbed components looks reasonable in browser"
    - "Navigation between sidebar items works"
    - "Form fields accept input and display errors"
    - "Modal overlay appears and can be dismissed"
  artifacts: []
  key_links: []
---

<objective>
Visual verification that the stubbed components render correctly in the browser.

Purpose: Stubs are minimal HTML+Tailwind. While the build passes, human eyes need to confirm that components are visually usable (not broken layouts, missing text, invisible elements).
Output: Human approval that Phase 10 foundation is visually acceptable
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-foundation/10-CONTEXT.md
@.planning/phases/10-foundation/10-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Start the full stack (backend + frontend) for visual testing</name>
  <files></files>
  <read_first>
    .planning/phases/10-foundation/10-02-SUMMARY.md
  </read_first>
  <action>
Start the dev environment so the user can visually test the stubbed components:

1. Check if the backend is already running, if not start it: `cd backend && cargo run --bin crm-demo &`
2. Start the frontend dev server: `cd frontend && npm run dev`
3. Report the URL to the user (typically http://localhost:5173)
  </action>
  <verify>
    <automated>curl -s -o /dev/null -w "%{http_code}" http://localhost:5173 2>/dev/null || echo "Dev server not running yet"</automated>
  </verify>
  <acceptance_criteria>
    - Dev server responds at http://localhost:5173 with HTTP 200
  </acceptance_criteria>
  <done>Dev server running, ready for visual verification</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of stubbed components</name>
  <action>
Checkpoint: User visually verifies that the Flowbite-to-stub migration produces functional, visible components.

What was built: Complete Flowbite-to-stub migration -- shadcn-svelte initialized, app.css rewritten with OKLCH Zinc tokens, all 17 Flowbite components replaced with HTML+Tailwind stubs, all icons replaced with lucide-svelte, Flowbite packages removed.

How to verify:
1. Open http://localhost:5173 in your browser
2. Verify the login screen renders (Container with card styling, text inputs, button)
3. Log in and verify:
   - Sidebar renders with navigation items
   - Clicking nav items navigates between screens
   - Data tables display with column headers and rows
   - Forms display with labeled inputs
   - Buttons are visible and clickable
4. Check that nothing is invisible, overlapping, or broken
5. If modals are testable (e.g., delete action), verify overlay appears and dismiss works

Note: Stubs are intentionally minimal. They won't look as polished as Flowbite originals. The key check is: does everything FUNCTION and is everything VISIBLE? Phase 11 will make them look good with proper shadcn-svelte primitives.
  </action>
  <verify>User confirms visual and functional correctness</verify>
  <done>User approves or describes issues to fix</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | This plan is verification-only, no code changes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| N/A | N/A | N/A | N/A | No code changes in this plan |
</threat_model>

<verification>
Human visual verification of rendered components in browser.
</verification>

<success_criteria>
- User confirms stubbed components render visibly in the browser
- User confirms navigation, form input, and basic interactions work
- No critical rendering issues identified
</success_criteria>

<output>
After completion, create `.planning/phases/10-foundation/10-03-SUMMARY.md`
</output>
