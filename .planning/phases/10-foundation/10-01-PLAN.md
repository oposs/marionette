---
phase: 10-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/components.json
  - frontend/src/lib/utils.ts
  - frontend/src/app.css
  - frontend/src/lib/components/core/Surface.svelte
  - frontend/package.json
autonomous: true
requirements: [FOUND-01, FOUND-02]
must_haves:
  truths:
    - "shadcn-svelte init artifacts exist (components.json, utils.ts with cn())"
    - "app.css uses OKLCH semantic color tokens with @theme inline block"
    - "app.css has zero Flowbite references (no @plugin flowbite, no @source flowbite)"
    - "Surface.svelte uses semantic tokens instead of raw gray colors"
    - "Frontend compiles without errors after CSS rewrite"
  artifacts:
    - path: "frontend/components.json"
      provides: "shadcn-svelte CLI configuration"
      contains: "shadcn"
    - path: "frontend/src/lib/utils.ts"
      provides: "cn() class merge helper"
      exports: ["cn"]
    - path: "frontend/src/app.css"
      provides: "OKLCH semantic color tokens and shadcn theme system"
      contains: "oklch"
  key_links:
    - from: "frontend/src/lib/utils.ts"
      to: "clsx, tailwind-merge"
      via: "import"
      pattern: "import.*clsx|import.*tailwind-merge"
    - from: "frontend/src/app.css"
      to: "tw-animate-css"
      via: "@import"
      pattern: "@import 'tw-animate-css'"
---

<objective>
Initialize shadcn-svelte infrastructure and rewrite the CSS theme system from Flowbite to OKLCH semantic tokens.

Purpose: Establishes the CSS foundation that all subsequent stub components will use. Without this, stubs cannot reference semantic token classes like `bg-primary`, `bg-background`, etc.
Output: components.json, utils.ts with cn(), OKLCH-themed app.css, semantic-token Surface.svelte
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-foundation/10-CONTEXT.md
@.planning/phases/10-foundation/10-RESEARCH.md
@.planning/phases/10-foundation/10-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run shadcn-svelte init and rewrite app.css with OKLCH Zinc theme</name>
  <files>
    frontend/components.json
    frontend/src/lib/utils.ts
    frontend/src/app.css
    frontend/package.json
  </files>
  <read_first>
    frontend/src/app.css
    frontend/package.json
    .planning/phases/10-foundation/10-RESEARCH.md
  </read_first>
  <action>
Step 1: Back up the grid safelist line from current app.css (it contains `@source inline("grid-cols-1 grid-cols-2 ...")`).

Step 2: Run `npx shadcn-svelte@latest init` in the `frontend/` directory. When prompted:
- Base style: Default (per D-01)
- Base color: Zinc (per D-04)
- CSS file: src/app.css
- Import aliases: keep defaults ($lib, $lib/components, $lib/utils, $lib/components/ui, $lib/hooks)

This will create `components.json`, `$lib/utils.ts` (with `cn()` helper), and overwrite `app.css`.

Step 3: After init completes, replace the generated `app.css` with the exact content below. This is the complete file -- do not merge, replace entirely:

```css
@import 'tailwindcss';
@import 'tw-animate-css';

@custom-variant dark (&:is(.dark *));

/* Safelist dynamic grid-cols classes used by Grid.svelte */
@source inline("grid-cols-1 grid-cols-2 grid-cols-3 grid-cols-4 grid-cols-5 grid-cols-6 md:grid-cols-1 md:grid-cols-2 md:grid-cols-3 md:grid-cols-4 md:grid-cols-5 md:grid-cols-6");

:root {
  --radius: 0.25rem;
  --background: oklch(1 0 0);
  --foreground: oklch(0.141 0.005 285.823);
  --card: oklch(1 0 0);
  --card-foreground: oklch(0.141 0.005 285.823);
  --popover: oklch(1 0 0);
  --popover-foreground: oklch(0.141 0.005 285.823);
  --primary: oklch(0.21 0.006 285.885);
  --primary-foreground: oklch(0.985 0 0);
  --secondary: oklch(0.967 0.001 286.375);
  --secondary-foreground: oklch(0.21 0.006 285.885);
  --muted: oklch(0.967 0.001 286.375);
  --muted-foreground: oklch(0.552 0.016 285.938);
  --accent: oklch(0.967 0.001 286.375);
  --accent-foreground: oklch(0.21 0.006 285.885);
  --destructive: oklch(0.577 0.245 27.325);
  --border: oklch(0.92 0.004 286.32);
  --input: oklch(0.92 0.004 286.32);
  --ring: oklch(0.705 0.015 286.067);
  --sidebar-background: oklch(0.985 0 0);
  --sidebar-foreground: oklch(0.141 0.005 285.823);
  --sidebar-primary: oklch(0.21 0.006 285.885);
  --sidebar-primary-foreground: oklch(0.985 0 0);
  --sidebar-accent: oklch(0.967 0.001 286.375);
  --sidebar-accent-foreground: oklch(0.21 0.006 285.885);
  --sidebar-border: oklch(0.92 0.004 286.32);
  --sidebar-ring: oklch(0.705 0.015 286.067);
}

.dark {
  --background: oklch(0.141 0.005 285.823);
  --foreground: oklch(0.985 0 0);
  --card: oklch(0.21 0.006 285.885);
  --card-foreground: oklch(0.985 0 0);
  --popover: oklch(0.21 0.006 285.885);
  --popover-foreground: oklch(0.985 0 0);
  --primary: oklch(0.92 0.004 286.32);
  --primary-foreground: oklch(0.21 0.006 285.885);
  --secondary: oklch(0.274 0.006 286.033);
  --secondary-foreground: oklch(0.985 0 0);
  --muted: oklch(0.274 0.006 286.033);
  --muted-foreground: oklch(0.705 0.015 286.067);
  --accent: oklch(0.274 0.006 286.033);
  --accent-foreground: oklch(0.985 0 0);
  --destructive: oklch(0.704 0.191 22.216);
  --border: oklch(1 0 0 / 10%);
  --input: oklch(1 0 0 / 15%);
  --ring: oklch(0.552 0.016 285.938);
  --sidebar-background: oklch(0.21 0.006 285.885);
  --sidebar-foreground: oklch(0.985 0 0);
  --sidebar-primary: oklch(0.985 0 0);
  --sidebar-primary-foreground: oklch(0.21 0.006 285.885);
  --sidebar-accent: oklch(0.274 0.006 286.033);
  --sidebar-accent-foreground: oklch(0.985 0 0);
  --sidebar-border: oklch(1 0 0 / 10%);
  --sidebar-ring: oklch(0.552 0.016 285.938);
}

@theme inline {
  --color-background: var(--background);
  --color-foreground: var(--foreground);
  --color-card: var(--card);
  --color-card-foreground: var(--card-foreground);
  --color-popover: var(--popover);
  --color-popover-foreground: var(--popover-foreground);
  --color-primary: var(--primary);
  --color-primary-foreground: var(--primary-foreground);
  --color-secondary: var(--secondary);
  --color-secondary-foreground: var(--secondary-foreground);
  --color-muted: var(--muted);
  --color-muted-foreground: var(--muted-foreground);
  --color-accent: var(--accent);
  --color-accent-foreground: var(--accent-foreground);
  --color-destructive: var(--destructive);
  --color-border: var(--border);
  --color-input: var(--input);
  --color-ring: var(--ring);
  --color-sidebar-background: var(--sidebar-background);
  --color-sidebar-foreground: var(--sidebar-foreground);
  --color-sidebar-primary: var(--sidebar-primary);
  --color-sidebar-primary-foreground: var(--sidebar-primary-foreground);
  --color-sidebar-accent: var(--sidebar-accent);
  --color-sidebar-accent-foreground: var(--sidebar-accent-foreground);
  --color-sidebar-border: var(--sidebar-border);
  --color-sidebar-ring: var(--sidebar-ring);
  --radius-sm: calc(var(--radius) - 0.125rem);
  --radius-md: var(--radius);
  --radius-lg: calc(var(--radius) + 0.125rem);
  --radius-xl: calc(var(--radius) + 0.25rem);
}
```

Key points:
- `--radius: 0.25rem` per D-02
- `@custom-variant dark (&:is(.dark *))` is shadcn's approach, replaces Flowbite's `(&:where(.dark, .dark *))` per D-07
- Dark mode tokens included per D-05 (defining costs nothing, toggle wiring deferred)
- Grid safelist `@source inline(...)` preserved from original app.css
- NO `@plugin "flowbite/plugin"`, NO `@source` Flowbite directives
- `@import 'tw-animate-css'` required by shadcn-svelte theming

Step 4: Verify `frontend/src/lib/utils.ts` was created by init and contains:
```typescript
import { type ClassValue, clsx } from 'clsx';
import { twMerge } from 'tailwind-merge';

export function cn(...inputs: ClassValue[]) {
	return twMerge(clsx(...inputs));
}
```
If it was not created or differs, write it with the above content.

Step 5: Verify `frontend/components.json` exists. If shadcn init created it, leave as-is. If missing, create it with:
```json
{
  "$schema": "https://shadcn-svelte.com/schema.json",
  "tailwind": {
    "css": "src/app.css",
    "baseColor": "zinc"
  },
  "aliases": {
    "lib": "$lib",
    "components": "$lib/components",
    "utils": "$lib/utils",
    "ui": "$lib/components/ui",
    "hooks": "$lib/hooks"
  },
  "typescript": true
}
```
  </action>
  <verify>
    <automated>cd /home/oetiker/checkouts/marionette/frontend && test -f components.json && test -f src/lib/utils.ts && grep -q 'oklch' src/app.css && ! grep -q 'flowbite' src/app.css && grep -q '@theme inline' src/app.css && grep -q 'grid-cols-1' src/app.css && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - frontend/components.json exists and contains "shadcn"
    - frontend/src/lib/utils.ts exists and contains "export function cn("
    - frontend/src/app.css contains "oklch" (at least 20 occurrences)
    - frontend/src/app.css does NOT contain "flowbite"
    - frontend/src/app.css contains "@theme inline"
    - frontend/src/app.css contains "@source inline" (grid safelist preserved)
    - frontend/src/app.css contains "--radius: 0.25rem"
    - frontend/src/app.css contains "@custom-variant dark (&:is(.dark *))"
    - frontend/src/app.css contains "@import 'tw-animate-css'"
    - frontend/package.json contains "bits-ui" in dependencies (added by shadcn init)
    - frontend/package.json contains "clsx" in dependencies
    - frontend/package.json contains "tailwind-merge" in dependencies
  </acceptance_criteria>
  <done>shadcn-svelte init artifacts exist, app.css has OKLCH Zinc theme with all semantic tokens, zero Flowbite CSS references, grid safelist preserved</done>
</task>

<task type="auto">
  <name>Task 2: Update Surface.svelte hardcoded colors to semantic tokens</name>
  <files>
    frontend/src/lib/components/core/Surface.svelte
  </files>
  <read_first>
    frontend/src/lib/components/core/Surface.svelte
  </read_first>
  <action>
In Surface.svelte, update the `layoutClasses` record to replace raw Tailwind color classes with semantic tokens. Change lines 14-19 from:

```typescript
const layoutClasses: Record&lt;string, string&gt; = {
    main: 'bg-white p-4 md:p-6 overflow-hidden flex-1 min-w-0 flex flex-col',
    sidebar: 'bg-gray-50 border-r border-gray-200 p-4 overflow-y-auto w-64 shrink-0 h-full',
    modal: '',
    toast: '',
};
```

To:

```typescript
const layoutClasses: Record&lt;string, string&gt; = {
    main: 'bg-background p-4 md:p-6 overflow-hidden flex-1 min-w-0 flex flex-col',
    sidebar: 'bg-sidebar-background border-r border-sidebar-border p-4 overflow-y-auto w-64 shrink-0 h-full',
    modal: '',
    toast: '',
};
```

Changes:
- `bg-white` -> `bg-background` (responds to dark mode)
- `bg-gray-50` -> `bg-sidebar-background` (uses sidebar-specific token)
- `border-gray-200` -> `border-sidebar-border` (uses sidebar-specific token)

No other changes to Surface.svelte. The component has no Flowbite imports.
  </action>
  <verify>
    <automated>cd /home/oetiker/checkouts/marionette && grep -q 'bg-background' frontend/src/lib/components/core/Surface.svelte && grep -q 'bg-sidebar-background' frontend/src/lib/components/core/Surface.svelte && grep -q 'border-sidebar-border' frontend/src/lib/components/core/Surface.svelte && ! grep -q 'bg-white' frontend/src/lib/components/core/Surface.svelte && ! grep -q 'bg-gray-50' frontend/src/lib/components/core/Surface.svelte && ! grep -q 'border-gray-200' frontend/src/lib/components/core/Surface.svelte && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - Surface.svelte contains "bg-background" (not "bg-white")
    - Surface.svelte contains "bg-sidebar-background" (not "bg-gray-50")
    - Surface.svelte contains "border-sidebar-border" (not "border-gray-200")
    - Surface.svelte does NOT contain "bg-white", "bg-gray-50", or "border-gray-200"
    - Surface.svelte still contains "overflow-hidden flex-1 min-w-0 flex flex-col" (layout unchanged)
  </acceptance_criteria>
  <done>Surface.svelte uses semantic color tokens, will respond to dark mode theme switching</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry -> project | shadcn-svelte CLI fetches packages from npm; supply chain risk |
| CSS theme -> UI rendering | Semantic tokens control all visual output |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Tampering | npm packages | accept | Standard npm install with lockfile; packages are well-known (bits-ui, clsx, tailwind-merge). Verified versions in RESEARCH.md. |
| T-10-02 | Information Disclosure | CSS tokens | accept | No secrets in CSS; all tokens are visual styling values only |
</threat_model>

<verification>
1. `test -f frontend/components.json` -- shadcn config exists
2. `grep -c 'export function cn' frontend/src/lib/utils.ts` -- cn() helper exists
3. `grep -c 'oklch' frontend/src/app.css` -- OKLCH tokens present (expect 40+)
4. `! grep -q 'flowbite' frontend/src/app.css` -- no Flowbite references in CSS
5. `grep -q 'bg-sidebar-background' frontend/src/lib/components/core/Surface.svelte` -- semantic tokens in Surface
</verification>

<success_criteria>
- components.json and utils.ts exist with correct content
- app.css has complete OKLCH Zinc theme (light + dark) with @theme inline block
- Zero Flowbite references in app.css
- Surface.svelte uses semantic tokens
- Note: build will NOT pass yet because component files still import from flowbite-svelte (Plan 02 fixes that)
</success_criteria>

<output>
After completion, create `.planning/phases/10-foundation/10-01-SUMMARY.md`
</output>
