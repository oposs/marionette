---
phase: 10-foundation
plan: 02
type: execute
wave: 2
depends_on: [10-01]
files_modified:
  - frontend/src/lib/components/form/Button.svelte
  - frontend/src/lib/components/form/TextInput.svelte
  - frontend/src/lib/components/form/SelectInput.svelte
  - frontend/src/lib/components/form/Checkbox.svelte
  - frontend/src/lib/components/form/Form.svelte
  - frontend/src/lib/components/nav/SideNav.svelte
  - frontend/src/lib/components/nav/NavItem.svelte
  - frontend/src/lib/components/nav/NavGroup.svelte
  - frontend/src/lib/components/layout/Container.svelte
  - frontend/src/lib/components/feedback/ErrorDisplay.svelte
  - frontend/src/lib/components/feedback/Spinner.svelte
  - frontend/src/lib/components/core/ConnectionBanner.svelte
  - frontend/src/lib/components/core/ErrorBoundary.svelte
  - frontend/src/lib/components/popup/ModalSurface.svelte
  - frontend/src/lib/components/popup/ToastSurface.svelte
  - frontend/src/lib/components/popup/ConfirmDialog.svelte
  - frontend/src/lib/components/table/DataTable.svelte
  - frontend/src/routes/+layout.svelte
  - frontend/package.json
autonomous: true
requirements: [FOUND-03]
must_haves:
  truths:
    - "Zero Flowbite imports exist in any source file under frontend/src/"
    - "Zero Flowbite packages remain in frontend/package.json"
    - "All 17 components with Flowbite imports have been stubbed with HTML+Tailwind"
    - "All stubs preserve the SDUI interface contract (surface, props, bind, action)"
    - "Frontend compiles and builds without errors"
    - "Existing tests pass"
  artifacts:
    - path: "frontend/src/lib/components/form/Button.svelte"
      provides: "Button stub with primary/outline/destructive variants"
      contains: "bg-primary"
    - path: "frontend/src/lib/components/form/TextInput.svelte"
      provides: "TextInput stub with data binding and error display"
      contains: "border-input"
    - path: "frontend/src/lib/components/popup/ModalSurface.svelte"
      provides: "Modal stub with overlay and close behavior"
      contains: "bg-black/50"
    - path: "frontend/src/routes/+layout.svelte"
      provides: "Layout with lucide icons replacing flowbite-svelte-icons"
      contains: "@lucide/svelte"
  key_links:
    - from: "frontend/src/lib/components/form/Button.svelte"
      to: "$lib/transport/dispatcher"
      via: "sendAction on click"
      pattern: "sendAction"
    - from: "frontend/src/lib/components/form/TextInput.svelte"
      to: "$lib/store/data.svelte"
      via: "getData/setData for two-way binding"
      pattern: "getData.*setData"
    - from: "frontend/src/lib/components/popup/ModalSurface.svelte"
      to: "$lib/store/surfaces.svelte"
      via: "getSurfaceTree('modal') for open state"
      pattern: "getSurfaceTree"
---

<objective>
Replace all Flowbite component imports with minimal HTML+Tailwind stubs, replace Flowbite icons with lucide, and remove Flowbite packages from package.json.

Purpose: Completes the Flowbite removal. After this plan, zero Flowbite references exist anywhere in the codebase. The app builds and renders with semantic tokens from Plan 01.
Output: 17 stubbed component files, updated layout, clean package.json, passing build
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-foundation/10-CONTEXT.md
@.planning/phases/10-foundation/10-RESEARCH.md
@.planning/phases/10-foundation/10-UI-SPEC.md
@.planning/phases/10-foundation/10-01-SUMMARY.md

<interfaces>
<!-- SDUI interface contract -- every component MUST accept these props -->
<!-- From existing component pattern (all 17 files follow this): -->
```typescript
let {
    props = {},
    bind,
    action,
    surface,
    children,
}: {
    props: Record<string, unknown>;
    bind?: string;
    action?: ComponentAction;
    surface: string;
    children?: Snippet;
} = $props();
```

<!-- Data store API (from $lib/store/data.svelte): -->
```typescript
export function getData(surface: string, path: string): unknown;
export function setData(surface: string, path: string, value: unknown): void;
export function getAllData(surface: string): Record<string, unknown> | undefined;
```

<!-- Dirty tracking API (from $lib/store/dirty.svelte): -->
```typescript
export function markDirty(field: string): void;
export function clearDirty(field: string, applyCb: (op: {path: string; value: unknown}) => void): void;
```

<!-- Transport API (from $lib/transport/dispatcher): -->
```typescript
export function sendAction(name: string, payload?: Record<string, unknown>, target?: string, optimistic?: { patch: PatchOperation[] }): void;
```

<!-- Sidebar store (from $lib/store/sidebar.svelte): -->
```typescript
export function isSidebarOpen(): boolean;
export function toggleSidebar(): void;
export function closeSidebar(): void;
```

<!-- Toast store (from $lib/store/toasts.svelte): -->
```typescript
export function getToasts(): Array<{id: string; message: string; severity: string}>;
export function removeToast(id: string): void;
```

<!-- Lucide icon import pattern: -->
```typescript
import IconName from '@lucide/svelte/icons/icon-name';
// Usage: <IconName class="size-4" />
```

<!-- Icon mapping (Flowbite -> Lucide): -->
// BarsOutline -> import Menu from '@lucide/svelte/icons/menu'
// CloseOutline -> import X from '@lucide/svelte/icons/x'
// ExclamationCircleOutline -> import AlertCircle from '@lucide/svelte/icons/alert-circle'
// ChevronUpOutline -> import ChevronUp from '@lucide/svelte/icons/chevron-up'
// ChevronDownOutline -> import ChevronDown from '@lucide/svelte/icons/chevron-down'
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Stub all 17 Flowbite component files with HTML+Tailwind and replace icons with lucide</name>
  <files>
    frontend/src/lib/components/form/Button.svelte
    frontend/src/lib/components/form/TextInput.svelte
    frontend/src/lib/components/form/SelectInput.svelte
    frontend/src/lib/components/form/Checkbox.svelte
    frontend/src/lib/components/form/Form.svelte
    frontend/src/lib/components/nav/SideNav.svelte
    frontend/src/lib/components/nav/NavItem.svelte
    frontend/src/lib/components/nav/NavGroup.svelte
    frontend/src/lib/components/layout/Container.svelte
    frontend/src/lib/components/feedback/ErrorDisplay.svelte
    frontend/src/lib/components/feedback/Spinner.svelte
    frontend/src/lib/components/core/ConnectionBanner.svelte
    frontend/src/lib/components/core/ErrorBoundary.svelte
    frontend/src/lib/components/popup/ModalSurface.svelte
    frontend/src/lib/components/popup/ToastSurface.svelte
    frontend/src/lib/components/popup/ConfirmDialog.svelte
    frontend/src/lib/components/table/DataTable.svelte
    frontend/src/routes/+layout.svelte
  </files>
  <read_first>
    frontend/src/lib/components/form/Button.svelte
    frontend/src/lib/components/form/TextInput.svelte
    frontend/src/lib/components/form/SelectInput.svelte
    frontend/src/lib/components/form/Checkbox.svelte
    frontend/src/lib/components/form/Form.svelte
    frontend/src/lib/components/nav/SideNav.svelte
    frontend/src/lib/components/nav/NavItem.svelte
    frontend/src/lib/components/nav/NavGroup.svelte
    frontend/src/lib/components/layout/Container.svelte
    frontend/src/lib/components/feedback/ErrorDisplay.svelte
    frontend/src/lib/components/feedback/Spinner.svelte
    frontend/src/lib/components/core/ConnectionBanner.svelte
    frontend/src/lib/components/core/ErrorBoundary.svelte
    frontend/src/lib/components/popup/ModalSurface.svelte
    frontend/src/lib/components/popup/ToastSurface.svelte
    frontend/src/lib/components/popup/ConfirmDialog.svelte
    frontend/src/lib/components/table/DataTable.svelte
    frontend/src/routes/+layout.svelte
    .planning/phases/10-foundation/10-RESEARCH.md
    .planning/phases/10-foundation/10-UI-SPEC.md
  </read_first>
  <action>
Replace every Flowbite import with a minimal HTML+Tailwind stub. Each stub MUST:
1. Remove ALL `flowbite-svelte` and `flowbite-svelte-icons` imports
2. Keep ALL business logic (event handlers, data binding, dirty tracking, action dispatch) IDENTICAL
3. Use semantic token classes (bg-primary, bg-background, etc.) not raw Tailwind colors
4. Accept the same $props() interface (surface, props, bind, action, children)

Replace each file as follows. The script sections preserve ALL existing business logic -- only the imports and template markup change.

- - -

**Button.svelte** -- Remove `FlowbiteButton` and `ButtonProps` imports from `flowbite-svelte`. Keep entire script (handleClick, sendAction, getAllData). Replace template:
```svelte
<button
	type="button"
	class="inline-flex items-center justify-center rounded-md text-sm font-medium h-10 px-4 py-2 w-full md:w-auto disabled:opacity-50 disabled:pointer-events-none {colorClass}"
	disabled={props.disabled as boolean}
	onclick={handleClick}
>
	{props.label ?? ''}
</button>
```
Add computed `colorClass`:
```typescript
let colorClass = $derived(
	(props.color as string) === 'red'
		? 'bg-destructive text-destructive-foreground hover:bg-destructive/90'
		: (props.outline as boolean)
			? 'border border-input bg-background hover:bg-accent hover:text-accent-foreground'
			: 'bg-primary text-primary-foreground hover:bg-primary/90'
);
```

- - -

**TextInput.svelte** -- Remove `Input, Label, Helper` imports from `flowbite-svelte`. Keep entire script (handleInput, handleFocus, handleBlur, value/fieldError deriveds). Replace template:
```svelte
<div class="w-full">
	{#if props.label}
		<label class="mb-2 block text-sm font-medium text-foreground">
			{props.label}
		</label>
	{/if}
	<input
		type={(props.type as string) ?? 'text'}
		placeholder={props.placeholder as string}
		required={props.required as boolean}
		disabled={props.disabled as boolean}
		{value}
		oninput={handleInput}
		onfocus={handleFocus}
		onblur={handleBlur}
		class="flex h-10 w-full rounded-md border border-input bg-background px-3 py-2 text-sm ring-offset-background placeholder:text-muted-foreground focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring focus-visible:ring-offset-2 disabled:cursor-not-allowed disabled:opacity-50 {fieldError ? 'border-destructive' : ''}"
	/>
	{#if fieldError}
		<p class="mt-1 text-sm text-destructive">{fieldError}</p>
	{:else if props.helperText}
		<p class="mt-1 text-sm text-muted-foreground">{props.helperText}</p>
	{/if}
</div>
```

- - -

**SelectInput.svelte** -- Remove `Select, Label` imports from `flowbite-svelte`. Keep entire script (handleChange, handleFocus, handleBlur, value/options deriveds). Replace template:
```svelte
<div class="w-full">
	{#if props.label}
		<label class="mb-2 block text-sm font-medium text-foreground">
			{props.label}
		</label>
	{/if}
	<select
		disabled={props.disabled as boolean}
		onchange={handleChange}
		onfocus={handleFocus}
		onblur={handleBlur}
		class="flex h-10 w-full rounded-md border border-input bg-background px-3 py-2 text-sm ring-offset-background focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring focus-visible:ring-offset-2 disabled:cursor-not-allowed disabled:opacity-50"
	>
		{#if props.placeholder}
			<option value="" disabled selected={!value}>{props.placeholder}</option>
		{/if}
		{#each options as opt (opt.value)}
			<option value={opt.value} selected={opt.value === value}>{opt.label}</option>
		{/each}
	</select>
</div>
```

- - -

**Checkbox.svelte** -- Remove `Checkbox as FlowbiteCheckbox` import from `flowbite-svelte`. Keep entire script (handleChange, checked derived). Replace template:
```svelte
<label class="inline-flex items-center gap-2 text-sm font-medium text-foreground">
	<input
		type="checkbox"
		checked={checked}
		disabled={props.disabled as boolean}
		onchange={handleChange}
		class="size-4 rounded border-input text-primary focus:ring-2 focus:ring-ring"
	/>
	{#if props.label}{props.label}{/if}
</label>
```

- - -

**Form.svelte** -- Remove `Helper` import from `flowbite-svelte`. Keep entire script (handleSubmit, formErrors derived). Replace template -- change the error block from `<Helper>` to `<p>`:
```svelte
<form onsubmit={handleSubmit} class="space-y-4 shrink-0 overflow-y-auto">
	{#if Array.isArray(formErrors) && formErrors.length > 0}
		<div class="rounded-lg bg-destructive/10 p-4">
			{#each formErrors as error}
				<p class="text-sm text-destructive">{error}</p>
			{/each}
		</div>
	{/if}
	{@render children?.()}
</form>
```

- - -

**SideNav.svelte** -- Remove `Sidebar, SidebarWrapper` imports from `flowbite-svelte`. Keep entire script. Replace template:
```svelte
<nav aria-label={props.ariaLabel as string ?? 'Sidebar'} class="h-full">
	<div class="h-full overflow-y-auto py-4 px-3">
		{@render children?.()}
	</div>
</nav>
```

- - -

**NavItem.svelte** -- Remove `SidebarItem` import from `flowbite-svelte`. Keep entire script (handleClick, isActive derived, closeSidebar). Replace template:
```svelte
<button
	class="flex w-full items-center rounded-md px-3 py-2 text-sm {isActive ? 'bg-accent text-accent-foreground font-medium' : 'text-muted-foreground hover:bg-accent hover:text-accent-foreground'}"
	onclick={handleClick}
>
	{props.label ?? ''}
</button>
```

- - -

**NavGroup.svelte** -- Remove `SidebarGroup` import from `flowbite-svelte`. Keep entire script. Replace template:
```svelte
<div class="space-y-1">
	{@render children?.()}
</div>
```

- - -

**Container.svelte** -- Remove `Card` import from `flowbite-svelte`. Keep entire script (paddingMap, paddingClass, isCard deriveds). Replace template:
```svelte
{#if isCard}
	<div class="flex items-start md:items-center justify-center min-h-0 md:min-h-[60vh]">
		<div class="w-full max-w-md rounded-lg border border-border bg-card shadow-sm {paddingClass} {(props.class as string) ?? ''}">
			{@render children?.()}
		</div>
	</div>
{:else}
	<div class="flex flex-col flex-1 min-h-0 {paddingClass} {(props.class as string) ?? ''}">
		{@render children?.()}
	</div>
{/if}
```

- - -

**ErrorDisplay.svelte** -- Remove `Alert` from `flowbite-svelte` and `ExclamationCircleOutline` from `flowbite-svelte-icons`. Add `import AlertCircle from '@lucide/svelte/icons/alert-circle';`. Keep entire script (errors derived). Replace template:
```svelte
{#if errors.length > 0}
	{#each errors as error}
		<div class="mb-2 flex items-center gap-2 rounded-md border border-destructive/20 bg-destructive/10 p-4 text-destructive">
			<AlertCircle class="size-5 shrink-0" />
			<span class="text-sm">{error.message}</span>
			{#if error.path}
				<span class="text-xs opacity-60 ml-1">{error.path}</span>
			{/if}
		</div>
	{/each}
{/if}
```

- - -

**Spinner.svelte** -- Remove `Spinner as FlowbiteSpinner` import from `flowbite-svelte`. Keep entire script. Replace template with inline SVG:
```svelte
<svg
	class="animate-spin text-primary {`size-${(props.size as string) ?? '6'}`}"
	xmlns="http://www.w3.org/2000/svg"
	fill="none"
	viewBox="0 0 24 24"
>
	<circle class="opacity-25" cx="12" cy="12" r="10" stroke="currentColor" stroke-width="4"></circle>
	<path class="opacity-75" fill="currentColor" d="M4 12a8 8 0 018-8V0C5.373 0 0 5.373 0 12h4z"></path>
</svg>
```

- - -

**ConnectionBanner.svelte** -- Remove `import Spinner from 'flowbite-svelte/Spinner.svelte'`. Keep entire script (isConnected import). Replace template with inline spinner SVG:
```svelte
{#if !isConnected()}
	<div class="bg-accent text-accent-foreground text-sm px-4 py-2 flex items-center gap-2 fixed top-0 left-0 right-0 z-50">
		<svg class="animate-spin size-4" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24">
			<circle class="opacity-25" cx="12" cy="12" r="10" stroke="currentColor" stroke-width="4"></circle>
			<path class="opacity-75" fill="currentColor" d="M4 12a8 8 0 018-8V0C5.373 0 0 5.373 0 12h4z"></path>
		</svg>
		<span>Connection lost. Reconnecting...</span>
	</div>
{/if}
```

- - -

**ErrorBoundary.svelte** -- Remove `ExclamationCircleOutline` import from `flowbite-svelte-icons`. Add `import AlertCircle from '@lucide/svelte/icons/alert-circle';`. Keep entire script. Replace icon usage in template:
```svelte
<svelte:boundary>
	{@render children()}

	{#snippet failed(error, reset)}
		<div class="border border-destructive/40 bg-destructive/10 p-4 rounded-md">
			<div class="flex items-center gap-2">
				<AlertCircle class="size-5 text-destructive" />
				<p class="text-destructive text-sm">Something went wrong rendering this component.</p>
			</div>
			<p class="text-destructive/70 text-xs mt-1">Try refreshing the page.</p>
		</div>
	{/snippet}
</svelte:boundary>
```

- - -

**ModalSurface.svelte** -- Remove `Modal` and `ModalProps` imports from `flowbite-svelte`. Keep entire script (tree, isOpen, rootProps deriveds, handleClose). Remove the `sizeMap` and `modalSize` derived (no longer needed). Replace template with fixed overlay:
```svelte
{#if isOpen}
	<!-- svelte-ignore a11y_no_static_element_interactions -->
	<div
		class="fixed inset-0 z-50 flex items-center justify-center bg-black/50"
		onclick={handleClose}
		onkeydown={(e) => e.key === 'Escape' && handleClose()}
	>
		<!-- svelte-ignore a11y_no_static_element_interactions -->
		<div
			class="relative w-full max-w-md rounded-lg bg-background shadow-lg"
			onclick={(e) => e.stopPropagation()}
			onkeydown={() => {}}
		>
			{#if tree}
				<NodeRenderer nodeId={tree.root} nodes={tree.nodes} surface="modal" />
			{/if}
		</div>
	</div>
{/if}
```

- - -

**ToastSurface.svelte** -- Remove `Toast` and `ToastProps` imports from `flowbite-svelte`. Keep entire script (toasts derived, removeToast import, fly transition import). Remove the `severityToColor` mapping. Add a new severity-to-class mapping:
```typescript
const severityClass: Record<string, string> = {
	success: 'border-green-500/30 bg-green-50 text-green-800',
	error: 'border-destructive/30 bg-destructive/10 text-destructive',
	warning: 'border-yellow-500/30 bg-yellow-50 text-yellow-800',
	info: 'border-border bg-background text-foreground',
};
```
Replace template:
```svelte
<div class="fixed top-4 left-4 right-4 md:left-auto z-[60] flex flex-col gap-2 md:max-w-sm">
	{#each toasts as toast (toast.id)}
		<div transition:fly={{ x: 100, duration: 200 }}>
			<div class="flex items-center justify-between rounded-md border p-4 shadow-md {severityClass[toast.severity] ?? severityClass.info}">
				<span class="text-sm">{toast.message}</span>
				<button
					class="ml-4 text-current opacity-50 hover:opacity-100"
					onclick={() => removeToast(toast.id)}
					aria-label="Dismiss"
				>&times;</button>
			</div>
		</div>
	{/each}
</div>
```

- - -

**ConfirmDialog.svelte** -- Remove `Modal, Button` imports from `flowbite-svelte`. Keep entire script (title, message, confirmLabel, cancelLabel, destructive deriveds, handleConfirm, handleCancel). Replace raw color classes with semantic tokens in template:
```svelte
<div class="p-4">
	{#if title}
		<h3 class="mb-4 text-lg font-semibold text-foreground">{title}</h3>
	{/if}
	{#if message}
		<p class="mb-6 text-muted-foreground">{message}</p>
	{/if}
	<div class="flex justify-end gap-3">
		<button
			type="button"
			class="inline-flex items-center justify-center rounded-md text-sm font-medium h-10 px-4 py-2 border border-input bg-background hover:bg-accent hover:text-accent-foreground"
			onclick={handleCancel}
		>
			{cancelLabel}
		</button>
		<button
			type="button"
			class="inline-flex items-center justify-center rounded-md text-sm font-medium h-10 px-4 py-2 {destructive ? 'bg-destructive text-destructive-foreground hover:bg-destructive/90' : 'bg-primary text-primary-foreground hover:bg-primary/90'}"
			onclick={handleConfirm}
		>
			{confirmLabel}
		</button>
	</div>
</div>
```

- - -

**DataTable.svelte** -- Remove `ChevronUpOutline, ChevronDownOutline` imports from `flowbite-svelte-icons`. Add:
```typescript
import ChevronUp from '@lucide/svelte/icons/chevron-up';
import ChevronDown from '@lucide/svelte/icons/chevron-down';
```
Keep ALL business logic (virtual scroll, sorting, fetch-rows, handleRowClick, handleScroll, handleSort). Update only the icon references and raw color classes in template:
- `<ChevronUpOutline class="w-4 h-4 inline" />` -> `<ChevronUp class="size-4 inline" />`
- `<ChevronDownOutline class="w-4 h-4 inline" />` -> `<ChevronDown class="size-4 inline" />`
- `text-gray-500` -> `text-muted-foreground`
- `bg-gray-50 text-xs uppercase text-gray-700` -> `bg-muted text-xs uppercase text-foreground`
- `hover:bg-gray-100` -> `hover:bg-accent`
- `bg-white hover:bg-gray-50` -> `bg-background hover:bg-accent`
- `bg-gray-100` (loading skeleton) -> `bg-muted`

- - -

**+layout.svelte** -- Remove `import { BarsOutline, CloseOutline } from 'flowbite-svelte-icons'`. Add:
```typescript
import Menu from '@lucide/svelte/icons/menu';
import X from '@lucide/svelte/icons/x';
```
Update template:
- `<CloseOutline class="w-6 h-6" />` -> `<X class="size-6" />`
- `<BarsOutline class="w-6 h-6" />` -> `<Menu class="size-6" />`
- `bg-gray-50 border-b border-gray-200` -> `bg-muted border-b border-border`
- `text-gray-600 hover:text-gray-900` -> `text-muted-foreground hover:text-foreground`
  </action>
  <verify>
    <automated>cd /home/oetiker/checkouts/marionette && ! grep -r 'flowbite' frontend/src/ && echo "ZERO_FLOWBITE_IMPORTS" && cd frontend && npx vite build 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `grep -r 'flowbite' frontend/src/` returns zero matches
    - Button.svelte contains "bg-primary text-primary-foreground" and does NOT contain "flowbite"
    - TextInput.svelte contains "border-input bg-background" and does NOT contain "flowbite"
    - SelectInput.svelte contains "border-input bg-background" and does NOT contain "flowbite"
    - Checkbox.svelte contains 'type="checkbox"' and does NOT contain "flowbite"
    - Form.svelte contains "bg-destructive/10" and does NOT contain "flowbite"
    - SideNav.svelte contains '<nav' and does NOT contain "flowbite"
    - NavItem.svelte contains "bg-accent text-accent-foreground" and does NOT contain "flowbite"
    - NavGroup.svelte contains "space-y-1" and does NOT contain "flowbite"
    - Container.svelte contains "border-border bg-card" and does NOT contain "flowbite"
    - ErrorDisplay.svelte contains "@lucide/svelte/icons/alert-circle" and does NOT contain "flowbite"
    - Spinner.svelte contains "animate-spin" and does NOT contain "flowbite"
    - ConnectionBanner.svelte contains "animate-spin" and does NOT contain "flowbite"
    - ErrorBoundary.svelte contains "@lucide/svelte/icons/alert-circle" and does NOT contain "flowbite"
    - ModalSurface.svelte contains "bg-black/50" and does NOT contain "flowbite"
    - ToastSurface.svelte contains "rounded-md border" and does NOT contain "flowbite"
    - ConfirmDialog.svelte contains "bg-primary text-primary-foreground" and does NOT contain "flowbite"
    - DataTable.svelte contains "@lucide/svelte/icons/chevron-up" and does NOT contain "flowbite"
    - +layout.svelte contains "@lucide/svelte/icons/menu" and does NOT contain "flowbite"
    - `cd frontend && npx vite build` exits with code 0
  </acceptance_criteria>
  <done>All 17 component files + layout stubbed with HTML+Tailwind+semantic tokens, zero Flowbite imports in frontend/src/, build passes</done>
</task>

<task type="auto">
  <name>Task 2: Remove Flowbite packages from package.json and verify clean state</name>
  <files>
    frontend/package.json
  </files>
  <read_first>
    frontend/package.json
  </read_first>
  <action>
Run in the `frontend/` directory:
```bash
npm uninstall flowbite-svelte flowbite-svelte-icons
```

This removes both packages from `dependencies` in package.json and updates package-lock.json.

After uninstall, run a full build and test suite:
```bash
cd frontend && npm run build && npx vitest run
```

If the build or tests fail, investigate and fix. The most likely issue is a missed Flowbite import somewhere (grep should have caught this in Task 1).

Also run `npx vitest run --config vitest-browser.config.ts` for browser tests if time permits.
  </action>
  <verify>
    <automated>cd /home/oetiker/checkouts/marionette/frontend && ! grep -q 'flowbite' package.json && npm run build 2>&1 | tail -3 && npx vitest run 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - frontend/package.json does NOT contain "flowbite-svelte" or "flowbite-svelte-icons"
    - `npm run build` exits with code 0
    - `npx vitest run` shows all tests passing
    - `grep -r 'flowbite' frontend/src/` returns zero matches (already true from Task 1, confirming post-uninstall)
  </acceptance_criteria>
  <done>Flowbite packages removed, frontend builds cleanly, all tests pass, zero Flowbite references anywhere in codebase</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user input -> SDUI stubs | Stubs render server-provided content; XSS mitigated by Svelte's default escaping |
| modal backdrop -> close handler | Click events on overlay trigger close-modal action |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-03 | Spoofing | ModalSurface | accept | Modal content is server-driven via SDUI protocol; no user-controllable URLs or actions beyond what server provides |
| T-10-04 | Tampering | Component stubs | accept | Stubs are rendering-only; all data validation is server-side per SDUI architecture. Svelte escapes interpolated text by default. |
| T-10-05 | Denial of Service | ToastSurface | accept | Toast accumulation is controlled by server-side protocol; client just renders what it receives |
</threat_model>

<verification>
1. `grep -r 'flowbite' frontend/src/` -- must return zero matches
2. `grep -q 'flowbite' frontend/package.json` -- must fail (no match)
3. `cd frontend && npm run build` -- must exit 0
4. `cd frontend && npx vitest run` -- all tests green
5. `test -f frontend/components.json` -- shadcn config still present
6. `grep -q 'oklch' frontend/src/app.css` -- OKLCH tokens still present
</verification>

<success_criteria>
- Zero Flowbite references in any file under frontend/src/
- Zero Flowbite packages in frontend/package.json
- Frontend builds without errors
- All existing tests pass
- All stubs use semantic token classes (bg-primary, bg-background, etc.)
- All stubs preserve the SDUI interface contract
</success_criteria>

<output>
After completion, create `.planning/phases/10-foundation/10-02-SUMMARY.md`
</output>
