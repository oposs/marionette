---
phase: 15-crm-migration-validation
plan: 06
type: execute
wave: 3
depends_on: [15-03, 15-04]
files_modified:
  - CONCEPT.md
  - TOOLING.md
  - .planning/codebase/STACK.md
  - spec/PROTOCOL.md
  - frontend/tests/e2e/ci-guards.spec.ts
autonomous: true
requirements: [COMP-03]
tags: [docs, ci-guard, flowbite-residue, protocol-spec, brand-voice]

must_haves:
  truths:
    - "CONCEPT.md no longer references Flowbite as primary vocabulary (lines 260, 268, 630 updated to shadcn-svelte)"
    - "TOOLING.md line 39 updated from Flowbite Svelte to shadcn-svelte"
    - ".planning/codebase/STACK.md frontend stack section describes shadcn-svelte (not flowbite-svelte)"
    - "spec/PROTOCOL.md legacy /contactForm/errors validation section (lines 803-819) deleted"
    - "spec/PROTOCOL.md canonical /_errors/{bind} section includes a worked multi-field validation example (D-D2)"
    - "frontend/tests/e2e/ci-guards.spec.ts has a new test 'No Flowbite residue in runtime code' that runs git grep and asserts 0 matches"
    - "The new test also guards FormScreen.svelte deletion (Phase 14 D-A1) — currently missing from the file"
    - "The Flowbite CI guard runs AFTER the doc sweep in the execution order — running before would turn CI red"
  artifacts:
    - path: "CONCEPT.md"
      provides: "No Flowbite references; shadcn-svelte is the primary vocabulary"
    - path: "TOOLING.md"
      provides: "shadcn-svelte described as the component library"
    - path: ".planning/codebase/STACK.md"
      provides: "Frontend stack entry updated to shadcn-svelte + bits-ui + lucide"
    - path: "spec/PROTOCOL.md"
      provides: "Only canonical /_errors/{bind} validation documentation + worked multi-field example"
      contains: "Worked example: multi-field validation"
    - path: "frontend/tests/e2e/ci-guards.spec.ts"
      provides: "Test block 'No Flowbite residue in runtime code' + FormScreen deletion guard"
      contains: "No Flowbite residue in runtime code"
  key_links:
    - from: "ci-guards.spec.ts"
      to: "git grep run via execSync"
      via: "execSync against repo root"
      pattern: "git grep.*flowbite"
    - from: "ci-guards.spec.ts new FormScreen guard"
      to: "frontend/src/lib/components/screen/FormScreen.svelte"
      via: "existsSync check"
      pattern: "FormScreen\\.svelte"

user_setup: []

threat_model:
  trust_boundaries:
    - name: "CI pipeline repo scan"
      description: "The Flowbite grep guard runs against the repo filesystem via git grep. Pattern is a literal string (no injection risk)."
  stride:
    - id: T-15-02
      category: Spoofing / Drift
      component: "Flowbite residue guard"
      disposition: mitigate
      mitigation: "D-F1 CI guard asserts 0 matches of 'flowbite' (case-insensitive) under runtime paths. Prevents regression via copy-pasted tutorials. Grep pattern is hard-coded literal; no user input."
    - id: T-15-03-PLAN06
      category: Information Disclosure (doc)
      component: "CONCEPT.md + TOOLING.md + STACK.md brand-voice"
      disposition: accept
      mitigation: "Doc edits are prose replacements. No sensitive content. No secrets embedded."
---

<objective>
Lock the Flowbite clean break with a CI grep guard and align user-facing + governance documentation to shadcn-svelte. Delete the legacy /contactForm/errors validation section from spec/PROTOCOL.md (WR-02 closure) and add the canonical worked-example for multi-field validation per D-D2.

Purpose: Closes D-F1 (CI guard), D-F2 (user-facing doc sweep), D-F3 (governance doc sweep), D-D2 (protocol spec surgery). The doc sweep must land BEFORE the grep guard runs for the first time — otherwise CI turns red.

Output:
- Updated CONCEPT.md (lines ~260, 268, 630)
- Updated TOOLING.md (line 39)
- Updated .planning/codebase/STACK.md (line 47)
- Updated spec/PROTOCOL.md (delete lines 803-819; add worked example under Validation semantics)
- Extended ci-guards.spec.ts with Flowbite residue test + FormScreen deletion guard

Runs in Wave 3 after handlers migrate. Parallel with Plan 05 (different files).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-crm-migration-validation/15-CONTEXT.md
@.planning/phases/15-crm-migration-validation/15-RESEARCH.md
@.planning/phases/15-crm-migration-validation/15-PATTERNS.md
@.planning/phases/15-crm-migration-validation/15-UI-SPEC.md

<interfaces>
See 15-UI-SPEC §Doc Brand-Voice Sweep + 15-PATTERNS.md items 22-25 + 15-RESEARCH.md §Example 6 (CI guard) and §Example 8 (worked-example text).

Target replacement strings (locked by 15-UI-SPEC):

CONCEPT.md:
- Line ~260: replace "Flowbite" with "shadcn-svelte" inside the cross-platform sentence.
- Line ~268: ASCII-table cell "(Flowbite)" becomes "(shadcn-svelte)".
- Line ~630: heading "Phase 2: Marionette Frontend (Svelte 5 + Flowbite)" becomes "Phase 2: Marionette Frontend (Svelte 5 + shadcn-svelte)".

TOOLING.md:
- Line ~39: "- **Flowbite Svelte** - Tailwind CSS component library" becomes "- **shadcn-svelte** - Tailwind CSS + bits-ui component library".

.planning/codebase/STACK.md:
- Line ~47: "flowbite-svelte 1.31 + flowbite-svelte-icons 3.1 - UI component library" becomes "shadcn-svelte 1.2.7 + bits-ui 2.17.3 + @lucide/svelte 1.8.0 - UI component library" (preserve backtick markdown wrapping).

spec/PROTOCOL.md:
- Delete the legacy "Validation Errors as Data" section (lines ~803-819 — the heading plus prose plus YAML snippet plus trailing paragraph).
- Preserve the canonical "Validation semantics" section (lines ~593-600).
- Append a "Worked example: multi-field validation on form submit" subsection (use #### heading, not ###) immediately after line 600, with the JSON payload from 15-RESEARCH.md §Example 8. Subsection text:

  "A handler receiving an invalid form payload returns a single PatchMessage with one SetData op per invalid field, targeting the form's surface. The frontend's Field.Error anatomy picks up each entry and renders it inline below the bound control."

  Followed by a fenced JSON code block showing:
    type: patch, surface: content, patch: [two Set ops against /_errors/contactForm/name and /_errors/contactForm/email].

  Followed by a paragraph:
  "The save handler that produced this patch returns Ok(vec![patch]) — NOT Err(ActionError::BadPayload). ErrorMessage is reserved for protocol-level failures (malformed action payload, unknown surface, server crash, auth failure). Field-level validation is data, and flows through the normal patch channel."

  Final paragraph:
  "When the user fixes the offending fields and resubmits, the next success render replaces the surface data wholesale, clearing any prior _errors. The handler does not need to emit clear-error patches explicitly."

ci-guards.spec.ts new test block content: see 15-RESEARCH.md §Example 6 (lines 955-1003). The test uses execSync to run `git grep -Iil 'flowbite'` with scoped pathspec pathspec arguments `'frontend/src/**' 'backend/crates/**' 'spec/**' CONCEPT.md TOOLING.md`, cwd set to REPO_ROOT. On exit code 1 (no matches) the match list is empty (success case). Any other non-zero code rethrows. Assertion: `expect(matches).toHaveLength(0)`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Doc brand-voice sweep (CONCEPT.md, TOOLING.md, STACK.md) + PROTOCOL.md validation section surgery</name>
  <files>CONCEPT.md, TOOLING.md, .planning/codebase/STACK.md, spec/PROTOCOL.md</files>
  <read_first>
    - CONCEPT.md (full file — locate all Flowbite mentions via grep, not just the three called out)
    - TOOLING.md (line 39)
    - .planning/codebase/STACK.md (line 47 or wherever the frontend stack is described)
    - spec/PROTOCOL.md (lines 593-600 canonical validation section; lines 803-819 legacy section to delete)
    - .planning/phases/15-crm-migration-validation/15-UI-SPEC.md §Doc Brand-Voice Sweep (locked target strings)
    - .planning/phases/15-crm-migration-validation/15-PATTERNS.md items 22, 23, 24, 25
    - .planning/phases/15-crm-migration-validation/15-RESEARCH.md §Example 8 (worked-example text for PROTOCOL.md)
  </read_first>
  <behavior>
    - CONCEPT.md: replace the three specific Flowbite mentions at lines 260/268/630 per 15-UI-SPEC locked strings. If grep reveals ADDITIONAL Flowbite mentions in CONCEPT.md beyond those three, sweep them too (per 15-UI-SPEC Assumption A6 — do not whitelist).
    - Do NOT add a historical footnote mentioning Flowbite — it would trigger CI guard failure in Task 2. Clean break only (consistent with user memory feedback_pre_deployment_no_backcompat).
    - TOOLING.md line 39: apply locked string replacement. Sweep additional Flowbite mentions if any.
    - .planning/codebase/STACK.md: replace frontend-stack line with shadcn-svelte entry. Sweep additional mentions if any (STACK.md is NOT in CI-guard grep scope but must still be updated per D-F3).
    - spec/PROTOCOL.md: DELETE legacy section at lines 803-819 wholesale (heading + prose + YAML block + trailing paragraph). PRESERVE canonical section at 593-600 verbatim. APPEND worked-example subsection immediately after line 600 using #### heading.
    - After edits, grep assertions: CONCEPT.md / TOOLING.md / STACK.md / spec/PROTOCOL.md have zero `flowbite` matches (case-insensitive); zero `contactForm/errors` matches; exactly one `Worked example: multi-field validation` match.
  </behavior>
  <action>
**Part A — CONCEPT.md (D-F2):**

Grep first to find all Flowbite mentions:

    grep -n -i flowbite /home/oetiker/checkouts/marionette/CONCEPT.md

Apply replacements for the three called-out lines (~260, ~268, ~630) per the locked strings in the `<interfaces>` section above. Preserve preceding and following text. For the ASCII-table cell at line ~268, maintain alignment — `shadcn-svelte` is longer than `Flowbite`; adjust adjacent whitespace or, if alignment breaks meaningfully, rebuild the row with consistent widths.

For ANY additional "flowbite" matches found by the grep that were NOT called out in CONTEXT, sweep them to appropriate shadcn-svelte equivalents per UI-SPEC §Doc Brand-Voice Sweep tone rules (preserve cross-platform framing, lowercase shadcn-svelte, no marketing adjectives).

Do NOT add a historical Flowbite footnote — the CI guard in Task 2 asserts zero matches.

**Part B — TOOLING.md (D-F2):**

Grep, then apply the one-line locked replacement at line 39. Sweep any additional Flowbite mentions.

**Part C — .planning/codebase/STACK.md (D-F3):**

Grep, then apply the one-line locked replacement at line 47 (or wherever the frontend-stack entry lives). Preserve markdown backtick formatting around package names.

**Part D — spec/PROTOCOL.md (D-D2):**

Step 1 — Preserve canonical section: Open the file, confirm the "Validation semantics" section at lines 593-600 is intact. Do NOT modify its content.

Step 2 — Delete legacy section: Locate the "Validation Errors as Data" section (heading around line 803, through closing paragraph around line 819). Remove the heading, prose intro, YAML code block, and trailing paragraph entirely. Preserve section separators — if there's a blank line before and after the deleted block, keep one blank line for rhythm.

Step 3 — Append worked-example subsection: Immediately after the canonical section's closing paragraph (line ~600), insert a blank line followed by:

```
#### Worked example: multi-field validation on form submit

A handler receiving an invalid form payload returns a single `PatchMessage`
with one `SetData` op per invalid field, targeting the form's surface. The
frontend's `Field.Error` anatomy picks up each entry and renders it inline
below the bound control.
```

Then a fenced JSON code block (triple backticks with `json` language tag) showing:

```
{
  "type": "patch",
  "surface": "content",
  "patch": [
    { "op": "set", "path": "/_errors/contactForm/name",  "value": "Contact name is required." },
    { "op": "set", "path": "/_errors/contactForm/email", "value": "Please enter a valid email address." }
  ]
}
```

Then a paragraph:

```
The save handler that produced this patch returns `Ok(vec![patch])` — NOT
`Err(ActionError::BadPayload)`. `ErrorMessage` is reserved for protocol-level
failures (malformed action payload, unknown surface, server crash, auth
failure). Field-level validation is data, and flows through the normal patch
channel.
```

Then a closing paragraph:

```
When the user fixes the offending fields and resubmits, the next success
render replaces the surface data wholesale, clearing any prior `_errors`.
The handler does not need to emit "clear error" patches explicitly.
```

Use `####` for the subsection heading (it sits under `### Validation semantics`). Preserve markdown rhythm with a blank line before and after each paragraph.

**Verification checklist (run after all edits):**

    grep -i -c flowbite CONCEPT.md           # expects 0
    grep -i -c flowbite TOOLING.md           # expects 0
    grep -i -c flowbite .planning/codebase/STACK.md    # expects 0
    grep -c "contactForm/errors" spec/PROTOCOL.md      # expects 0
    grep -c "Worked example: multi-field validation" spec/PROTOCOL.md    # expects 1

All five must pass.
  </action>
  <verify>
    <automated>cd /home/oetiker/checkouts/marionette && bash -c 'echo "CONCEPT.md: $(grep -i -c flowbite CONCEPT.md)"; echo "TOOLING.md: $(grep -i -c flowbite TOOLING.md)"; echo "STACK.md: $(grep -i -c flowbite .planning/codebase/STACK.md)"; echo "PROTOCOL.md contactForm/errors: $(grep -c contactForm/errors spec/PROTOCOL.md)"; echo "PROTOCOL.md worked-example: $(grep -c "Worked example" spec/PROTOCOL.md)"'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -i -c flowbite /home/oetiker/checkouts/marionette/CONCEPT.md` returns 0
    - `grep -i -c flowbite /home/oetiker/checkouts/marionette/TOOLING.md` returns 0
    - `grep -i -c flowbite /home/oetiker/checkouts/marionette/.planning/codebase/STACK.md` returns 0
    - `grep -c "contactForm/errors" /home/oetiker/checkouts/marionette/spec/PROTOCOL.md` returns 0
    - `grep -c "Worked example: multi-field validation" /home/oetiker/checkouts/marionette/spec/PROTOCOL.md` returns exactly 1
    - `grep -c "shadcn-svelte" /home/oetiker/checkouts/marionette/CONCEPT.md` returns ≥3 (the three replaced mentions + any historical-context mentions added during sweep)
    - `grep -c "shadcn-svelte" /home/oetiker/checkouts/marionette/TOOLING.md` returns ≥1
    - `grep -c "shadcn-svelte" /home/oetiker/checkouts/marionette/.planning/codebase/STACK.md` returns ≥1
    - spec/PROTOCOL.md canonical "Validation semantics" section (lines ~593-600 content) still present: `grep -c "/_errors/{bind}" /home/oetiker/checkouts/marionette/spec/PROTOCOL.md` returns ≥2 (canonical section + worked example)
  </acceptance_criteria>
  <done>All three runtime-guarded docs (CONCEPT.md, TOOLING.md, STACK.md) have zero Flowbite residue, PROTOCOL.md has only the canonical validation shape documented plus a worked multi-field example, legacy section deleted.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend ci-guards.spec.ts with Flowbite residue test + FormScreen deletion guard</name>
  <files>frontend/tests/e2e/ci-guards.spec.ts</files>
  <read_first>
    - frontend/tests/e2e/ci-guards.spec.ts (current full file — existing test blocks at lines 31-47, imports at lines 1-26 which Plan 05 already cleaned by removing @ts-expect-error suppressions)
    - .planning/phases/15-crm-migration-validation/15-RESEARCH.md §Example 6 (target CI guard test block at lines 955-1003)
    - .planning/phases/15-crm-migration-validation/15-PATTERNS.md item 16 (ci-guards delta)
    - After Task 1 ran: CONCEPT.md / TOOLING.md / spec/PROTOCOL.md are clean — the grep guard should pass green on first run
  </read_first>
  <behavior>
    - Existing TableScreen deletion guard (lines 31-47) preserved.
    - New FormScreen deletion guard test block added (mirrors TableScreen block, paths to `src/lib/components/screen/FormScreen.svelte` and `src/lib/components/screen/FormScreen.browser-test.ts` — Phase 14 D-A1 retired both).
    - New Flowbite residue test block added — uses execSync to run git grep with scoped pathspec args against the repo root. On git grep exit code 1 (no matches) the matches list is empty (success). Any other non-zero exit rethrows.
    - REPO_ROOT constant computed as `resolve(FRONTEND_ROOT, '..')` — one level up from the existing FRONTEND_ROOT.
    - Test groups wrapped under a new `test.describe('Phase 13/14/15 CI guards', ...)` block (or whatever group structure the existing file uses — extend, don't replace).
    - All three guards pass green after Task 1's doc sweep completes.
  </behavior>
  <action>
Open `frontend/tests/e2e/ci-guards.spec.ts`. After Plan 05 Task 1, the imports section at lines 1-26 no longer has the three `@ts-expect-error` suppressions. Preserve the current structure and extend as follows:

Step 1 — Add import for `execSync` from `node:child_process` if not already present:

    import { execSync } from 'node:child_process';

Step 2 — Add a `REPO_ROOT` constant near the existing `FRONTEND_ROOT` constant:

    const REPO_ROOT = resolve(FRONTEND_ROOT, '..');

Step 3 — Extend or add a `test.describe` group so the structure contains three guard tests. Final structure:

- **TableScreen.svelte retired** — existing, preserve verbatim (Phase 13 D-A2).
- **TableScreen.browser-test.ts retired** — existing, preserve verbatim.
- **FormScreen.svelte retired (Phase 14 D-A1)** — NEW test. Mirrors TableScreen.svelte guard. Path: `src/lib/components/screen/FormScreen.svelte`. Expected: `existsSync(p)` returns false.
- **FormScreen.browser-test.ts retired (Phase 14 D-A1)** — NEW test. Mirrors pattern. Path: `src/lib/components/screen/FormScreen.browser-test.ts`.
- **No Flowbite residue in runtime code (Phase 15 D-F1)** — NEW test. Uses execSync + git grep, asserts 0 matches.

Step 4 — The Flowbite residue test body (append at the end of the `test.describe` group):

    test('No Flowbite residue in runtime code (Phase 15 D-F1)', () => {
        // git grep -Iil 'flowbite' → case-insensitive match, text files only,
        // name-only output. Respects .gitignore (skips node_modules/, target/,
        // __snapshots__/, etc.). -I skips binary files automatically.
        let matches: string[] = [];
        try {
            const out = execSync(
                "git grep -Iil 'flowbite' -- " +
                "'frontend/src/**' " +
                "'backend/crates/**' " +
                "'spec/**' " +
                "CONCEPT.md " +
                "TOOLING.md",
                { cwd: REPO_ROOT, encoding: 'utf8' }
            );
            matches = out.trim().split('\n').filter(Boolean);
        } catch (e: unknown) {
            // git grep exits with code 1 when there are NO matches — that's the
            // success case for this test. Any other non-zero exit is a genuine
            // error.
            const err = e as { status?: number; stdout?: string };
            if (err.status === 1) {
                matches = [];
            } else {
                throw e;
            }
        }
        expect(
            matches,
            `Flowbite residue found in:\n${matches.join('\n')}`
        ).toHaveLength(0);
    });

Step 5 — The FormScreen deletion guards follow the existing TableScreen pattern exactly (copy, rename). Example:

    test('FormScreen.svelte is retired (Phase 14 D-A1)', () => {
        const p = resolve(
            FRONTEND_ROOT,
            'src/lib/components/screen/FormScreen.svelte',
        );
        expect(existsSync(p)).toBe(false);
    });

    test('FormScreen.browser-test.ts is retired (Phase 14 D-A1)', () => {
        const p = resolve(
            FRONTEND_ROOT,
            'src/lib/components/screen/FormScreen.browser-test.ts',
        );
        expect(existsSync(p)).toBe(false);
    });

**Rename the outer `test.describe` group** from whatever it currently says (likely "Phase 13 CI guards") to "Phase 13/14/15 CI guards" to reflect the expanded scope.

**Verification:** `cd frontend && npx playwright test tests/e2e/ci-guards.spec.ts` — all 5 tests pass green. If the Flowbite residue test fails, it means Task 1's doc sweep was incomplete or missed a file — re-grep the full repo and fix any surviving matches. Tests should pass in this order:

    cd /home/oetiker/checkouts/marionette/frontend
    npx playwright test tests/e2e/ci-guards.spec.ts

All 5 guards green.
  </action>
  <verify>
    <automated>cd /home/oetiker/checkouts/marionette/frontend && npx playwright test tests/e2e/ci-guards.spec.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "No Flowbite residue in runtime code" /home/oetiker/checkouts/marionette/frontend/tests/e2e/ci-guards.spec.ts` returns exactly 1
    - `grep -c "FormScreen.svelte is retired" /home/oetiker/checkouts/marionette/frontend/tests/e2e/ci-guards.spec.ts` returns exactly 1
    - `grep -c "FormScreen.browser-test.ts is retired" /home/oetiker/checkouts/marionette/frontend/tests/e2e/ci-guards.spec.ts` returns exactly 1
    - `grep -c "TableScreen.svelte is retired" /home/oetiker/checkouts/marionette/frontend/tests/e2e/ci-guards.spec.ts` returns ≥1 (existing guard preserved)
    - `grep -c "from 'node:child_process'" /home/oetiker/checkouts/marionette/frontend/tests/e2e/ci-guards.spec.ts` returns ≥1
    - `grep -c "REPO_ROOT" /home/oetiker/checkouts/marionette/frontend/tests/e2e/ci-guards.spec.ts` returns ≥1
    - `cd /home/oetiker/checkouts/marionette/frontend && npx playwright test tests/e2e/ci-guards.spec.ts` — all tests pass (5 expected)
  </acceptance_criteria>
  <done>ci-guards.spec.ts has 5 guards: TableScreen.svelte + TableScreen.browser-test.ts retired (existing), FormScreen.svelte + FormScreen.browser-test.ts retired (new), and Flowbite residue grep (new). All pass green after Task 1's doc sweep.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI pipeline → repo scan | git grep guard runs against filesystem; pattern is a literal string (no injection). |
| docs → external consumers | CONCEPT.md / TOOLING.md are public-facing; content must be factual. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-02 | Spoofing / Drift regression | Flowbite residue guard | mitigate | D-F1 CI grep asserts 0 matches under scoped paths; hard-coded pattern; prevents future drift. |
| T-15-03-PLAN06 | Information Disclosure (doc) | Public docs | accept | Prose-only changes; no sensitive content. |
| T-15-01 | Test-hook leak | n/a (Plan 05) | — | — |

Security invariant: the CI guard's pattern string `'flowbite'` is a literal — no user input. execSync argument array passes through Node's child_process safely; no shell-metachar injection vector.
</threat_model>

<verification>
- All 5 ci-guards.spec.ts tests pass: TableScreen x2, FormScreen x2, Flowbite residue.
- Grep assertions from Task 1 all return expected counts (0 for flowbite, 1 for worked-example).
- svelte-check still green (no new errors introduced).
- `cd frontend && npm run test:browser` — no regressions.
</verification>

<success_criteria>
- Zero Flowbite tokens under frontend/src, backend/crates, spec/, CONCEPT.md, TOOLING.md.
- STACK.md frontend stack entry describes shadcn-svelte (not enforced by CI guard but governed by D-F3).
- PROTOCOL.md has a single canonical validation section plus a worked multi-field example.
- ci-guards.spec.ts protects against regression via the grep guard.
</success_criteria>

<output>
After completion, create `.planning/phases/15-crm-migration-validation/15-06-SUMMARY.md` following `~/.claude/get-shit-done/templates/summary.md`.
</output>
