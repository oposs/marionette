---
phase: 08-crm-features
plan: 02
type: execute
wave: 2
depends_on: [08-01]
files_modified:
  - backend/crates/crm-demo/src/handlers/note.rs
  - backend/crates/crm-demo/src/handlers/mod.rs
  - backend/crates/crm-demo/src/handlers/contact.rs
  - backend/crates/crm-demo/src/handlers/company.rs
  - backend/crates/crm-demo/src/main.rs
autonomous: true
requirements: [CRM-06]
must_haves:
  truths:
    - "User can add a plain text note to a contact from the contact edit view"
    - "User can add a plain text note to a company from the company edit view"
    - "Notes appear chronologically (newest first) below the edit form"
    - "Each note shows text, author name, and timestamp"
  artifacts:
    - path: "backend/crates/crm-demo/src/handlers/note.rs"
      provides: "note_save handler for creating notes"
      exports: ["handle_note_save"]
    - path: "backend/crates/crm-demo/src/handlers/contact.rs"
      provides: "Contact form with notes section appended"
      contains: "notes-heading"
    - path: "backend/crates/crm-demo/src/handlers/company.rs"
      provides: "Company form with notes section appended"
      contains: "notes-heading"
  key_links:
    - from: "backend/crates/crm-demo/src/handlers/note.rs"
      to: "note entity"
      via: "note::Entity insert"
      pattern: "note::ActiveModel"
    - from: "backend/crates/crm-demo/src/main.rs"
      to: "note handler"
      via: "action router registration"
      pattern: "note_save.*handle_note_save"
---

<objective>
Add notes feature: a note_save handler, and integrate notes section into contact and company edit/detail views. Notes are append-only plain text with timestamps.

Purpose: Fulfills CRM-06 -- users can add timestamped notes to contacts and companies.
Output: New note handler, modified contact and company form handlers with notes section, router wiring.
</objective>

<execution_context>
@/home/oetiker/.claude/get-shit-done/workflows/execute-plan.md
@/home/oetiker/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-crm-features/08-CONTEXT.md
@.planning/phases/08-crm-features/08-RESEARCH.md
@.planning/phases/08-crm-features/08-01-SUMMARY.md

<interfaces>
<!-- Existing handler patterns -->

From backend/crates/crm-demo/src/handlers/contact.rs:
```rust
use marionette::builders::standard::{Button, Container, DataTable, Form, Heading, Select, SelectOption, TableColumn, TextInput};
use marionette::error::{ActionError, ActionResult};
use marionette::extractors::{Db, FromHandlerContext, HandlerContext, Payload, Session};
use marionette_protocol::{ComponentAction, ProtocolMessage, RenderMessage};

// handle_contact_form builds form + renders to "main" surface
// Pattern: heading + form fields + save/cancel buttons -> Container -> nodes HashMap -> RenderMessage
```

From backend/crates/crm-demo/src/audit.rs:
```rust
pub async fn record_audit(db: &DatabaseConnection, user_id: i32, table: &str, record_id: i32, action: &str, changes: serde_json::Value) -> Result<(), ActionError>;
```

From backend/crates/crm-demo/src/main.rs:
```rust
// Action router pattern:
.action("contact_save", box_handler(handlers::contact::handle_contact_save), AuthRequirement::Authenticated)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create note_save handler and wire into router</name>
  <files>
    backend/crates/crm-demo/src/handlers/note.rs,
    backend/crates/crm-demo/src/handlers/mod.rs,
    backend/crates/crm-demo/src/main.rs
  </files>
  <read_first>
    backend/crates/crm-demo/src/handlers/contact.rs,
    backend/crates/crm-demo/src/handlers/mod.rs,
    backend/crates/crm-demo/src/main.rs,
    backend/crates/crm-demo/src/entities/note.rs,
    backend/crates/crm-demo/src/audit.rs
  </read_first>
  <action>
Create `handlers/note.rs` with a `handle_note_save` handler following the contact_save pattern:

1. Define `NoteSavePayload` struct: `contact_id: Option<i32>`, `company_id: Option<i32>`, `text: String`.
2. Extract Db, Session, Payload from context.
3. Validate that `text.trim()` is not empty (return `ActionError::BadPayload("Note text is required")`).
4. Validate that exactly one of contact_id or company_id is provided (not both, not neither).
5. Insert `note::ActiveModel` with: note_id=NotSet, note_contact=Set(contact_id), note_company=Set(company_id), note_text=Set(text), note_user=Set(caller_id parsed from session), note_created_at=NotSet (DB default).
6. Call `crate::audit::record_audit` with table="note", action="create", changes=json with text + contact/company id.
7. After save, re-render the appropriate form:
   - If contact_id is Some, call `super::contact::handle_contact_form` with a cloned context where the payload is set to `{"contact_id": id}` -- OR simpler: construct a new HandlerContext with the appropriate action payload. Since we need to trigger re-render of the contact form, the simplest approach is to directly call `crate::handlers::contact::handle_contact_form` passing the original context. The contact_form handler already extracts contact_id from payload optionally, so set the action payload to `{"contact_id": X}` by modifying `ctx.action.payload`.
   - If company_id is Some, similarly call company form.

SIMPLER APPROACH: Instead of re-calling another handler, just navigate back by returning a re-render. The note_save payload should include a `return_to` field indicating "contact_edit" or "company_edit" plus the entity ID. After inserting the note, reconstruct the context action to look like a contact_edit/company_edit call and delegate:

```rust
#[derive(Deserialize)]
struct NoteSavePayload {
    contact_id: Option<i32>,
    company_id: Option<i32>,
    text: String,
}
```

After insert, determine what to re-render:
- If contact_id is Some(cid): create a new HandlerContext with action payload `{"contact_id": cid}` and call `super::contact::handle_contact_form(new_ctx).await`
- If company_id is Some(cid): create a new HandlerContext with action payload `{"company_id": cid}` and call `super::company::handle_company_form(new_ctx).await`

To create the new context, clone `ctx` and replace `ctx.action.payload` with the appropriate JSON value. Check how HandlerContext is structured -- it likely has `action: ActionMessage` with a `payload: Option<serde_json::Value>` field.

Update `handlers/mod.rs`: add `pub mod note;`

Update `main.rs` action router: add `note_save` action pointing to `handlers::note::handle_note_save` with `AuthRequirement::Authenticated`.
  </action>
  <verify>
    <automated>cd /home/oetiker/checkouts/marionette/backend && cargo check -p crm-demo 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep -q 'handle_note_save' backend/crates/crm-demo/src/handlers/note.rs
    - grep -q 'pub mod note' backend/crates/crm-demo/src/handlers/mod.rs
    - grep -q 'note_save' backend/crates/crm-demo/src/main.rs
    - grep -q 'NoteSavePayload' backend/crates/crm-demo/src/handlers/note.rs
    - grep -q 'record_audit' backend/crates/crm-demo/src/handlers/note.rs
    - cargo check -p crm-demo succeeds
  </acceptance_criteria>
  <done>note_save handler exists, validates input, inserts note, records audit, re-renders parent form. Registered in action router.</done>
</task>

<task type="auto">
  <name>Task 2: Add notes section to contact and company edit forms</name>
  <files>
    backend/crates/crm-demo/src/handlers/contact.rs,
    backend/crates/crm-demo/src/handlers/company.rs
  </files>
  <read_first>
    backend/crates/crm-demo/src/handlers/contact.rs,
    backend/crates/crm-demo/src/handlers/company.rs,
    backend/crates/crm-demo/src/entities/note.rs,
    backend/crates/crm-demo/src/entities/user.rs,
    backend/crates/crm-demo/src/handlers/note.rs
  </read_first>
  <action>
Modify `handle_contact_form` in contact.rs to append a notes section AFTER the form (but inside the root container), only in EDIT mode (when contact_id is Some):

1. Add `use crate::entities::{note, user};` and `use sea_orm::{ColumnTrait, QueryFilter, QueryOrder};` to the imports at top of file (some may already be there).
2. After building the form fields and before the final Container build, when in edit mode:
   a. Query notes: `note::Entity::find().filter(note::Column::NoteContact.eq(cid)).order_by_desc(note::Column::NoteCreatedAt).all(&*db.0).await`
   b. For each note, also look up the user name: query `user::Entity::find_by_id(note.note_user).one(&*db.0)` -- or to avoid N+1, load all relevant user IDs in one query first. At demo scale, N+1 is acceptable per Phase 7 decisions.
   c. Build notes section components:
      - `Heading::new("Notes").id("notes-heading")`
      - A `TextInput::new("Add a note...").id("note-input").bind("/noteForm/text")` for new note input
      - A `Button::new("Add Note").id("note-submit").action(ComponentAction::submit("note_save"))` -- the form data at /noteForm/text will be submitted. The note_save handler also needs contact_id, so bind a hidden-like approach: include contact_id in the data at `/noteForm/contact_id`.

      APPROACH: Since the submit action sends all form data from the bound paths, include `contact_id` in the render data under `/noteForm/contact_id`. The TextInput binds to `/noteForm/text`. When the user clicks "Add Note" (submit action), the payload will include `{ "noteForm": { "text": "...", "contact_id": N } }`. BUT the note_save handler expects `NoteSavePayload { contact_id, company_id, text }` -- these need to match.

      BETTER APPROACH: Use `ComponentAction::submit("note_save")` and include the contact_id in the data object at the path the form reads. The submit action sends the data object contents. Structure the data as: `noteForm: { text: "", contact_id: cid }`. The NoteSavePayload should deserialize from the flattened form fields. Actually, submit sends the entire data store contents bound to the form. Check how submit payloads work in this codebase.

      SIMPLEST APPROACH: Wrap the note input + button in a `Form` component. The Form's submit will collect all bound fields within it. Set data: `noteForm: { text: "", contact_id: cid }`. Bind text input to `/noteForm/text`. The contact_id is in the data but needs no visible input -- it just needs to be in the submitted payload. Since ComponentAction::submit sends the data namespace, include contact_id as a value in the noteForm data object. The handler deserializes the payload which includes all noteForm fields.

   d. Render existing notes as a list of Text components, each showing: "[timestamp] author: text". Use `marionette::builders::standard::Text` if available, or `Heading` at a smaller level, or a Container with Text children. The simplest: for each note, create a `Text::new(formatted_string).id(format!("note-{}", note.note_id))`.

   e. Add notes section components to `all_nodes` before the Container build.
   f. Merge noteForm data into the render data: `"noteForm": { "text": "", "contact_id": cid }`

3. Add the same notes section to `handle_company_form` in company.rs for edit mode, but:
   - Filter by `note::Column::NoteCompany.eq(cid)` instead
   - Data key: `noteForm: { "text": "", "company_id": cid }`
   - The note_save handler will receive `company_id` instead of `contact_id`

IMPORTANT: Check what builders are available. The existing imports show: Button, Container, DataTable, Form, Heading, Select, SelectOption, TableColumn, TextInput. There should be a `Text` builder too -- check `marionette::builders::standard`. If Text is not available, use `Heading` with appropriate sizing or just display notes as rows in a simple Container with Heading components at small size. Since the research mentions "Text" builder is available (18 builders total), use `Text::new(content).id(id).build()`.
  </action>
  <verify>
    <automated>cd /home/oetiker/checkouts/marionette/backend && cargo test -p crm-demo 2>&1 | tail -10 && cargo clippy -p crm-demo -- -D warnings 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - grep -q 'notes-heading' backend/crates/crm-demo/src/handlers/contact.rs
    - grep -q 'noteForm' backend/crates/crm-demo/src/handlers/contact.rs
    - grep -q 'note_save' backend/crates/crm-demo/src/handlers/contact.rs
    - grep -q 'NoteContact' backend/crates/crm-demo/src/handlers/contact.rs
    - grep -q 'notes-heading' backend/crates/crm-demo/src/handlers/company.rs
    - grep -q 'noteForm' backend/crates/crm-demo/src/handlers/company.rs
    - grep -q 'NoteCompany' backend/crates/crm-demo/src/handlers/company.rs
    - cargo test -p crm-demo passes
    - cargo clippy -p crm-demo -- -D warnings passes
  </acceptance_criteria>
  <done>Contact and company edit forms display a notes section with: add-note text input + button at top, chronological list of existing notes (newest first) showing text, author, timestamp. Notes are append-only.</done>
</task>

</tasks>

<verification>
- `cargo test -p crm-demo` passes
- `cargo clippy -p crm-demo -- -D warnings` passes
- Contact edit form renders notes section with input, button, and existing notes
- Company edit form renders notes section with input, button, and existing notes
- note_save action is registered in the router
- Notes include author name and timestamp display
</verification>

<success_criteria>
Users can add plain text notes to contacts and companies. Notes display chronologically below the edit form with author and timestamp. The note_save handler validates input, inserts the note, audits the action, and re-renders the parent form.
</success_criteria>

<output>
After completion, create `.planning/phases/08-crm-features/08-02-SUMMARY.md`
</output>
