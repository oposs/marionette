---
phase: 17
plan: 07
type: execute
wave: 2
depends_on:
  - 17-05
  - 17-06
gap_closure: true
files_modified:
  - .planning/phases/17-gallery-crate-skeleton-colocated-built-in-demos/17-VERIFICATION.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: false
requirements:
  - SC-17-05
gaps_addressed:
  - all   # Re-UAT of all 7 gaps + regression guard for the 11 passing demos

must_haves:
  truths:
    - "All 20 nav entries produce a screen (not an error surface, not an empty content area)"
    - "All 7 gaps (G-01 through G-07) are verified closed via Chrome MCP UAT"
    - "The 11 passing demos + home-content continue to render per the VERIFICATION.md behavior audit"
    - "Interactive flows on Modal + ConfirmDialog + Toast work end-to-end (open + dismiss)"
    - "VERIFICATION.md is updated to status=verified, all SCs marked ✅"
    - "ROADMAP.md marks Phase 17 complete; STATE.md rolls forward to Phase 18 hand-off"
  artifacts:
    - path: ".planning/phases/17-gallery-crate-skeleton-colocated-built-in-demos/17-VERIFICATION.md"
      contains: "status: verified"
      provides: "Updated verification report closing SC #5 for all 20 demos"
    - path: ".planning/ROADMAP.md"
      contains: "[x] **Phase 17:"
      provides: "Phase 17 marked complete in roadmap"
    - path: ".planning/STATE.md"
      contains: "Phase 17 complete"
      provides: "STATE.md rolled forward to Phase 18 hand-off with Phase 17 lessons-learned (Task 5)"
  key_links:
    - from: "Phase 17 Plan 05 + 06 fixes"
      to: "Chrome MCP UAT of all 20 nav entries"
      via: "End-to-end re-walk"
      pattern: "20/20"
---

<objective>
Re-run the Chrome MCP UAT walk across all 20 nav entries in the gallery to confirm Phase 17 closure after Plans 17-05 + 17-06 land. Update VERIFICATION.md, ROADMAP.md, and STATE.md.

Purpose: close out Phase 17 Success Criterion #5 and mark the phase complete. This is the gate for Phase 18 (Catalog screens) to start.
Output: Updated VERIFICATION.md with status=verified; ROADMAP.md Phase 17 checkbox checked; STATE.md rolled forward to Phase 18 hand-off.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-gallery-crate-skeleton-colocated-built-in-demos/17-VERIFICATION.md
@.planning/phases/17-gallery-crate-skeleton-colocated-built-in-demos/17-05-PLAN.md
@.planning/phases/17-gallery-crate-skeleton-colocated-built-in-demos/17-06-PLAN.md
@.planning/phases/17-gallery-crate-skeleton-colocated-built-in-demos/17-05-SUMMARY.md
@.planning/phases/17-gallery-crate-skeleton-colocated-built-in-demos/17-06-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full workspace clean-build + lint + test sweep</name>
  <files>(no files modified — gate check only)</files>
  <read_first>
    - backend/Cargo.toml (workspace members)
    - .planning/phases/17-gallery-crate-skeleton-colocated-built-in-demos/17-05-SUMMARY.md
    - .planning/phases/17-gallery-crate-skeleton-colocated-built-in-demos/17-06-SUMMARY.md
  </read_first>
  <action>
    Run the canonical gate sweep to confirm Plans 17-05 + 17-06 landed cleanly:

    ```bash
    cd backend && cargo test --workspace --features gallery 2>&1 | tee /tmp/phase17-test.log
    cd backend && cargo clippy --workspace --features gallery -- -D warnings 2>&1 | tee /tmp/phase17-clippy.log
    cd backend && cargo build --workspace --features gallery --release 2>&1 | tee /tmp/phase17-build.log
    cd frontend && npm run check 2>&1 | tee /tmp/phase17-check.log
    cd frontend && npm run lint 2>&1 | tee /tmp/phase17-lint.log
    cd frontend && npm run test -- --run 2>&1 | tee /tmp/phase17-fe-test.log
    cd frontend && npm run test:browser -- --run 2>&1 | tee /tmp/phase17-fe-browser.log
    ```

    If ANY gate fails: STOP. Report the failure to the orchestrator with the failing log tail. DO NOT proceed to Task 2 until every gate is green.

    Expected pass signals:
    - `cargo test` — all tests across all 6 workspace crates pass (including the Phase 16 `builtin_coverage` tests and all the gallery-demo handler tests)
    - `cargo clippy -D warnings` — no new warnings over Phase 17 plans 01-06 baseline
    - `npm run check` / `lint` / `test` / `test:browser` — all zero-exit
  </action>
  <verify>
    <automated>
      # Gate verification — all commands must exit 0.
      cd backend && cargo test --workspace --features gallery > /dev/null 2>&1 && echo "PASS: cargo test"
      cd backend && cargo clippy --workspace --features gallery -- -D warnings > /dev/null 2>&1 && echo "PASS: cargo clippy"
      cd frontend && npm run check > /dev/null 2>&1 && echo "PASS: npm check"
      cd frontend && npm run lint > /dev/null 2>&1 && echo "PASS: npm lint"
      cd frontend && npm run test -- --run > /dev/null 2>&1 && echo "PASS: npm test"
      # Expect 5 "PASS" lines.
    </automated>
  </verify>
  <done>
    - All 5 gates exit 0
    - No new clippy warnings over Plans 01-06 baseline
    - No new test failures
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Chrome MCP UAT — full 20-demo walk + 7-gap re-verification</name>
  <files>(no files modified — human-verify checkpoint driven via Chrome MCP)</files>
  <action>Execute the Chrome MCP walkthrough described in &lt;how-to-verify&gt;. Automate through the mcp__claude-in-chrome__* tools; do NOT hand the checklist to the user (per feedback_use_chrome_for_uat.md). Start the gallery with `make gallery-dev` in a background shell before driving Chrome. On any failure, pause execution and report — see &lt;resume-signal&gt;.</action>
  <verify>
    <automated>
      cd backend && cargo test --workspace --features gallery > /dev/null 2>&1 && echo "PASS tests"
      cd backend && cargo clippy --workspace --features gallery -- -D warnings > /dev/null 2>&1 && echo "PASS clippy"
    </automated>
  </verify>
  <done>See acceptance criteria in &lt;how-to-verify&gt; — the human-verify resume-signal is the canonical completion gate for this task. Task completes when the user types `approved` after the Chrome MCP walk confirms every expected outcome listed in &lt;how-to-verify&gt;.</done>
  <what-built>Plans 17-05 + 17-06 have landed all 7 gap fixes. This task drives Chrome MCP through the full nav list to confirm 20/20 demos render correctly AND all 7 gaps are closed AND the interactive flows work.</what-built>
  <how-to-verify>
    **Pre-check:**
    - Task 1 (this plan) passed — workspace clean-build + lint + test all green.

    **Start gallery:** `make gallery-dev` (background shell). Confirm the server binds to :3002 and `curl http://localhost:3002/api/health` returns `ok`.

    **Chrome MCP UAT — full walk (drive via the claude-in-chrome MCP tools):**

    Navigate to `http://localhost:3002/` and proceed through every nav entry. Use the 20-entry nav list from the gallery's sidebar; each click loads the demo into the `content` sub-surface.

    **Part A — 20 demos nav walk:** For each demo, screenshot + record pass/fail:

    | # | Demo key | Expected render |
    |---|----------|-----------------|
    | 1 | home | Welcome Heading + 20-tile grid |
    | 2 | app-shell | **G-02 fixed** — static slot preview, outer sidebar intact |
    | 3 | button | 3 buttons (Primary, Disabled, Destructive) |
    | 4 | checkbox | 3 checkboxes with labels |
    | 5 | confirm-dialog | Demo body + trigger button (empty initial state) |
    | 6 | data-table | **G-03 fixed** — 5 rows visible + column headers |
    | 7 | error-display | **G-05 fixed** — red error boxes visible |
    | 8 | field-set | **G-05 fixed** — FieldSet with visible children |
    | 9 | form | Form with TextInputs + Selects + Submit |
    | 10 | grid | 3-col grid A-F |
    | 11 | heading | 3 headings level 1-3 |
    | 12 | modal | Demo body + "Open modal" trigger (empty initial state) |
    | 13 | radio-group | **G-05 fixed** — 3 radio options visible |
    | 14 | select | 2 Selects (Fruit, Disabled) |
    | 15 | smoke | "gallery-smoke" text |
    | 16 | spinner | 3 spinners |
    | 17 | switch | **G-05 fixed** — 2 switches visible |
    | 18 | text | 3 text blocks |
    | 19 | text-input | 3 text inputs |
    | 20 | textarea | **G-05 fixed** — 2 textareas visible |
    | 21 | toast | "Fire toast" button |

    (20 demos + home. The smoke demo is registered by gallery-smoke crate.)

    **Part B — 7 gap re-verification:**

    | Gap | Chrome MCP test |
    |-----|-----------------|
    | G-01 Modal lockup | Click modal → "Open modal" → overlay opens (no tab hang) → click X → overlay closes cleanly |
    | G-02 AppShell hijacks sidebar | Click app-shell → outer gallery sidebar's 20 entries STILL VISIBLE (not replaced) |
    | G-03 DataTable empty | Click data-table → 5 rows visible |
    | G-04 ConfirmDialog dismiss | Click confirm-dialog → "Open confirm" → click Accept → dialog closes + toast "Confirm accepted" appears; repeat with Reject |
    | G-05 Empty demos | Click each of {error-display, field-set, radio-group, switch, textarea} → visible non-empty content |
    | G-06 Home footer oversized | Navigate to home → footer shows small muted text (not large headings) |
    | G-07 Home skeletons | Navigate to home → no grey skeleton bars below footer |

    **Part C — Interactive flow integrity (the 3 interactive demos):**

    1. Modal — open + X-close + Esc-close + click-backdrop-close (each should work without lockup).
    2. ConfirmDialog — open + Accept + open + Reject + open + X-close (all should dismiss cleanly).
    3. Toast — click "Fire toast" 3 times in succession → 3 toasts stack, each with a dismiss action that works.

    **Record evidence:**
    - 20 screenshots (one per demo)
    - 3 GIF/recordings (or sequential screenshots) of the interactive flows
    - Save to `.planning/phases/17-gallery-crate-skeleton-colocated-built-in-demos/evidence/phase17-reuat-*`

    **Expected outcome:** 20/20 demos pass Part A; 7/7 gaps confirmed closed in Part B; 3/3 interactive flows work in Part C. If ANY demo/gap/flow fails, report the specifics — it indicates either (a) an incomplete fix in Plans 17-05 / 17-06, requiring an additional gap-closure iteration, or (b) a new unexpected regression.
  </how-to-verify>
  <resume-signal>
    Type `approved` if 20/20 demos + 7/7 gaps + 3/3 flows all pass.

    Type `blocked: <description>` if any fails. Include the Chrome MCP evidence pointing at the specific failure.
  </resume-signal>
</task>

<task type="auto">
  <name>Task 3: Update 17-VERIFICATION.md with gap-closure results</name>
  <files>.planning/phases/17-gallery-crate-skeleton-colocated-built-in-demos/17-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/17-gallery-crate-skeleton-colocated-built-in-demos/17-VERIFICATION.md (current state with status=uat-incomplete)
    - Task 2 evidence (the 20 screenshots + 3 flow recordings)
  </read_first>
  <action>
    Edit `.planning/phases/17-gallery-crate-skeleton-colocated-built-in-demos/17-VERIFICATION.md`:

    **(a) Frontmatter updates:**
    - `status: uat-incomplete` → `status: verified`
    - Add `closed_by: gap-closure-plans-05-06-07`
    - Update `verified_at` to the date of Task 2's UAT walk (today's date).

    **(b) Success Criteria table — row #5:**

    Change row 5 Status from ❌ **FAIL** to ✅ PASS. Update Evidence cell to cite the Task 2 re-UAT:

    ```
    | 5 | Each built-in demo renders without panicking when visited in the running gallery; clicking every nav entry produces a screen, not an error surface | ✅ PASS | Chrome MCP re-UAT (2026-04-22, Plan 17-07 Task 2): 20/20 demos render correctly; 7/7 gaps closed; 3/3 interactive flows work. See gap-closure section below. |
    ```

    **(c) Add a new section after the Gaps section (insert BEFORE §"Behavior audit"):**

    ```markdown
    ## Gap closure (Plans 17-05 + 17-06 + 17-07)

    All 7 gaps identified in the initial UAT have been closed. Plan 17-07's Task 2 Chrome MCP re-walk confirmed 20/20 demo renders + 7/7 gap fixes + 3/3 interactive flows.

    | Gap | Severity | Closed by | Root cause | Fix summary |
    |-----|----------|-----------|------------|-------------|
    | G-01 | BLOCKER | Plan 17-05 Task 2 + 4 | handle_modal_open emitted `type: "modal"` Component which maps to ModalSurface.svelte → infinite recursion | handle_modal_open now emits a plain Container body; ModalSurface.svelte's isOpen now discriminates empty-Container-as-closed |
    | G-02 | BLOCKER | Plan 17-06 Task 1 | Nested AppShell's Sidebar.Provider collided with outer gallery's Sidebar.Provider | AppShell::gallery_demo rewritten to render a static structural preview with plain Container+Heading+Text (no AppShell builder, no SurfaceMount) |
    | G-03 | BLOCKER | Plan 17-05 Task 3 | fetch_rows emitted N per-row Set ops keyed by id (object-map) but DataTable frontend expects an array | fetch_rows now emits one Set op with the full rows array at the CRM-style path `/tables/demo-rows/rows` |
    | G-04 | BLOCKER | Plan 17-05 Task 2 + 4 | ModalSurface.isOpen = (tree !== undefined) stayed true after empty-Container close-sentinel render | ModalSurface.svelte's isOpen now returns false when root is an empty Container (matches backend close-sentinel contract) |
    | G-05 | BLOCKER | Plan 17-06 Task 3 | Mix of: (a) ErrorDisplay needed .bind() + seed; (b) Switch/Textarea seed paths didn't match demo bind paths; (c) remaining RadioGroup/FieldSet per Task 2 diagnosis | error-display now has .bind + seed_for_key arm; seed_for_key aligned to demo bind paths for Switch/Textarea; remaining demos fixed per Plan 17-06 Task 2's diagnostic note |
    | G-06 | HIGH | Plan 17-05 Task 1 | Footer lines used Heading builder (text-xl font-semibold) not Text | navigate.rs footer now uses Text::new (two lines) |
    | G-07 | HIGH | Plan 17-05 Task 1 | navigate.rs seeded toasts but not modal sub-surface → LoadingSkeleton in modal slot on Home | navigate.rs now seeds modal with empty Container (mirrors toasts seed pattern from CRM:302-309) |

    **Regression guard:** The 11 previously-passing demos (home, button, checkbox, form, grid, heading, select, smoke, spinner, text, text-input, toast) + home-content all continue to pass. See Task 2's 20-screenshot evidence set.
    ```

    **(d) Update the "Gap closure criteria" section at the bottom:**

    Change the bullet list from `Phase 17 is complete when: 1. G-01 through G-05 ...` to a completion statement:

    ```markdown
    ## Gap closure criteria — MET

    Phase 17 is complete:
    1. ✅ G-01 through G-05 fixed (9 demos pass SC #5 on fresh Chrome walk)
    2. ✅ G-06 and G-07 fixed (Home page footer is small muted text; no skeleton bars)
    3. ✅ All 11 currently-passing demos remain passing (no regression — see Task 2 evidence)
    4. ✅ `cd backend && cargo test --workspace --features gallery` exits 0
    5. ✅ `cd backend && cargo clippy --workspace --features gallery -- -D warnings` stays clean
    6. ✅ Chrome MCP UAT walk of all 20 nav entries: 20/20 produce a screen; 3/3 interactive flows work (Modal + ConfirmDialog + Toast)
    ```

    **Do NOT delete any of the original gap descriptions or the behavior-audit section** — they remain as historical record.
  </action>
  <verify>
    <automated>
      grep -c '^status: verified' .planning/phases/17-gallery-crate-skeleton-colocated-built-in-demos/17-VERIFICATION.md
      # Expect: 1
      grep -c '✅ PASS' .planning/phases/17-gallery-crate-skeleton-colocated-built-in-demos/17-VERIFICATION.md
      # Expect: 5 (all 5 SCs now PASS)
      grep -c 'Gap closure (Plans 17-05' .planning/phases/17-gallery-crate-skeleton-colocated-built-in-demos/17-VERIFICATION.md
      # Expect: 1
    </automated>
  </verify>
  <done>
    - VERIFICATION.md frontmatter shows `status: verified`
    - SC #5 row shows ✅ PASS
    - New "Gap closure" section added with 7-row table
    - Gap closure criteria section shows all 6 sub-criteria MET
  </done>
</task>

<task type="auto">
  <name>Task 4: Update ROADMAP.md — mark Phase 17 complete</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - .planning/ROADMAP.md lines 1-250 (find the Phase 17 section and the high-level phase checkbox)
  </read_first>
  <action>
    Edit `.planning/ROADMAP.md`:

    **(a) Toggle the Phase 17 high-level checkbox:**
    - Find `- [ ] **Phase 17: Gallery Crate Skeleton + Colocated Built-in Demos**` (around line 45 per the grep earlier).
    - Change to `- [x] **Phase 17: Gallery Crate Skeleton + Colocated Built-in Demos**`.

    **(b) In the Phase 17 detail section (around lines 187+), mark all 4 original plans + the 3 gap-closure plans as checked:**
    - `- [ ] 17-01-PLAN.md` → `- [x] 17-01-PLAN.md` (and same for 02, 03, 04)
    - Add entries for the 3 gap-closure plans:
      ```
      - [x] 17-05-PLAN.md — Handler + sub-surface fixes (G-01, G-03, G-04, G-06, G-07)
      - [x] 17-06-PLAN.md — Demo body fixes (G-02, G-05)
      - [x] 17-07-PLAN.md — Re-UAT + phase close (all 7 gaps verified closed)
      ```

    **(c) Do NOT touch Phases 18/19/20.** They remain pending.
  </action>
  <verify>
    <automated>
      grep -c '^- \[x\] \*\*Phase 17:' .planning/ROADMAP.md
      # Expect: 1
      grep -cE '^- \[x\] 17-0[1-7]-PLAN\.md' .planning/ROADMAP.md
      # Expect: 7
    </automated>
  </verify>
  <done>
    - Phase 17 high-level checkbox toggled to [x]
    - All 7 plan-level checkboxes toggled to [x]
    - No changes to Phase 18/19/20
  </done>
</task>

<task type="auto">
  <name>Task 5: Update STATE.md — roll forward to Phase 18 hand-off</name>
  <files>.planning/STATE.md</files>
  <read_first>
    - .planning/STATE.md (current state)
    - .planning/ROADMAP.md lines 200-260 (Phase 18 section — gives context for the hand-off note)
  </read_first>
  <action>
    Edit `.planning/STATE.md`:

    **(a) Frontmatter:**
    - `stopped_at: "Phase 17 context gathered"` → `stopped_at: "Phase 17 complete (verified via gap closure plans 05-06-07)"`
    - `last_updated: "2026-04-22T09:36:03.454Z"` → update to today's ISO-8601 UTC timestamp
    - `last_activity: "2026-04-22 -- Phase 17 execution started"` → `"2026-04-22 -- Phase 17 complete (all 7 UAT gaps closed)"`
    - `progress.completed_phases: 1` → `2`
    - `progress.completed_plans: 4` → `7`
    - `progress.percent: 50` → recompute (2/11 phases ≈ 18%; 7/8 plans originally listed … actually update `total_plans` to 11 if new plans 05-07 add to the total, then compute 7/11 ≈ 64%). Use whichever math is consistent with the project's prior calculation convention.

    **(b) Current Position section:**
    - `Phase: 17 (Gallery Crate Skeleton + Colocated Built-in Demos) — EXECUTING` → `Phase: 17 COMPLETE; next: Phase 18 (Catalog screens)`
    - `Plan: 1 of 4` → `Plan: 7 of 7 complete`
    - `Status: Executing Phase 17` → `Status: Phase 17 shipped; ready to start Phase 18`
    - `Last activity` → update with today's date + "Phase 17 UAT gap closure complete"

    **(c) Add a "Phase 17 close-out" section under "Accumulated Context" or "Recent decisions", summarizing:**

    ```markdown
    ### Phase 17 close-out (2026-04-22)

    **Phase 17 (Gallery Crate Skeleton + Colocated Built-in Demos) shipped** after 4 original plans (17-01 through 17-04) + 3 gap-closure plans (17-05 through 17-07). Chrome MCP UAT confirmed 20/20 demos pass SC #5, 7/7 UAT-discovered gaps closed, 3/3 interactive flows (Modal, ConfirmDialog, Toast) work end-to-end.

    **Key Phase 17 lessons carried into Phase 18/19:**

    1. **Modal sub-surface contract** — The `modal` sub-surface tree's root MUST be a plain content component (Container, Heading, Text, ConfirmDialog, etc.), NEVER a `type: "modal"` component (that type maps to ModalSurface.svelte in the frontend registry — rendering it into the modal sub-surface is an infinite recursion). Documented inline in `handlers/modal.rs` and ModalSurface.svelte.

    2. **AppShell nestability status** — Nesting an AppShell inside an outer AppShell's content sub-surface causes shadcn-svelte Sidebar.Provider context collision (inner Sidebar.Root renders at the same viewport position as outer, visually replacing it). Phase 17 ships a static structural preview in AppShell::gallery_demo; Phase 19 EXER-01 owns the real nestability investigation (see `.planning/STATE.md` §Blockers). No framework change made in Phase 17.

    3. **ModalSurface close semantics** — With no ClearSurface protocol message, the close-sentinel is an empty Container rendered into the modal sub-surface. `ModalSurface.svelte`'s `isOpen` check discriminates empty-Container-at-root as closed. Frontend change was minimal (single `isEmptyContainer` helper fn). Documented inline.

    4. **DataTable row-patch shape** — `fetch-rows` handlers MUST emit a SINGLE `Set` op with the full rows array, NOT N per-row Sets keyed by id (those produce an object-map that the frontend DataTable cannot iterate). CRM's `handle_fetch_rows` is the reference pattern; Phase 17's `handle_demo_fetch_rows` now mirrors it.

    5. **Demo-bind / seed alignment** — Demos with `.bind("/demo/{key}/{leaf}")` MUST have a matching `seed_for_key` arm in `show.rs` that writes to THE SAME PATH. Static analysis alone cannot catch typos; Chrome MCP UAT was essential. Phase 18/19 demo authors must cross-check their bind paths against show.rs seeds before submitting.

    ### Phase 18 hand-off

    Phase 18 (Catalog screens — CAT-01 through CAT-05) can start. Depends on Phase 17. See ROADMAP.md §Phase 18 for goals. No blockers carried forward from Phase 17.
    ```

    **(d) Update the "Blockers/Concerns" section:**

    - **AppShell nestability unknown** — Update with: "Phase 17 Plan 17-06 Task 1 confirmed the blocker is real (Sidebar.Provider context collision). AppShell::gallery_demo ships a static preview workaround. **Phase 19 EXER-01 owns the resolution** — likely requires either (a) scoped Sidebar.Provider context in shadcn-svelte, or (b) a scoped-surface-name framework extension."

    - Mark **Phase 20 scope risk** and all other blockers as-is (no change).
  </action>
  <verify>
    <automated>
      grep -c 'stopped_at: "Phase 17 complete' .planning/STATE.md
      # Expect: 1
      grep -c 'Phase 17 close-out' .planning/STATE.md
      # Expect: 1
      grep -c 'Phase: 17 COMPLETE' .planning/STATE.md
      # Expect: 1
      grep -c 'completed_phases: 2' .planning/STATE.md
      # Expect: 1
    </automated>
  </verify>
  <done>
    - STATE.md frontmatter updated to Phase 17 COMPLETE
    - Phase 17 close-out section added with the 5 lessons-learned + Phase 18 hand-off note
    - AppShell nestability blocker updated to reflect Phase 17 findings + Phase 19 ownership
  </done>
</task>

<task type="auto">
  <name>Task 6: Commit the phase close-out</name>
  <files>
    - .planning/phases/17-gallery-crate-skeleton-colocated-built-in-demos/17-VERIFICATION.md
    - .planning/ROADMAP.md
    - .planning/STATE.md
  </files>
  <read_first>
    - git status
    - Recent git log (for commit message style consistency)
  </read_first>
  <action>
    Stage the three docs + commit with a message summarizing the phase close:

    ```bash
    git add .planning/phases/17-gallery-crate-skeleton-colocated-built-in-demos/17-VERIFICATION.md \
            .planning/ROADMAP.md \
            .planning/STATE.md
    git commit -m "$(cat <<'EOF'
    docs(17): phase close — all 7 UAT gaps verified closed

    Phase 17 (Gallery Crate Skeleton + Colocated Built-in Demos) complete after
    gap-closure plans 17-05 (handler fixes) + 17-06 (demo body fixes) +
    17-07 (re-UAT). Chrome MCP re-walk confirmed 20/20 demos pass SC #5;
    7/7 gaps closed; 3/3 interactive flows work end-to-end.

    Docs updated:
    - 17-VERIFICATION.md status=verified; SC #5 marked ✅ PASS
    - ROADMAP.md Phase 17 checkbox toggled; 7 plan checkboxes all checked
    - STATE.md rolled forward to Phase 18 hand-off + 5 lessons-learned

    AppShell nestability blocker confirmed + scope handed to Phase 19 EXER-01.

    Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
    EOF
    )"
    ```

    Do NOT commit any SUMMARY.md files from this plan or Plans 05/06 — those are created by the plan executor per the Phase workflow and committed separately.

    Verify the commit succeeded: `git log --oneline -1`.
  </action>
  <verify>
    <automated>
      git log --oneline -1 | grep -c 'docs(17): phase close'
      # Expect: 1
      git log --oneline -1 2>&1 | head -3
    </automated>
  </verify>
  <done>
    - HEAD commit is the phase-close commit
    - All three docs staged + committed together
    - Working tree clean (for the three docs)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

N/A — this plan only edits documentation and runs verification commands. No new trust boundaries introduced.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17.07-01 | N/A | — | accept | Docs-only plan; no runtime threat surface. |
</threat_model>

<verification>
After all 6 tasks complete:

1. Task 1 gate sweep passed (cargo test + clippy + npm check + lint + test all exit 0)
2. Task 2 Chrome MCP UAT confirmed 20/20 + 7/7 + 3/3
3. VERIFICATION.md shows status=verified
4. ROADMAP.md Phase 17 marked complete
5. STATE.md rolled forward to Phase 18 hand-off
6. Phase-close commit landed
</verification>

<success_criteria>
Plan 17-07 is complete when:
- [ ] All workspace gates (cargo test + clippy + npm check + lint + test) pass
- [ ] Chrome MCP re-UAT: 20/20 demos render correctly
- [ ] Chrome MCP re-UAT: 7/7 gaps confirmed closed
- [ ] Chrome MCP re-UAT: 3/3 interactive flows (Modal, ConfirmDialog, Toast) work end-to-end
- [ ] VERIFICATION.md status=verified with updated gap-closure section
- [ ] ROADMAP.md Phase 17 checkbox toggled
- [ ] STATE.md rolled forward to Phase 18 hand-off with lessons-learned
- [ ] Phase-close commit landed (single commit, three files)
</success_criteria>

<output>
After completion, create `.planning/phases/17-gallery-crate-skeleton-colocated-built-in-demos/17-07-SUMMARY.md` with:
- Full gate-sweep results (Task 1)
- Full Chrome MCP UAT walk evidence (Task 2) — 20 screenshots + 3 flow recordings
- Gap-closure map with before/after evidence per gap
- Confirmation that Phase 17 is shipped and Phase 18 is unblocked
- Any deferred items (Phase 19 EXER-01 ownership of AppShell nestability — documented but NOT a Phase 17 blocker)
</output>
