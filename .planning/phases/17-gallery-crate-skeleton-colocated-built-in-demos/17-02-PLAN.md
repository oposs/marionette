---
phase: 17-gallery-crate-skeleton-colocated-built-in-demos
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/crates/marionette/src/builders/standard.rs
  - backend/crates/marionette/src/builders/mod.rs
  - backend/crates/marionette/src/builders/button.rs
  - backend/crates/marionette/src/builders/text_input.rs
  - backend/crates/marionette/src/builders/select.rs
  - backend/crates/marionette/src/builders/checkbox.rs
  - backend/crates/marionette/src/builders/container.rs
  - backend/crates/marionette/src/builders/grid.rs
  - backend/crates/marionette/src/builders/heading.rs
  - backend/crates/marionette/src/builders/text.rs
  - backend/crates/marionette/src/builders/side_nav.rs
  - backend/crates/marionette/src/builders/nav_item.rs
  - backend/crates/marionette/src/builders/nav_group.rs
  - backend/crates/marionette/src/builders/surface_mount.rs
  - backend/crates/marionette/src/builders/form.rs
  - backend/crates/marionette/src/builders/textarea.rs
  - backend/crates/marionette/src/builders/radio_group.rs
  - backend/crates/marionette/src/builders/switch.rs
  - backend/crates/marionette/src/builders/field_set.rs
  - backend/crates/marionette/src/builders/field_separator.rs
  - backend/crates/marionette/src/builders/data_table.rs
  - backend/crates/marionette/src/builders/modal.rs
  - backend/crates/marionette/src/builders/toast.rs
  - backend/crates/marionette/src/builders/confirm_dialog.rs
  - backend/crates/marionette/src/builders/spinner.rs
  - backend/crates/marionette/src/builders/error_display.rs
  - backend/crates/marionette/src/builders/composites.rs
autonomous: true
requirements: []
# This plan is pure code organization — per D-B3 it sets the stage for DEMO-01
# (plan 04) by giving every builder its own file. No phase REQUIREMENTs are
# directly satisfied by the split itself; plan 04 appends gallery_demo() fns
# to the files this plan creates.

must_haves:
  truths:
    - "Every ComponentBuilder struct + every related props struct lives in its own `backend/crates/marionette/src/builders/<snake_case_name>.rs` file (24 files total)"
    - "The public import paths `marionette::builders::{Button, TextInput, ...}` AND `marionette::builders::standard::{Button, TextInput, ...}` BOTH continue to resolve (re-export shim preserved) — every external caller builds unchanged"
    - "All `standard.rs` tests (700+ lines at lines 666-1398) split by subject into per-component `#[cfg(test)] mod tests` blocks or consolidated meta-tests"
    - "cargo build -p marionette (default) and -p marionette --features gallery both succeed with zero compile errors"
    - "cargo test -p marionette --lib passes with the same total test count as before the refactor (no tests lost in the split)"
  artifacts:
    - path: "backend/crates/marionette/src/builders/button.rs"
      provides: "Button builder struct isolated to its own file"
      contains: "#[component(type = \"button\")]"
    - path: "backend/crates/marionette/src/builders/data_table.rs"
      provides: "DataTable + TableColumn + ColumnKind + Filter all colocated"
      contains: "pub struct DataTable"
    - path: "backend/crates/marionette/src/builders/mod.rs"
      provides: "Re-export hub with all 25 sub-modules + preserved pub mod standard shim"
      contains: "pub mod standard"
    - path: "backend/crates/marionette/src/builders/standard.rs"
      provides: "Re-export shim preserving `builders::standard::*` imports"
      contains: "pub use super::"
    - path: "backend/crates/marionette/src/builders/composites.rs"
      provides: "form_shell() helper relocated from standard.rs"
      contains: "pub fn form_shell"
  key_links:
    - from: "backend/crates/marionette/src/builders/mod.rs"
      to: "each per-component module file"
      via: "pub mod <name>; pub use <name>::*;"
      pattern: "pub mod (button|text_input|select|checkbox|container|grid|heading|text|side_nav|nav_item|nav_group|surface_mount|form|textarea|radio_group|switch|field_set|field_separator|data_table|modal|toast|confirm_dialog|spinner|error_display|composites);"
    - from: "backend/crates/marionette/src/builders/standard.rs"
      to: "all per-component modules"
      via: "pub use super::{module::*, ...}; re-export shim"
      pattern: "pub use super::"
    - from: "backend/crates/marionette/src/builders/data_table.rs"
      to: "backend/crates/marionette/src/builders/select.rs"
      via: "use super::select::SelectOption; (Filter::Select uses SelectOption)"
      pattern: "use super::select::SelectOption"
---

<objective>
Execute the per-component file refactor (CONTEXT.md D-B3): split `backend/crates/marionette/src/builders/standard.rs` (currently 1398 lines, 19 ComponentBuilder structs + helpers + tests) into 25 per-component files under `builders/`, one ComponentBuilder struct per file, with related props structs colocated (SelectOption with Select, RadioOption with RadioGroup, TableColumn/ColumnKind/Filter with DataTable). Preserve the `marionette::builders::standard::*` import path via a `pub use` re-export shim (Option A from RESEARCH.md §Pattern 5) — this keeps all 10 external call sites (crm-demo + gallery-smoke + marionette's own app_shell.rs) building without edits.

Purpose: Plan 04's 19-demo sweep needs a per-component home for each `#[gallery_demo] pub fn gallery_demo() -> Vec<Node>` sibling. A single 1100+-line file (standard.rs + 18 sibling demos) would be unmaintainable. One-shot mechanical split now pays forward: every future component addition colocates its builder + tests + demo in a single file.

Output: 25 new files under `backend/crates/marionette/src/builders/` (24 per-component + `composites.rs`), rewritten `builders/mod.rs` declaring all modules with `pub use` re-exports, reduced `builders/standard.rs` as a 1-block `pub use super::{…::*}` shim. All existing tests split by subject OR consolidated in a meta-test module.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-gallery-crate-skeleton-colocated-built-in-demos/17-CONTEXT.md
@.planning/phases/17-gallery-crate-skeleton-colocated-built-in-demos/17-RESEARCH.md
@.planning/phases/17-gallery-crate-skeleton-colocated-built-in-demos/17-PATTERNS.md
@backend/crates/marionette/src/builders/standard.rs
@backend/crates/marionette/src/builders/mod.rs
@backend/crates/marionette/src/builders/app_shell.rs
@backend/crates/marionette/src/builders/node.rs

<interfaces>
Current `builders/mod.rs` (7 lines):

```rust
pub mod node;
pub mod standard;
pub mod app_shell;

pub use node::*;
pub use standard::*;
pub use app_shell::*;
```

Struct → file map (from PATTERNS.md §Cluster 2, verified against standard.rs lines 2026-04-22):

| File              | Struct(s) + colocated props           | Source lines in standard.rs |
|-------------------|---------------------------------------|-----------------------------|
| button.rs         | Button                                | 11-21                       |
| text_input.rs     | TextInput                             | 23-46                       |
| select.rs         | SelectOption + Select                 | 49-53 + 69-97               |
| checkbox.rs       | Checkbox                              | 99-116                      |
| container.rs      | Container                             | 120-125                     |
| grid.rs           | Grid                                  | 127-134                     |
| heading.rs        | Heading                               | 138-144                     |
| text.rs           | Text                                  | 146-150                     |
| side_nav.rs       | SideNav                               | 154-156                     |
| nav_item.rs       | NavItem                               | 158-165                     |
| nav_group.rs      | NavGroup                              | 167-171                     |
| surface_mount.rs  | SurfaceMount                          | 175-185 (incl. doc comment) |
| form.rs           | Form                                  | 189-194                     |
| textarea.rs       | Textarea                              | ~196-226 (incl. doc)        |
| radio_group.rs    | RadioOption + RadioGroup              | 55-67 + 228-253 (incl. doc) |
| switch.rs         | Switch                                | 255-277 (incl. doc)         |
| field_set.rs      | FieldSet                              | 279-309 (incl. doc)         |
| field_separator.rs| FieldSeparator                        | 311-317 (incl. doc)         |
| data_table.rs     | TableColumn + ColumnKind + Filter + DataTable | 320-532            |
| modal.rs          | Modal                                 | 534-542 (incl. doc)         |
| toast.rs          | Toast                                 | 544-552 (incl. doc)         |
| confirm_dialog.rs | ConfirmDialog                         | 554-559 (incl. doc)         |
| spinner.rs        | Spinner                               | 561-566                     |
| error_display.rs  | ErrorDisplay                          | 568-572                     |
| composites.rs     | form_shell() helper fn                | 574-664 (incl. doc + tests) |

Cross-struct imports (from RESEARCH.md §Pattern 5 "Cross-struct dependencies"):
- `data_table.rs` needs `use super::select::SelectOption;` (Filter::Select uses SelectOption)
- `composites.rs` needs `use super::{button::Button, container::Container, form::Form, heading::Heading};`
- No other cross-struct imports inside standard.rs.

External callers of `builders::standard::*` (from RESEARCH.md §Pattern 5 audit, verified 2026-04-22):
1. backend/crates/crm-demo/src/main.rs:19
2. backend/crates/crm-demo/src/handlers/audit.rs:6
3. backend/crates/crm-demo/src/handlers/company.rs:6
4. backend/crates/crm-demo/src/handlers/contact.rs:9
5. backend/crates/crm-demo/src/handlers/interaction.rs:4
6. backend/crates/crm-demo/src/handlers/user.rs:6
7. backend/crates/crm-demo/tests/integration_test.rs:11
8. backend/crates/gallery-smoke/src/lib.rs:16
9. backend/crates/gallery-smoke/tests/ui/fail_wrong_signature.rs:1
10. backend/crates/gallery-smoke/tests/ui/fail_not_pub.rs:1
11. backend/crates/marionette/src/builders/app_shell.rs:244 (internal, via `crate::builders::standard::...`)

Target `builders/mod.rs` (Option A shim — from PATTERNS.md §11):

```rust
pub mod node;
pub mod app_shell;

// Per-component builder modules (Phase 17 D-B3 refactor).
pub mod button;
pub mod text_input;
pub mod select;
pub mod checkbox;
pub mod container;
pub mod grid;
pub mod heading;
pub mod text;
pub mod side_nav;
pub mod nav_item;
pub mod nav_group;
pub mod surface_mount;
pub mod form;
pub mod textarea;
pub mod radio_group;
pub mod switch;
pub mod field_set;
pub mod field_separator;
pub mod data_table;
pub mod modal;
pub mod toast;
pub mod confirm_dialog;
pub mod spinner;
pub mod error_display;
pub mod composites;

// Preserved re-export shim (Option A from RESEARCH.md §Pattern 5).
pub mod standard;

pub use node::*;
pub use app_shell::*;
pub use button::*;
pub use text_input::*;
pub use select::*;
pub use checkbox::*;
pub use container::*;
pub use grid::*;
pub use heading::*;
pub use text::*;
pub use side_nav::*;
pub use nav_item::*;
pub use nav_group::*;
pub use surface_mount::*;
pub use form::*;
pub use textarea::*;
pub use radio_group::*;
pub use switch::*;
pub use field_set::*;
pub use field_separator::*;
pub use data_table::*;
pub use modal::*;
pub use toast::*;
pub use confirm_dialog::*;
pub use spinner::*;
pub use error_display::*;
pub use composites::*;
```

Target `builders/standard.rs` (shim — from PATTERNS.md §11):

```rust
//! Re-export shim for pre-Phase-17 import paths.
//!
//! `marionette::builders::standard::Button` etc. continue to resolve here
//! after the D-B3 per-component file refactor (Option A — preserved
//! import surface for the 10 external callers enumerated in RESEARCH.md).

pub use super::{
    button::*, text_input::*, select::*, checkbox::*,
    container::*, grid::*, heading::*, text::*,
    side_nav::*, nav_item::*, nav_group::*, surface_mount::*,
    form::*, textarea::*, radio_group::*, switch::*,
    field_set::*, field_separator::*, data_table::*,
    modal::*, toast::*, confirm_dialog::*,
    spinner::*, error_display::*,
    composites::*,
};
```

Per-file template (from PATTERNS.md §Pattern F):

```rust
//! `<Component>` component builder.
//!
//! Split from `standard.rs` in Phase 17 D-B3.

use marionette_macros::ComponentBuilder;
// [additional uses as needed per component — serde for SelectOption/RadioOption,
//  cross-module imports for composites.rs and data_table.rs]

// [struct definition: copy verbatim from standard.rs using the line range]

#[cfg(test)]
mod tests {
    use super::*;
    // [tests moved from standard.rs by subject]
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Split standard.rs into 25 per-component files + write re-export hub mod.rs + shim standard.rs</name>
  <files>
    backend/crates/marionette/src/builders/standard.rs,
    backend/crates/marionette/src/builders/mod.rs,
    backend/crates/marionette/src/builders/button.rs,
    backend/crates/marionette/src/builders/text_input.rs,
    backend/crates/marionette/src/builders/select.rs,
    backend/crates/marionette/src/builders/checkbox.rs,
    backend/crates/marionette/src/builders/container.rs,
    backend/crates/marionette/src/builders/grid.rs,
    backend/crates/marionette/src/builders/heading.rs,
    backend/crates/marionette/src/builders/text.rs,
    backend/crates/marionette/src/builders/side_nav.rs,
    backend/crates/marionette/src/builders/nav_item.rs,
    backend/crates/marionette/src/builders/nav_group.rs,
    backend/crates/marionette/src/builders/surface_mount.rs,
    backend/crates/marionette/src/builders/form.rs,
    backend/crates/marionette/src/builders/textarea.rs,
    backend/crates/marionette/src/builders/radio_group.rs,
    backend/crates/marionette/src/builders/switch.rs,
    backend/crates/marionette/src/builders/field_set.rs,
    backend/crates/marionette/src/builders/field_separator.rs,
    backend/crates/marionette/src/builders/data_table.rs,
    backend/crates/marionette/src/builders/modal.rs,
    backend/crates/marionette/src/builders/toast.rs,
    backend/crates/marionette/src/builders/confirm_dialog.rs,
    backend/crates/marionette/src/builders/spinner.rs,
    backend/crates/marionette/src/builders/error_display.rs,
    backend/crates/marionette/src/builders/composites.rs
  </files>
  <read_first>
    - backend/crates/marionette/src/builders/standard.rs (entire 1398-line file; use Grep first if needed for specific structs)
    - backend/crates/marionette/src/builders/mod.rs (the 7-line file being rewritten)
    - backend/crates/marionette/src/builders/app_shell.rs (lines 1-20 — for the module-doc + imports + lint-preamble shape that per-component files mirror; and line 244 — `use crate::builders::standard::{Container, Heading, SideNav, SurfaceMount};` which MUST continue to resolve)
    - backend/crates/marionette/src/builders/node.rs (unchanged; read to confirm the `Node` alias signature)
    - .planning/phases/17-gallery-crate-skeleton-colocated-built-in-demos/17-PATTERNS.md §Cluster 2 (File Classification table), §Pattern F (per-component file skeleton), §11 (mod.rs + standard.rs target shapes)
    - .planning/phases/17-gallery-crate-skeleton-colocated-built-in-demos/17-RESEARCH.md §Pattern 5 (struct inventory with line ranges, external caller audit, cross-struct dependency list)
  </read_first>
  <action>
**Step 1 — Create the 25 per-component files.** For each entry in the Struct → file map (see `<interfaces>`), create `backend/crates/marionette/src/builders/<file>.rs` with:

1. A 2-line module doc comment: `//! \`<StructName>\` component builder.\n//!\n//! Split from \`standard.rs\` in Phase 17 D-B3.`
2. A `use marionette_macros::ComponentBuilder;` line.
3. Additional `use` lines ONLY for what the struct actually needs:
   - `select.rs` + `radio_group.rs`: add `use serde::{Deserialize, Serialize};` (SelectOption / RadioOption derive Serialize/Deserialize — see standard.rs line 7 for original import)
   - `data_table.rs`: add `use serde::{Deserialize, Serialize};` AND `use super::select::SelectOption;` (Filter::Select uses SelectOption)
   - `data_table.rs`: add whatever additional `use` standard.rs uses for Filter/ColumnKind (check lines 320-486)
   - `composites.rs`: add `use super::{button::Button, container::Container, form::Form, heading::Heading};` per PATTERNS.md §Pattern F
4. The struct definition(s), copied VERBATIM from the specified source-line range in `standard.rs`. Keep attribute order: `#[derive(ComponentBuilder)] #[component(type = "...")] pub struct ...`. Preserve every doc comment, every `#[builder(optional)]`, every `#[serde(skip_serializing_if = "...")]` — no editorial rewriting.
5. For `select.rs`: `SelectOption` (struct from standard.rs lines 49-53) comes FIRST in the file, then `Select` (lines 69-97).
6. For `radio_group.rs`: `RadioOption` (lines 55-67) FIRST, then `RadioGroup` (lines 228-253).
7. For `data_table.rs`: in order — `TableColumn` + its impl (lines 320-370), `ColumnKind` enum (lines 374-388), `Filter` enum + impl (lines 394-486), `DataTable` + `impl DataTableBuilder` (lines 488-532).
8. For `composites.rs`: `form_shell` fn (lines 620-664 from standard.rs).

NO `#[cfg(feature = "gallery")]` or `gallery_demo()` additions in this plan — Plan 04 appends those separately.

**Step 2 — Split the tests from `standard.rs` lines 666-1398.** For each test fn, identify its subject struct by inspecting which builder/type it constructs:
- `button_builder` test → `button.rs` `#[cfg(test)] mod tests` block
- `text_input_builder` + any other `text_input_*` → `text_input.rs`
- `select_*` → `select.rs`
- ... and so on for each struct

The canonical pattern for each per-component test module is:
```rust
#[cfg(test)]
mod tests {
    use super::*;
    use marionette_protocol::ComponentAction;  // or whatever imports the tests need

    #[test]
    fn <test_name>() { ... }
}
```

For the `all_19_standard_types` meta-test (standard.rs lines 1079-1110 per PATTERNS.md Task 1 note): move it into `backend/crates/marionette/src/builders/mod.rs` inside a `#[cfg(test)] mod tests` block at the bottom. It's the "one place that proves every builder still works" and doesn't belong under any single component. Update the asserted type list if needed to reference the re-exports (`Button`, `TextInput`, ...) — all resolve via `pub use` from mod.rs.

For any test that uses multiple builders (e.g., `form_shell` tests that combine Heading + Button + Form), put it in the file of its PRIMARY subject (`composites.rs` for `form_shell` tests).

If any test is ambiguous (touches 2+ equally primary subjects), put it in the meta-test block in `mod.rs`.

**Step 3 — Rewrite `backend/crates/marionette/src/builders/mod.rs`** to the target shape specified in `<interfaces>` above. The hub declares every per-component module with `pub mod <name>;`, retains `pub mod standard;` for the shim, and emits `pub use <name>::*;` for every sub-module plus `pub use node::*;` and `pub use app_shell::*;` as before. The meta-test `all_19_standard_types` goes inside `#[cfg(test)] mod tests { ... }` at the bottom of this file.

**Step 4 — Reduce `backend/crates/marionette/src/builders/standard.rs`** to the pure re-export shim shape specified in `<interfaces>` above. The body is:
```rust
//! Re-export shim for pre-Phase-17 import paths.
//!
//! `marionette::builders::standard::Button` etc. continue to resolve here
//! after the D-B3 per-component file refactor (Option A — preserved
//! import surface for the 10 external callers enumerated in RESEARCH.md).

pub use super::{
    button::*, text_input::*, select::*, checkbox::*,
    container::*, grid::*, heading::*, text::*,
    side_nav::*, nav_item::*, nav_group::*, surface_mount::*,
    form::*, textarea::*, radio_group::*, switch::*,
    field_set::*, field_separator::*, data_table::*,
    modal::*, toast::*, confirm_dialog::*,
    spinner::*, error_display::*,
    composites::*,
};
```

Nothing else — no struct definitions, no tests (the tests have all moved to per-component files or mod.rs meta-block), no helpers (form_shell moved to composites.rs).

**Step 5 — Do NOT edit any of the external caller files.** The 10 sites listed in `<interfaces>` MUST keep their `use marionette::builders::standard::{...};` line EXACTLY as-is — the Option A shim preserves the path. Verify by running the full workspace build in Task 2.

**Step 6 — Do NOT edit `backend/crates/marionette/src/builders/app_shell.rs`.** Its `use crate::builders::standard::{Container, Heading, SideNav, SurfaceMount};` at line 244 MUST keep resolving via the shim. Plan 04 is the one that appends `gallery_demo()` to app_shell.rs — this plan does not touch it.

Per user's global memory §feedback_pre_deployment_no_backcompat: the shim is an intentional clean public API, NOT a deprecation shim. Do not add `#[deprecated]` attributes or "will be removed in v1.3" comments to `standard.rs`. It stays as a co-equal re-export path.

Per CONTEXT.md §Claude's Discretion: the shim (Option A) is the chosen path — it avoids a 10-file caller edit sweep for zero functional benefit.
  </action>
  <verify>
    <automated>cd backend && cargo build -p marionette && cargo build -p marionette --features gallery</automated>
  </verify>
  <acceptance_criteria>
- `ls backend/crates/marionette/src/builders/*.rs | wc -l` returns 28 (24 per-component + composites + node + app_shell + mod.rs + standard.rs; standard.rs still exists as shim)
- `grep -l 'pub struct Button' backend/crates/marionette/src/builders/*.rs` returns exactly `backend/crates/marionette/src/builders/button.rs` (Button is in button.rs and nowhere else)
- `grep -l 'pub struct DataTable' backend/crates/marionette/src/builders/*.rs` returns exactly `backend/crates/marionette/src/builders/data_table.rs`
- `grep -l 'pub struct SelectOption' backend/crates/marionette/src/builders/*.rs` returns exactly `backend/crates/marionette/src/builders/select.rs`
- `grep -l 'pub struct RadioOption' backend/crates/marionette/src/builders/*.rs` returns exactly `backend/crates/marionette/src/builders/radio_group.rs`
- `grep -l 'pub fn form_shell' backend/crates/marionette/src/builders/*.rs` returns exactly `backend/crates/marionette/src/builders/composites.rs`
- `grep -c 'pub mod ' backend/crates/marionette/src/builders/mod.rs` returns 27 (node + app_shell + standard + 24 per-component modules — NOTE: composites.rs counts as one of the 24 per-component for simplicity but is really a 25th; recount against the actual written file and verify it lists: node, app_shell, button, text_input, select, checkbox, container, grid, heading, text, side_nav, nav_item, nav_group, surface_mount, form, textarea, radio_group, switch, field_set, field_separator, data_table, modal, toast, confirm_dialog, spinner, error_display, composites, standard)
- `grep -c 'pub use super::' backend/crates/marionette/src/builders/standard.rs` returns 1 (the single re-export block in the shim)
- `wc -l backend/crates/marionette/src/builders/standard.rs` returns fewer than 25 lines (pure shim, no structs or tests remain)
- `cd backend && cargo build -p marionette` exits 0
- `cd backend && cargo build -p marionette --features gallery` exits 0
- `grep -rn 'pub struct' backend/crates/marionette/src/builders/standard.rs` returns ZERO matches (no struct definitions remain in the shim)
  </acceptance_criteria>
  <done>
standard.rs is reduced to a 1-block re-export shim; all 19 ComponentBuilder structs + form_shell helper live in 25 per-component files; `builders/mod.rs` is the re-export hub wiring every module; the crate builds both under default and `--features gallery`. External callers (crm-demo, gallery-smoke, app_shell.rs) are untouched and continue to resolve `builders::standard::*` imports via the shim.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Run workspace-wide test + clippy sweep to confirm refactor is regression-free</name>
  <files></files>
  <read_first>
    - backend/crates/marionette/src/builders/mod.rs (just rewritten in Task 1)
    - backend/crates/marionette/src/builders/standard.rs (just reduced to shim in Task 1)
    - .planning/phases/16-framework-hooks/16-VERIFICATION.md §Integration Sanity (baseline test + clippy expectations from Phase 16 — this plan must NOT introduce regressions vs that baseline)
  </read_first>
  <behavior>
    - Test 1: `cargo test -p marionette --lib` — all pre-refactor marionette lib tests still pass (same count, same tests). Run WITHOUT `--features gallery` first since the refactor is feature-agnostic.
    - Test 2: `cargo test -p marionette --lib --features gallery` — all tests still pass with the gallery feature enabled (ensures feature-gated builders aren't accidentally hidden behind the wrong cfg).
    - Test 3: `cargo test --workspace --exclude crm-demo --features gallery` — full workspace sweep (excluding pre-existing crm-demo clippy drift). gallery-smoke's registry_roundtrip + ui_errors both green; marionette's no_gallery_symbols still green.
    - Test 4: `cargo clippy -p marionette --lib --all-features -- -D warnings` — zero clippy warnings on the refactored lib.
    - Test 5: `cargo build -p crm-demo` — crm-demo (which uses `marionette::builders::standard::{...}`) still compiles via the shim. crm-demo has pre-existing clippy drift (per Phase 16 VERIFICATION.md §Open Issues) so we skip clippy on it, but it MUST compile.
  </behavior>
  <action>
Run the five verification commands below in sequence. Abort and fix root cause on first failure — do NOT mask issues with `#[allow(...)]`.

**Step 1:** `cd backend && cargo test -p marionette --lib`

If this fails with "test X not found", the test migration in Task 1 Step 2 missed a test — use grep to locate the test in `standard.rs` pre-split state (git show) and move it to the correct per-component file. Re-run.

**Step 2:** `cd backend && cargo test -p marionette --lib --features gallery`

Should yield identical test count + result to Step 1 (no `#[cfg(feature = "gallery")]`-gated tests exist in this plan — Plan 04 adds those).

**Step 3:** `cd backend && cargo test --workspace --exclude crm-demo --features gallery`

Phase 16 baseline expectations (from VERIFICATION.md §Integration Sanity):
- marionette lib ≥4 gallery tests
- marionette-macros ≥7 title_case tests
- gallery-smoke registry_roundtrip ≥4 tests
- gallery-smoke ui_errors ≥4 trybuild fixtures
- marionette no_gallery_symbols ≥2 tests

Plus the refactored marionette lib's per-component tests (total test count must equal or exceed the pre-refactor standard.rs test count). If a test is missing post-refactor, it was dropped during Task 1 Step 2 — locate and restore.

**Step 4:** `cd backend && cargo clippy -p marionette --lib --all-features -- -D warnings`

Common failure modes:
- Unused imports in a per-component file (remove the `use` line)
- Missing `use serde::{Deserialize, Serialize}` in select.rs / radio_group.rs / data_table.rs (add it)
- `clippy::module_inception` if a module is named the same as its parent (shouldn't happen with snake_case names)
- `clippy::missing_docs_in_private_items` is NOT on by default; do not trip.

Do NOT silence warnings with `#[allow(...)]` unless the original `standard.rs` already had the same `#[allow(...)]` for that item. Per user's global memory, fix root causes.

**Step 5:** `cd backend && cargo build -p crm-demo`

crm-demo uses 7 different `use marionette::builders::standard::{...}` lines (per the external-caller audit). If any fail to resolve, the shim in `builders/standard.rs` is missing a re-export — add the missing module to the `pub use super::{…::*}` block.

**Step 6 (sanity, no edits):** `grep -rn 'use marionette::builders::standard::' backend/` should return at least 10 lines — confirming no caller migrated away from the `standard::*` path (that's Option A's whole point).

This task modifies NO files — if Task 1 was done correctly, this passes green. If any step fails, go back to Task 1 and fix the root cause there.
  </action>
  <verify>
    <automated>cd backend && cargo test -p marionette --lib && cargo test -p marionette --lib --features gallery && cargo test --workspace --exclude crm-demo --features gallery && cargo clippy -p marionette --lib --all-features -- -D warnings && cargo build -p crm-demo</automated>
  </verify>
  <acceptance_criteria>
- `cd backend && cargo test -p marionette --lib` exits 0
- `cd backend && cargo test -p marionette --lib --features gallery` exits 0 with the same test count as the previous command
- `cd backend && cargo test --workspace --exclude crm-demo --features gallery` exits 0 with zero failures
- `cd backend && cargo clippy -p marionette --lib --all-features -- -D warnings` exits 0 with zero warnings
- `cd backend && cargo build -p crm-demo` exits 0 (shim preserves all crm-demo imports)
- `grep -rn 'use marionette::builders::standard::' backend/` returns ≥10 matches (external callers are unchanged)
- `git diff --stat backend/crates/marionette/src/builders/` shows exactly 27 files changed: 1 rewritten mod.rs, 1 shrunken standard.rs, 25 new per-component files (app_shell.rs and node.rs are UNCHANGED by this plan)
- `git diff --stat backend/ | grep -v builders/` shows ZERO files changed outside `marionette/src/builders/` (external callers untouched)
  </acceptance_criteria>
  <done>
The per-component file refactor compiles cleanly under default and `--features gallery`; all pre-existing tests pass (none lost); clippy zero-warning; crm-demo and gallery-smoke continue to build against their unchanged `builders::standard::*` imports via the Option A shim. Plan 04 now has a file per in-scope builder to append its `gallery_demo()` sibling into.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Compile-time only (no runtime trust boundary) | This plan is pure code organization — moving structs between files within a library crate. No runtime behavior changes, no input validation changes, no new public API surface. All struct/field visibility is preserved exactly. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17.02-01 | Tampering | `builders/mod.rs` re-export hub | accept | An attacker with write access to mod.rs could hide a struct from the re-export, breaking downstream code — but that's a correctness bug, not a security issue. Mitigated by Task 2 Step 3 (workspace test) + Step 5 (crm-demo build check) which would surface any missing re-export as a compile failure. |
| T-17.02-02 | Denial of Service | Compile-time cost of 25-file builders/ module | accept | More files = marginally more rustc parsing, but compile time remains dominated by macro expansion (ComponentBuilder derives). No measurable impact; rebuild cost is bounded by incremental compilation touching only changed files — usually better than the 1398-line monolith. |
| T-17.02-03 | Info Disclosure | Struct visibility after split | mitigate | Every `pub struct ...` stays `pub`; every `#[builder(optional)]` stays; every serde attribute stays. Task 1 Step 1 "copy VERBATIM" mandate + Task 2 clippy-zero-warning bar catches any accidental visibility regression. |
</threat_model>

<verification>
Plan-wide verification is the 5-command Task 2 sweep. The refactor is correct when:
1. Every marionette lib test passes (no tests lost)
2. The gallery-enabled workspace passes
3. Zero clippy warnings on the refactored lib
4. crm-demo continues to build via the shim
5. Grep confirms external callers are untouched

No runtime behavior change — this plan introduces zero new code paths, zero new wire messages, zero new handlers. Every ComponentBuilder's generated `new(...).build()` method returns the same `(String, Component)` tuple as before.
</verification>

<success_criteria>
Plan complete when:
- [ ] 25 per-component files exist under `backend/crates/marionette/src/builders/` (24 builder files + composites.rs)
- [ ] `builders/mod.rs` declares every sub-module with `pub mod <name>;` + re-exports with `pub use <name>::*;`
- [ ] `builders/standard.rs` is reduced to a single `pub use super::{…::*};` block (the shim)
- [ ] All 10 external callers of `marionette::builders::standard::*` still compile unchanged
- [ ] `cargo test --workspace --exclude crm-demo --features gallery` exits 0 with all tests green
- [ ] `cargo clippy -p marionette --lib --all-features -- -D warnings` exits 0 with zero warnings
- [ ] No back-compat `#[deprecated]` attributes; the shim is an intentional co-equal path
</success_criteria>

<output>
After completion, create `.planning/phases/17-gallery-crate-skeleton-colocated-built-in-demos/17-02-SUMMARY.md` following the GSD summary template. Highlight:
1. The 25-file split outcome with a compact table (file → struct(s) colocated)
2. The cross-struct imports introduced (data_table.rs → select.rs via SelectOption; composites.rs → button/container/form/heading)
3. The shim-preservation strategy (Option A) with a git-diff-stat-style summary (27 files changed in `builders/`, 0 files changed outside)
4. Test-migration notes: which tests moved to which per-component file, which tests landed in the meta-test mod inside mod.rs
5. Hand-off note for Plan 04: each in-scope file now has a natural home for its `#[cfg(feature = "gallery")] #[gallery_demo(key = "...")] pub fn gallery_demo() -> Vec<Node>` sibling at the bottom.
</output>
